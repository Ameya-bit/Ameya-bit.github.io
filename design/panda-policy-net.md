# The learned line-walker — engineering an emergent world model for the hat panda

*Drafted 2026-07-24; task upgraded to the viewing game the same day; lit-reviewed 2026-07-26; **rewritten 2026-07-26 (v3) as a phased implementation plan.** Expands Phase 6 of [panda-chaos.md](panda-chaos.md).*

*Status: **Phase A is DONE — character gate passed 2026-07-27 and the homepage default is now the ported engine** (`pandas.js` kept unmodified behind `?engine=old` as the reference). Parity gate green at batch `d4a2d47b` @ 32 seeds × 10k ticks and `bfbc8a5c` @ 32 × 60k. **Phase B is DONE (2026-07-27): its exit is met — the corpora are cut (`train-wild`, 840 × 12000 = 10.08M ticks, 15.2 GB; `eval-natural`, 120 × 12000, 1.7 GB, both labelled), the roster is frozen by a test rather than a paragraph, encoder parity is green, and rollouts clear the throughput bar.** B1–B4 and B6 are done and the `.stop` hit box landed before the cut; B5 (the worker pool) stays open as a Phase-E want, not a Phase-B need. **Phase C is DONE (2026-07-27): C1 (the game), C2 (the yardstick gate), C3 (the twin-episode battery), C4 (the game-shape iteration) and C5 (the risk economy) are complete, and all three exit checks pass** — `node evaluate.js --gate`, game v3. **Check 1: PASS**, memory gap **87%** of the oracle against a 30% threshold, measured over the plan's own memoryless twin; C5 demoted the conservative reading to a diagnostic (15%) after measuring that it is not a bound — it goes **−19% on `wild`**, and one unprincipled planner knob walks it 17% → 6% while the full gap never leaves 77–126%. **Check 2: PASS**, both action-space holes closed in the engine by C4 (stride cadence, roll cooldown — worth +26% and +22%), every reward exploit below the reactive ceiling. **Check 3: PASS**, unmoved by two economies. C5's one rule change: `knockPenalty` 40 → 20, because **85% of what it charged for was ambient** (`still` pays 425 of the oracle's 501-point knock bill), which also closed C1's two open findings — the crowded-regime one turned out to be a claim about the incumbent, since on `dense` the oracle beats `still` by 157/min. ⚠️ C4 **voids a published C2 claim**: the yardsticks were priced against `DEFAULT_RULES` rather than the rules of the episode, so every knob sweep — including "the gap stays at ~5% under everything" — measured policies that had never seen the knob.*

***Phase D is BUILT (2026-07-28). Two of its four exits pass; two fail, both measured rather than pending.** ✅ JS↔PyTorch action agreement **24,000/24,000** (bar: 99.9%), ✅ weights **237.5 KB** (bar: ~400 KB). ❌ The forward pass is **p99 2 ms in the browser** against a 1 ms bar (Node's 567 µs mean flatters it ~3.5× at the tail). ❌ The character gate **failed** — "terrible at dodging" — so the clone stays **opt-in behind `?policy=nn`** and the page keeps the rules watcher. Neither failure blocks Phase E and both have their fix inside it: the latency answer is a Web Worker (a 10 Hz decision arriving a tick late is invisible; a 2 ms main-thread tail is not), and the dodging answer is the reward. D0 found and fixed a real defect first: every corpus paired each action with the frame it *produced*, which handed a clone 93.3% of its direction label on the hat panda's own facing — all four corpora were re-cut against shard v2, and no Phase-C number moves. The clone learned the reflex and not the navigation, which is the imitation gap localised: `roll_recall` **0.620** and direction **57.8%** against 12.5% chance, but online it is floored **2.04/min against the expert's 1.23** — not by failing to dodge (0.098 rolls per danger-frame against 0.103) but by **walking 45% more**, because `hat.js` paces every external action at the expert's *alert* cadence. **Phase E is underway (2026-07-28): E1, E0 and E2 are done — see the Phase-E progress notes.** The deployed `?policy=nn` weights are now the partial-obs, delay-trained clone (E1); the on-policy bridge runs at 176–201k decisions/s against a 25–50k bar (E0); and the memory architecture is picked — per-slot GRU — priced under throttle before training (E2). E3 (the PPO trainer) is next; F–G not started. Live detail is in the progress notes below and, authoritatively, in [`assets/pandas/engine/README.md`](../assets/pandas/engine/README.md), [`trainer/README.md`](../trainer/README.md) and [`trainer/py/README.md`](../trainer/py/README.md); prefer those files over this line if they ever disagree.*

***The goal was reframed, and the pre-E groundwork landed (2026-07-28).** The primary deliverable is the **website**, not the writeup: a transformer visibly piloting the hat panda, and a world model a visitor can actually see. Three consequences, decided with Ameya. **The belief overlay is co-equal with the policy** — a policy that works is behaviourally indistinguishable from the rules watcher to anyone but Ameya (the character gate's own pass condition is "looks the same"), so without a window into the model, success is invisible; Phase G's overlay moves from appendix to product. **The deployment budget is a Phase-E design constraint**, not a Phase-F discovery. And the **multi-seed / purity-arm rigor becomes optional** — one good policy and one probe set good enough to draw the overlay ships the site; the supervised-belief fallback is a legitimate ship path (a visitor cannot distinguish an emerged belief from a supervised one in the overlay), with emergence still the main arm because it is the interesting version. Four pieces of groundwork shipped the same day: **(1) the Web Worker path** — `?policy=nn` now runs the forward pass off the main thread on a **pipelined** schedule: the frame from decision k produces the action applied at k+1, a fixed schedule rather than a race, so behaviour cannot depend on the visitor's hardware; `?policy=nn-main` keeps the synchronous driver for A/B, misses fall back to the rules expert, and a machine that cannot keep up retires the policy for the visit — progressive enhancement by construction. Measured in headless Chrome: worker round-trip p99 **0.7 ms** against the **100 ms pipeline deadline** (~71× headroom; `tools/policy-bench.html` now measures both arms). **(2) The delay contract, everywhere a policy runs**: `delayPolicy` in the rollout harness, `--delay` on evaluate.js, `--delay` through train/eval/export on the Python side with the value recorded in the exported manifest. Priced on the shipped clone (trained synchronous, run delayed): **−64.7 → −68.6/min and 2.01 → 2.42 knocks/min** — a reflex firing off a 100 ms-stale picture. A policy *trained* under the delay can anticipate; one merely run under it cannot. D0's lesson, applied before RL instead of after. **(3) The overlay infrastructure** (`render/overlay.js`, `?overlay=truth` on the page and on `tools/stage.html`): per-panda belief chips behind a provider seam — ground truth is the dev tenant, the probes are the intended one, and the flagship sleeper-vs-knocked distinction is loud by design. **(4) The parity artefacts moved to `trainer/parity/`** — everything under `assets/pandas/` is a Quarto site resource, and a local render was sweeping 250 MB of test fixtures into `_site`. The dodging failure is untouched by all of this; it is the reward's to fix, in Phase E.*

*The v3 decision: the world model must **emerge from playing the game** — no supervision on hidden state, ever. The supervised alternative (train belief heads directly on logged ground truth; near-guaranteed to produce "that's a sleeper, ~9s left" as a literal network output, with the debug overlay for free) was weighed 2026-07-26 and set aside. It survives as the **documented fallback** if the emergence run comes up dry, and shares ~80% of the infrastructure below — sim, corpora, logging, export, evals, even the probe tooling. Only Phase E actually commits to emergence; nothing built before it is wasted either way.*

---

## The pitch

Replace the hand-written maneuvering rules (sidestep + dive-roll + weave) with a small transformer that learns to walk the line itself — trained only on the **viewing game** (+points per tick spent within `R_VIEW` of an actively anomalous panda, −large penalty per knockdown, **no incident feed**), from partial egocentric observation, with memory. Structured like OthelloGPT: a training task that cannot be done well without reconstructing hidden state — here, each neighbour's anomaly FSM. Then probed as a real interp project, shipped as the hat panda's brain, and written up as the site's flagship post. The self-portrait goes fully recursive: a mech-interp site whose mascot is a trained policy, whose brain the site's author then opens up.

The policy owns **where to be** (locomotion + attention — what's worth walking to and viewing, that's the game). The **presentation layer stays hand-authored** on top: gaze flourish, plant/inspect poses, the hat-fetch skit, all the character beats. Decisions learned; look hand-made.

## What emergence can and cannot guarantee (the frame the plan hangs on)

You cannot guarantee a *representation*; you can guarantee a *computation*. If the game is engineered so that the achieved score is information-theoretically impossible without inferring the hidden state, then any network reaching that score is computing the latent *somewhere*. So the plan engineers two things separately:

1. **Necessity — hard guarantees available.** Close every reactive and degenerate path to a good score, then *measure* that they're closed:
   - **The reactive yardstick.** Train a deliberately memoryless twin (same architecture, 2-frame window). Its score is the **reactive ceiling**; a privileged **oracle** policy (fed true state) sets the upper bound. The gap between them — the **memory gap** — is the exact quantity of score only a world model can claim. Small gap = the game has a shortcut = *fix the game, not the model*. The gap is the project's health meter from Phase C on.
   - **Twin-episode certificates.** Matched episode pairs identical in every current observable, differing only in hidden history. Flagship: two pandas lying in the same fall cels — one a sleeper (no witnessed collision), one freshly knocked. A policy that approaches one and ignores the other has *behaviorally proven* the event-memory inference, no probes required. One battery per knowability tier.
2. **Legibility — strong pressure, no guarantee.** Diversity along every latent axis, small-but-deep-enough architecture, per-panda token addresses, training long past score saturation. The residual risk — the belief exists but is smeared and entangled rather than linear and compact — is irreducible. It is also what makes this an experiment instead of a construction.

**The purity dial (D2 — defaulted).** Three readings of "emerged from playing": **(1)** reward-only; **(2)** reward + predicting *his own future observations* (action-conditioned, multi-step — no labels, no privileged information; an agent anticipating its own sensory stream is still learning from play); **(3)** model-based RL (world model architecturally mandated — the letter of emergence, not the spirit). **Default: setting 2 is the main arm; setting 1 runs alongside on matched seeds as the aspiration check.** They differ by one loss term, so the purist result comes free if reward-only gets there. The POMDP literature is blunt that setting 1 alone usually yields near-optimal behavior *without* a clean belief state (appendix).

## ⚠️ Required changes to the live simulation (read before anything else)

Everything below Phase A trains against the engine, so the engine moves first. In order of invasiveness:

1. **Fixed-tick determinism refactor — the big one (Phase A).** The engine currently runs on wall-clock time (rAF/timer callbacks, ms-denominated constants like the 0.17px/ms zoomies speed, loops polling at 400ms). It becomes a **fixed 20Hz integer-tick engine**: all `*_MS` and px-per-ms constants re-expressed per-tick; `Math.random()` replaced everywhere in sim logic by a seeded PRNG (mulberry32) carried *inside* sim state; no `Date.now()` in state-affecting code; transcendentals (`Math.sin/exp/pow`) wrapped for swappability. The renderer interpolates between ticks. **Risk: subtle feel drift across every behaviour that was ms-tuned.** Gate: Ameya's preview judgment ([[verify-animations-static-only]]) plus side-by-side trace comparison.
2. **Engine/presentation split (Phase A).** `pandas.js` splits into a pure, DOM-free `step(state, actions) → state` (movement, collision, director, all 8 anomaly FSMs, stack, cascade, hat physics) and a presentation layer (DOM, cels, gaze flourish, skits). Behavior-preserving refactor; golden-trace CI proves browser bundle ≡ Node import byte-for-byte.
3. **Hat-panda action seam + i-frames (Phase A).** The sim accepts the **17-way discrete action interface** (hold / step×8 / dive-roll×8) at 10Hz for the hat panda, and the shipped rules watcher+dodge is wrapped to emit through the *same* interface — expert and policy become plug-swappable and BC targets are exact by construction. The dive-roll gains **i-frames** (agreed 2026-07-24: committed escape; skill moves into *when*). Note this slightly changes live behaviour before any NN exists.
4. **Randomization/config hooks (Phase A–B).** Anomaly duration distributions, kind mix, `ANOM_GAP`, panda count, spawn placement become sim config with defaults = today's live values. The live site is unaffected; training corpora need broad randomization — diversity is a load-bearing emergence lever (legibility forms only along axes the training distribution varies).
5. **Roster freeze (exit of Phase B).** The 8 kinds lock; any new anomaly after corpora are cut forces a retrain.
6. **D1 — field of view: RESOLVED (2026-07-26) → heading-cone FOV.** Touches only the observation encoder, **not** the engine — so this list's engine scope is final: items 1–5 are the complete set of live-sim changes.

**Explicitly not changing:** the other pandas' logic and art, the incident feed (stays, for rules mode and presentation), scoring (`R_VIEW`, caps, bonuses live entirely trainer-side — the live site never computes score), two-way collidability (D4: stays, with an exploit watch in Phase E).

## The game, hardened for emergence

Base score: **+points per tick within `R_VIEW` of a panda whose anomaly is active; −large penalty per knockdown.** Hardening — all trainer-side, every item exists to move score from *reaction* to *inference*:

- **Pay for anticipation, not presence:** early-arrival multiplier (or pay only from arrival onward), so a late walk to a nearly-done nap earns ~nothing. The money moves into "worth the trip?" — the duration posterior.
- **Per-anomaly diminishing returns and/or score cap:** kills camping structurally (no penalty tuning), and mechanizes principle #3 of [panda-chaos.md](panda-chaos.md) — he watches a while, then *moves on*.
- **Small per-step movement cost:** sharpens every trip into a wager on time-remaining.
- **Danger asymmetry does classification pressure:** approaching a zoomies risks the knockdown penalty, a sleeper is free — misclassification is expensive, so kind-inference is consulted continuously.
- **Pre-training geometry audit** for the known degenerate optima: `R_VIEW`-intersection parking, spawn-centroid camping, corner-cowering (watch the penalty/reward ratio), episode-extension exploits. Every closed shortcut widens the memory gap; every open one is an off-ramp from inference.

The knowability spectrum stays the spec's spine, now with teeth at each tier:

| Tier | Example | What forces it | Certificate |
|---|---|---|---|
| Fully inferable | kind + kinematics (zoomies dangerous, sleeper free points, starer vs ordinary idle only via history) | can't score at all without it | twin episodes + probes (positive control) |
| Statistically inferable | duration posterior ("will the nap outlast my walk?") | anticipation pay + movement cost | calibration curves; skip/commit behavior |
| Provably uninferable | cascade arming (zero observable signature until ignition) | nothing — that's the point | negative control; a probe that "finds" it is finding leakage |

**Flagship discrimination:** sleeper vs freshly-knocked — near-identical fall cels, decidable only by remembering whether something collided with it first. Event-memory-into-state; the canonical "you cannot do this reactively."

## Observation model — and D1, the FOV decision

The policy sees only what a panda could see: egocentric neighbour positions + observable pose class per frame, own proprioception (position vs walls + hero-card fence, hat on/off, roll cooldown). No velocities, no anomaly flags, no director state, no incident feed. Per-panda tokens with **persistent slot identity** (sticky k-nearest with hysteresis) — otherwise the net must solve identity binding before it can infer any FSM, a confound, not the study.

**D1 — how much does he see at once? RESOLVED (Ameya, 2026-07-26): variant (b), the heading cone.** Variants kept for the record:

- **(a) Full field** (as originally drafted): all pandas observable every frame. Memory pays only for event history and duration priors — there is no object permanence to learn, because everything is re-derivable by looking. Weakest emergence pressure; zero changes anywhere.
- **(b) Heading-cone FOV — recommended:** he observes pandas within a cone about his heading (plus a short omnidirectional "peripheral" radius). Object permanence becomes real: he must *track* the sleeper he's walking toward while watching elsewhere for threats, carry decaying estimates of pandas not seen for seconds. This is the strongest single emergence lever available, and it hands Phase G its cleanest probe — decodable state for *currently-unseen* pandas can only be memory. Implementation is an observation-encoder mask; **no engine change, no action-space change**; gaze flourish stays cosmetic. In character: he sees where he's looking.
- **(c) Controlled gaze:** look-direction becomes part of the action space (policy chooses where to point the cone independently of heading). Maximal pressure and attention-becomes-literal-attention poetry — but it extends the action space, breaks BC (the rules expert has no gaze policy to clone), requires engine support, and fights the authored gaze presentation. Not for v1; revisit as an add-on era idea.

## Model & training — locked decisions

- **Architecture: 4 layers minimum, d64–128, per-panda tokens, pre-LN, no dropout.** Four layers is the floor, not a sweep endpoint — shallow models hold decodable-but-*causally-unused* state (appendix: the Othello depth study); we need the belief wired into action selection. Width stays tight: overparameterization is what bred OthelloGPT's bag of per-square heuristics. Context 100–200 tokens (strided), with one hard constraint: **the window must cover the knock-to-classification horizon**, or the flagship discrimination is impossible by construction. A GRU-over-window fallback stays warm (small-scale PPO literature: GRU matches or beats attention; if the transformer won't train, the project survives).
- **BC from the rules expert = locomotion/style prior only.** The expert is privileged (reads the incident feed), so naive cloning teaches superstitious beelining and zero information-gathering — the imitation gap, formally established (appendix). RL owns every go/no-go decision. When the Stage-D clone confidently walks to invisible targets, that's the documented phenomenon, not a bug.
- **RL recipe:** critic-only warmup (actor frozen) until advantages are sane, then unfreeze at ~10× lower LR; **KL-to-frozen-BC replaces the entropy bonus** — anti-forgetting, anti-hacking, and the believability anchor in one term; conservative PPO (clip 0.1–0.2, 1–2 epochs/batch, advantage normalization); curriculum via cranked anomaly density; potential-based shaping only if reward starves.
- **The leash schedule is a first-class knob:** the KL anchor actively fights emergence — the expert never gathers information, so a tight leash taxes exactly the scanning/hedging behaviors the belief state exists to drive. Tight early (stability), **deliberately annealed loose mid-training** (information-seeking must be discoverable), re-tightened late if character drifts.
- **Auxiliary loss (setting 2 only):** action-conditioned, multi-step prediction of *his own future observations*. No time-to-live head, no state labels — those are the fallback's tools, and they'd contaminate the emergence claim.
- **Deployment: sample from the softmax (T ≤ 1), never argmax.** Determinism reads as a drone; the KL-anchored policy's entropy is BC-derived character for free.
- **Seeds are budget:** 3–5 policy seeds per arm, 5 probe seeds per probe, distributions reported. Ship-candidate selected jointly on score + twin battery + legibility.
- **Believability is measured, not vibes-only:** action-switch rate, direction reversals, path smoothness, reaction-time distributions vs the expert's traces, tracked per checkpoint. The character gate ([[verify-animations-static-only]] — Ameya judges all motion) still precedes the interp gate; if optimal play reads wrong, the page wins and the post reframes.

## Phases

Each phase is independently shippable or independently informative, with hard exit results. A and D are worth having even if the project stops after them.

### Phase A — engine groundwork *(the only phase that touches the live site's code)*
**Build:** sim changes #1–#3 above (+ #4 config plumbing); golden-trace CI; a lint ban on `Math.random`/`Date.now` inside engine code.
**Done when:**
- ✅ The live site runs on the extracted engine — **it is now the default** (`?engine=old` keeps the original). The character gate passed 2026-07-27, on its second round; what round one caught is recorded below.
- ✅ Golden traces: 32 seeds × 10k ticks, per-tick checksums byte-identical, browser vs Node import (`d4a2d47b`; `bfbc8a5c` @ 60k).
- ✅ The rules watcher+dodge runs through the 17-action seam at 10Hz (i-frames live).
- ✅ Every engine constant is tick-denominated; renderer interpolates between the two held ticks.

> **Progress — Phase A COMPLETE (2026-07-27). The port is now the site's default; the untouched `pandas.js` stays as the reference behind `?engine=old`.** *(The milestone notes below were written as the work landed, when the port was still the opt-in `?engine=new`.)* *(This note is a running log, so the digests and test counts inside each milestone are that milestone's, not today's — the current pair is `d4a2d47b` @ 10k / `bfbc8a5c` @ 60k, 170 engine tests; `916ea37b`/`24a89cd2` below are M6's, from before the glide fix moved them.)* New code lives under `assets/pandas/engine/` (buildless ES modules; dev tooling `node --test`, zero deps). See that dir's `README.md` for the live status table and `design/panda-engine-map.md` for the behavioral spec the port validates against. **Done so far:** the determinism foundation (seeded PRNG in state, wrapped transcendentals, 50 ms/20 Hz tick clock, FNV golden-trace harness Node+browser, determinism lint); the full behavioral map of `pandas.js`; **sim-core port M1** (world + wander + model-space collision + knock/recover — the original's collision reads the smooth glide position, so entities carry a logical stride position + a visual position that eases toward it, collision on the visual); **sim-core port M2** (the tier-1 director + incident queue + all 8 anomaly FSMs as per-tick state machines); **sim-core port M3** (the hat panda — attention picker over the incident queue with stickiness/abandonment, observe = relocate/weave/plant/sidestep, the reflex + dive-roll **now with i-frames**, all behind the **17-way action seam** `step(state, action)` with the applied action logged on `hat.action` as the exact BC target; `watcher.js` = the rules "expert" `rulesAction`, `hat.js` = the engine that executes an action, `actions.js` = the vocabulary); **sim-core port M4** (the two multi-panda set pieces — **tier-2 stack**: one tower at a time, the assembly as an explicit `MOUNT → FLIGHT → PARADE` phase machine, the base a `solid` actor with riders re-pinned each tick and a sway that accumulates up the tower as a function of tick count, toppling on its clock or on a zoomies strike into an *ordinary* three-way knock; **tier-3 cascade**: the arming clock, ignition by the next natural collision or a topple (the coupling rule), a greedy nearest-neighbour sweep with each faller steered at its own next domino, coverage capped at 70–90% so the oblivious one plus a few stragglers always survive, the recursive `after(hop)` timers now a tick-keyed `pending` queue, plus the liveness backstop and the "erupt away from his gaze" hold). The cascade's tier-3 incident is the first whose subject is a **place, not a panda** (`POINT_SUBJECT`) — the watcher walks to the origin and stands among the bodies. **Gaze flourish + hat-drop/fetch skit deferred to M5** as hand-authored presentation per the plan — a planted hat just faces its subject; a knocked hat keeps its hat and recovers into observing. **The sim is now feature-complete against `pandas.js`** — everything left in [the map](panda-engine-map.md) is §10, the presentation layer. 108 tests at that point, deterministic (golden batch `e4bb5a41` @ 32×10k — superseded twice since). **Density decision (Ameya):** count scales with viewport to hold `areaPerPanda ≈ 217000` (`layout.js`); pairs with Phase B's density randomization. **Port M5 — the presentation layer — done 2026-07-27, and with it Phase A's build work.** Everything lives under `assets/pandas/engine/render/`: the art baked verbatim out of `pandas.js` by `tools/bake-art.js` (sprite rows, hat pixels + seats, the five seated cels — never hand-edited); a renderer that holds two ticks and draws between them (the CSS glide is *gone*, the engine owns it) with cels free-running at the original's 140 ms except where the sim owns the progress (dive-roll, mount hop, hiccup pop); seated riders corrected per facing onto the drawn head (48–54 px) rather than the engine's flat `riderRise = 62`, eased in across the hop; a host owning the fixed-timestep loop, pause, resize re-framing and the visit's seed; the reduced-motion tableau built as an ordinary state so the ordinary renderer draws it; and the two deferred beats — the gaze flourish (a drawn facing, never state) and **the hat-drop/fetch skit, which drives the 17-way action seam itself** rather than smuggling a second movement system into the sim (while it owns him the expert doesn't run, so he can't dodge mid-fetch — as in the original). `index.qmd` now picks its engine from `?engine=old|new`, default `old`. 129 tests at that point. **⚠️ The parity gate earned its keep on its first real run: Node 25 and Chrome disagree on `Math.sin` by one ULP** (seed `-626627309`, tick 5189 — a stack rider's x off by 1e-13 through the wobble, never reconverging). `mathx.js` — the module that existed precisely to be this swap point — now computes `sin`/`cos` from IEEE-754-pinned arithmetic, `exp`/`pow`/`atan2` are unexported, and `x ** 2` (Math.pow semantics, same hazard) is lint-banned in favour of `mathx.sq`. **Gate then passed: `6acd01f7` @ 32×10k and `711c07e4` @ 32×60k, identical Node and Chrome** (both superseded by M6's digests) (`tools/parity.mjs` re-runs it and bisects any mismatch to seed/tick/encode slot). **Port M6 — the entrance — done 2026-07-27** (Ameya's call, taken now on purpose: the roster/config freeze is at the *exit* of Phase B, so a sim change costs a day today and a training run later). `MODE.ENTERING` behind `cfg.entrance`: the troupe starts off-stage and walks on, the hat panda alone first with his 1.8 s lead, then waves of two; each arrival target doubles as that panda's `home` (so the oblivious one's patch is where it walked in to, as in the original). An entering panda is a collision ghost, invisible to all three directors, and is the one thing in the sim that moves with the bounds/fence clamp bypassed — its corridor begins outside the stage. **It is engine config, not a host script, because the corpora need it:** it is the first ~20 s of every episode a deployed policy meets and the calmest one (no anomaly fires for 9 s), which is exactly where relax beats and camping exploits live; training can still open mid-scene with `entrance: false`. Digest moved to `916ea37b` @ 32×10k, parity re-confirmed. 135 tests. **Then the character gate passed on its second round and the default flipped** (both rounds are recorded below).

> **The character gate, both rounds (2026-07-27).** Round one failed on two reports — "they're gliding on ice, holding momentum they have to overturn" and "pandas phase right through the hat panda, but sometimes there is a collision" — which turned out to be **one bug with two halves, both in the glide**. (a) The port modelled `transition: transform 2s` as an exponential chase, `x += (lx - x) * 0.08`. That has the same average lag and none of the shape: the original writes the transform only when the logical position changes, so every stride *restarts* the `ease` curve — surge, settle, and a turn that commits immediately. Fixed by porting the transition literally (`cfg.glideTicks`, `mathx.cssEase`, sampled once per config). (b) Fixing the engine did not fix the page, because `styles.scss` still carries the old engine's `.panda_wrapper { transition: transform 2s }` for `?engine=old`, and `render/pandas.css` merely *omitted* a transition — the cascade resolves conflicting declarations, not absent ones. So the browser animated the engine's transforms a second time: measured 22 px/s drawn against ~44 px/s simulated, and the drawn body sat tens of pixels from the hitbox that collides, which is exactly why pandas appeared to pass through each other. Fixed by specificity (`.panda_engine` on the stage). **Two lessons worth keeping: the dev pages (`tools/stage.html`, `tools/preview.html`) never load `styles.scss` and so never showed it — anything about how the engine looks must be judged on the real page; and a presentation-layer regression can silently move what the sim collides on, so "presentation only" is not a safety argument.** The collision economy was then measured against the original by polling both pages in a browser for 10 minutes each (the original is DOM/timer-driven, so a browser is its only clock): at matched density the port floors the watcher **3.20 times a minute against the original's 4.00** (33% vs 40% of his life on the ground), and on a real 2560×1343 monitor 0.80 against 1.70 — the port is consistently *gentler*, which is the dive-roll's i-frames working as intended, with an identical 5.6 s median knock confirming the recovery FSM is faithful. Also measured: the old engine spawns a flat 10 pandas on any desktop (`W >= 1200 ? 10 : 7`) while the port scales with free area, giving 11 on that monitor — a deliberate density decision (2026-07-26), not a port artefact, and the knob if the field ever reads too busy. Knock rate turns out to depend violently on stage *area*: the same 10 pandas go from 4.0/min to 1.7/min between a 1600×900 and a 2560×1343 window, which is worth remembering when Phase B randomises density.

### Phase B — trainer + corpus infrastructure *(no site impact)*
**Build:** Node rollout harness (parallel workers); per-tick ground-truth logging (every anomaly's FSM kind/phase/timers, cascade arm, `cascadeLock` claims) aligned for future activation capture; JSONL trajectory export; corpus configs — natural distribution, cranked density, fully randomized; observation encoder frozen and bit-matched (`Math.fround` at the boundary, cross-language unit tests).
**Done when:** rollouts ≥ ~50k ticks/s/core; a ≥10M-tick randomized training corpus + natural-distribution eval corpus cut and versioned; encoder parity tests green; **roster frozen (change #5).**

**Done — all four, 2026-07-27:**
- ✅ Rollouts 116k ticks/s/core on the binding spec; the end-to-end cut (rollout + encoder + ground truth + shard) runs 42–49k, recorded as a near-miss rather than rounded up.
- ✅ `train-wild` — 840 × 12000 = **10.08M ticks**, 5.04M rows, 15.20 GB, labelled, 6–28 pandas per episode. `eval-natural` — 120 × 12000, 720k rows, 1.72 GB, labelled. Versioned by manifest: shards are gitignored, manifests are not, and `--verify` re-cuts an episode to prove the bytes are a cache of them.
- ✅ Encoder fixture check green, and the fixture's digest is now recorded *in every manifest* (see below).
- ✅ Roster frozen — `trainer/test/freeze.test.js`.

> **Progress — B1 done (2026-07-27), in [`trainer/`](../trainer/README.md).** Zero deps, `node --test`, imports the shipped engine directly rather than vendoring it, so a corpus is always a recording of the machine the visitor sees. `rollout.js` runs one episode into a sink (episode = pure function of seed + config; recording defaults to the policy's 10 Hz clock, not the engine's 20 Hz); `corpus.js` holds the three specs as pure functions of a PRNG — `natural` (the live distribution, the eval corpus), `dense` (curriculum), `wild` (the training corpus: stage, density, cadence, duration priors and both set-piece clocks all move, half the episodes opening mid-scene). 17 tests, including one that checks every key every spec can emit against `DEFAULT_CONFIG` — `makeConfig` is a shallow merge that silently ignores unknown keys, so a typo would train on shipped defaults instead of throwing.
>
> **The throughput bar is already met on one core, with room:** 377k ticks/s on `natural`, 116k on `wild`, **96k on `dense`** (the binding one) against a 50k bar — a 10M-tick corpus is ~1.7 core-minutes. Cost is dominated by O(n²) collision, hence per-spec reporting. **Consequence: B5's worker pool is off the critical path for the corpus cut** — it is still wanted for Phase E's on-policy rollouts, where the step count is billions, so it moves after B6 rather than before it.
>
> **Progress — B2 done (2026-07-27): the observation encoder, in [`assets/pandas/engine/policy/obs.js`](../assets/pandas/engine/policy/obs.js).** One `Float32Array` frame per decision tick: a self token of proprioception (facing, drawn pose, roll cooldown, clearance to the four walls, signed offsets to the hero card) plus 8 neighbour tokens, 37 columns each, 333 floats a frame. **It lives in the engine rather than in `trainer/` because Phase D ships it** — the in-browser policy encodes its own observations from the same state the renderer draws, so trainer and page must run one implementation, inside the determinism lint's reach. D1's heading cone is implemented as `coneDeg` (120°) about his facing out to `sightRange` (520 px), plus a `peripheralR` (130 px) omnidirectional stub, plus fence occlusion (the card is opaque, via the watcher's own `crossesFence`). **Measured over 4×10-minute episodes per spec: he sees a mean of 1.7 (natural) to 3.7 (wild) pandas at once out of 7–17 on the field, holds 0.6–1.4 more from memory alone, and sees nobody at all in 3–12% of frames** — the numbers D1 exists to produce, and Phase C's first knobs on the memory gap. Slot occupancy p95 is 8–10 with slots uncapped, so **8 slots truncates a real tail on `wild`** (that is k-nearest working, and 9 tokens/frame keeps a 16-frame window inside the plan's 100–200 context budget).
>
> Three properties are pinned by tests rather than by intent. **(1) The sensor may read of another panda only what `render/renderer.js` reads** — a read-tracking proxy enforces it, and the flagship consequence has its own test: *a sleeper and a freshly-knocked panda encode to identical bytes*, as do all 8 anomaly kinds sharing a drawn pose. **(2) The encoder remembers identity and nothing else.** A slot stays bound through `holdTicks` (2 s) out of sight — otherwise the net must solve identity binding before it can infer any FSM, a confound rather than the study — but an unseen panda's token drops to `present = 1` and zeros. A decaying position estimate in the sensor would be the belief we are trying to watch emerge, pre-built. **(3) Frozen and bit-matched:** all arithmetic is +,−,×,÷ and `sqrt` into a `Float32Array` (the same rounding `Math.fround` performs) plus one pinned `cos` at construction, and `policy/obs-fixture.json` turns that into a portable contract — four (world, slot memory) → (frame, slot memory) cases carrying only the observable projection of a real state, so a Python/WASM port can be checked without a simulator. `tools/obs-fixture.js --check` runs in the test suite; the layout freezes with the roster at the exit of Phase B, since changing it invalidates a cut corpus. Positions are the **visual** ones (the drawn body, which is also what collides): the rules expert reasons on the logical stride grid and so sees a step into the future, and the policy deliberately does not. Encoding costs ~4% of rollout throughput (110k observed ticks/s on `dense`), so the corpus cut is unaffected. 168 engine tests, 23 trainer tests.
>
> **Progress — B3 done (2026-07-27): per-tick ground truth, in [`trainer/truth.js`](../trainer/truth.js).** Everything the encoder refused, recorded as labels that never reach a policy: each anomaly's kind/sub-phase/timers, the knock FSM, the director's clock and incident queue, the stack machine, the cascade's arm flag / claims / pending falls, the true positions and hidden gaits, and the rules expert's own attention. It doubles as the **Phase-C oracle's input** — the privileged upper bound reads exactly this record. `GLOBAL_FIELDS`/`ENTITY_FIELDS` are the schemas in column order, asserted against the emitted records so B4's manifest cannot drift from the data.
>
> **The design point is the second pass.** Kind and phase are readable off one tick; **time-remaining is not**, and it is the tier the anticipation economy rests on. Deriving it from the FSM constants would re-implement `anomalies.js` in a second place *and still be wrong* — a zoomies ends early when it finds a wall, a spinner draws its stagger count only when the spin ends. So `scanEpisode` walks the episode once keeping only behaviour-change ticks (a timeline of spans, a few hundred numbers), and `recordEpisode` re-runs it emitting rows with `age`/`ttl`/`nextMode` looked up there. Determinism makes the re-run free and exact; pass two re-checks every panda's mode against the span it lands in, so a timeline from a different seed or config throws rather than quietly poisoning the labels. **Two labels are deliberately unlearnable and kept as negative controls:** `cascadeIgnitesIn` (arming has no observable signature — a probe that finds it has found leakage) and a wanderer's `ttl`, which is the tick the director reaches in and picks it.
>
> `recordEpisode` emits **aligned** rows — `{ tick, action, obs, truth }` — and `truth.slots` records which panda each observation token addressed, which is what "aligned for future activation capture" has to mean in practice: Phase G scores "token 3 believes X" against "panda 7 is X" instead of guessing the join. **Throughput with the whole pipeline in the loop: 56k recorded ticks/s on `dense`** (the binding spec) against the 50k bar — recording costs ~2× because of the second pass, so the margin narrowed from 2× to 12% and a 10M-tick corpus is ~3 core-minutes. Still off the critical path for B5's worker pool; worth watching. 36 trainer tests.
>
> **Progress — B4 done (2026-07-27): the shard writer, in [`trainer/shard.js`](../trainer/shard.js) and [`trainer/cut.js`](../trainer/cut.js).** A shard is a **rectangle** — `rows × width` little-endian float32, no header, no framing — so the whole loader is `np.fromfile(path, '<f4').reshape(-1, width)`. That costs one constraint, a constant row width, and the row width is *not* constant across a corpus because panda count is a per-episode draw. **So the unit of a file is one episode**, which is the natural grain anyway: an episode is already the unit of determinism, and so the unit that can be re-cut alone, verified alone, and (B5) written by its own worker with nothing to coordinate. A row is `obs | action | global | slots | entities`, with entity sub-row *k* pinned to panda id *k* — the join a Phase-G probe needs — and everything is float32 including the labels, because two dtypes would mean two reads and an alignment rule. Integer labels are exact below 2²⁴ and that is asserted per episode rather than assumed — on the first row *and the last*, since `tick` only grows and the first row is by construction the safest one there is; true positions are the one lossy field, ~1e-4 px, and exactly recoverable by re-running.
>
> **The manifest is the corpus.** The shards are gitignored and the manifest is not, which is a claim, not a convention: an episode is a pure function of (seed, config), the seeds come from one root, and the configs come from a named spec and that same root — so the manifest holds every input and the bytes are a cache of them. Which means nothing in a manifest may be a clock or an outside path: cut the same corpus twice and the two files are byte-identical, and a test pins it. `cut.js --verify` executes the claim by re-cutting one episode and comparing seed/rows/width/bytes/digest, and it also compares a short **golden digest of the engine taken at cut time**, so a corpus cut before a sim change announces itself instead of quietly mixing with one cut after. `<name>.sample.jsonl` ships beside the manifest in git — a dozen rows spread across the first episode with every field named, decoded back *out of the shard's bytes* rather than re-rendered from memory (also pinned by a test), because a format nobody can look at is a format nobody checks. Ground truth can be dropped with `--no-truth` for a pure BC corpus, which is 44% of the disk on `natural` and more on a crowded world.
>
> **Throughput, honestly: rollouts clear the ≥50k ticks/s/core bar with room (116k on the binding spec), but the end-to-end cut lands at 42–44k — about 16% under it.** Most of that step is not computation: a labelled `wild` episode is ~18 MB, and writing plus digesting the bytes is roughly two thirds of the gap (hashing 122 MB measures 0.12 s). It changes no decision — a 10M-tick corpus is ~4 core-minutes and B5's pool stays off the critical path — but it is recorded as a miss rather than rounded into a pass. `--dry-run` prices a cut exactly before it runs: **the B6 training corpus (`wild`, 840 × 12000 = 5.04M rows) is 15.2 GB with truth, 6.7 GB without; the eval corpus (`natural`, 120 × 12000) is 1.7 GB.** Two small additions to the engine came with it, both pure and neither moving the golden digest (`f557d268` @ 32×10k): `ACTION_NAME`/`actionName` in `actions.js` (the manifest's action vocabulary; Phase D's debug overlay will want it too) and `hashBytes` in `tools/checksum.js` — the same FNV-1a fed bytes that need no canonicalising, since a shard's float32 rows *are* the artefact. 51 trainer tests, 168 engine tests.
>
> **The debt owed before B6 is paid (2026-07-27): the `.stop` hit box is ported**, so the sim is now settled for the cut. The original's `.stop` class was never only a style — besides killing the CSS glide it shortened the hit area (`margin-top: 8px; height: 46px`), which `collision.js` had not carried. The class is now an entity field, `stopped`, set and cleared at exactly the eight sites pandas.js added and removed the class (a knock, a dive-roll, a tumbler skid, a zoomies dash, every grounded phase, the parading base, the riders a topple drops) rather than derived from `mode` — two of those cases are not modes of their own, and a test pins the six that are so the flag cannot quietly drift.
>
> **The behavioural content is an asymmetry, not a shrink.** The shorter box shares the standing box's bottom edge, but contacts compare corner *top-lefts*, so the two corner rows sit 8px and 4px lower: a stopped panda cedes reach upward and gains a little downward. Measured headless before vs after (24 episodes × 20 min at 1600×900; 40 × 20 min at 1200×520), **the watcher is floored 10–16% more often** — 1.53 → 1.77 knocks/min and 13.2% → 15.0% of his life grounded on the larger stage, 2.63 → 2.89 and 22.9% → 24.9% on the smaller — because the box a *fallen* panda wears now reaches further down, and standing next to fallen pandas is his entire job. That moves the port toward the original's hotter economy, which is what fidelity predicted; **the motion itself is Ameya's gate, not the number's.** Digests moved to `d4a2d47b` @ 32×10k / `bfbc8a5c` @ 60k (parity re-confirmed in Chrome), the obs fixture was regenerated, and `format-demo` was re-cut — its stale-corpus guard caught itself first, which is the guard working. 170 engine tests, 51 trainer tests.

> **Progress — B6 done (2026-07-27): the corpora are cut and the roster is frozen, so Phase B is closed.** `train-wild` (840 × 12000 = 10.08M ticks, 5.04M rows, 15.20 GB, 205 s at 49k ticks/s) and `eval-natural` (120 × 12000, 720k rows, 1.72 GB, 11 s), both with ground truth — kept because a corpus missing labels it could have had is the expensive mistake, and because Phase C's oracle and Phase G's probes read exactly those columns. The training corpus spans **6 to 28 pandas per episode — 23 distinct row widths**, which is the clearest possible vindication of B4's "the unit of a file is one episode": there is no single rectangle this corpus could have been. All 17 actions appear; every cell is finite; loading from the manifest alone in NumPy was checked end to end, entity sub-row *k* ↔ panda *k* included.
>
> **The freeze is a test, not a paragraph.** A change to the roster, the mode vocabulary, the observation layout, the action space or a truth column does not corrupt a cut shard — it silently *re-labels* one, and every number downstream stays plausible. So `cut.js` gained `checkContract(manifest)` and `trainer/test/freeze.test.js` runs it over every committed manifest, comparing shard version, engine digest, encoder digest, action names, observation layout, truth schemas and label vocabularies against the code as it stands. It reads no shards, so it lives in the ordinary unit suite while the bytes stay gitignored: edit the roster and the suite goes red naming the field that moved.
>
> **One hole the freeze had to close first.** The engine's golden digest cannot cover the sensor — `policy/obs.js` never touches the sim, so an encoder change moves no golden digest while changing what every `obs` column means. Manifests now carry an `encoder` digest (of `policy/obs-fixture.json`, which is the encoder's own contract and is itself enforced by an engine test), and the freeze test checks it is sensitive to a single flipped bit. `observation.params` is deliberately *not* frozen: the cone angle and peripheral radius are per-corpus config Phase C tunes, recorded per manifest.
>
> B5's worker pool never became necessary here — a 10M-tick corpus is 3.4 core-minutes — and stays open for Phase E, where the step count is billions and one shard per episode makes the fan-out embarrassing. 57 trainer tests, 170 engine tests.

### Phase C — the yardstick gate *(game design happens here, where iteration is cheap)*
**Build:** the privileged **oracle** policy (true state as input — or the rules expert, re-scored on the game); the **reactive twin** (2-frame window, same architecture); scripted exploit bots (spawn-centroid camper, `R_VIEW`-intersection parker, corner cowerer); the twin-episode battery, one set per knowability tier.
**Done when:**
- **Memory gap ≥ threshold** (D3; initial proposal: oracle − reactive ≥ ~30% of oracle score). — ✅ **PASS (C5)**: **87%** over `reactiveObs`, the memoryless twin this line was written about. **D3 resolved on the reading, not the number:** the verdict is on the full gap alone. C5 measured that the conservative reading (`reactiveTruth`) is not a bound — it is **−19% on `wild`**, and a planner knob moves it 17% → 6% while the full gap holds at 77–126% — so it is now printed as a diagnostic with no verdict, and what it prices is the duration tier alone (15%, against ~0% when C3 closed).
- Every scripted exploit bot scores much nearer the reactive ceiling than the oracle. — ✅ **PASS after C4**: all four reward exploits −32% to −1% on the ceiling→oracle line under the C5 economy, and both action exploits (`speeder` +5%, `roller` −59%) measured against the oracle they are unbraked copies of, which is the denominator that question actually has.
- The oracle passes the twin-episode battery; the reactive twin fails it. — ✅ **PASS (C3, 2026-07-27)**: oracle 1.00 on both knowable tiers, memoryless twin 0.00 on both, nothing distinguishes the unknowable one. Unmoved by C4's and C5's economies.
- **If any check fails: iterate the game knobs and re-run this phase. Do not proceed with a leaky game — Phase E cannot succeed against one.**

> **Progress — C1 done (2026-07-27): the game exists, in [`trainer/game.js`](../trainer/game.js).** Everything in Phase C is a comparison of scores, and until now there was no score: the plan's economy lived in prose. It is now `DEFAULT_RULES` plus a ledger — pay per tick per live incident inside `viewRadius`, minus a knockdown penalty, minus a small price on strides and a larger one on rolls; with the three hardening terms as first-class knobs (an arrival multiplier fixed at arrival, per-incident diminishing returns, a per-incident cap) because "game design happens here, where iteration is cheap" only means something if turning a knob is one flag. `evaluate.js` runs a policy across the episodes of the committed `eval-natural` corpus — same root, same spec, so a score and a shard are the same world — and reports the distribution, not just a mean. Scoring is free (320k ticks/s; the whole 24-episode set is 0.9 s), which matters because C2's yardsticks will be run against every knob setting the phase tries.
>
> **`runEpisode` gained the policy seam** it was always going to need: `policy` is `(state, tick) -> action | null`, or `{ init(ctx) }` for anything with per-episode scratch, consulted once per decision tick and fed straight into `step(state, action)`. Null hands the tick back to the expert, which is both how the `expert` policy is *defined* and the engine's own bad-logit fallback (a test drives NaN, 99 and −1 through it). Leaving `policy` unset is byte-identical to before it existed — `cut.js --verify` re-cuts a committed shard to the same digest, which is the check that matters now that the corpora are frozen. One thing is deliberately left open and written down rather than fixed: a policy sees the state it acts *from* (tick t−1) while the corpora pair the action at tick t with the observation encoded *after* that step, so a BC policy is one tick off its training pairing. It cancels out of every Phase-C comparison; it is Phase D's to settle.
>
> **Two rules carry more weight than they look.** *Ordinary knocks pay nothing* — only the three directors post incidents — which is what gives the flagship twin-episode certificate its teeth: a sleeper and a freshly knocked panda encode to identical bytes (already pinned by an engine test), one is worth points and the other nothing, and only event memory separates them. And *`inc.abandoned` is ignored by the referee*, though the watcher's own `isLive` honours it: it is the rules expert's bookkeeping and is never set when a policy drives, so scoring through it would have two policies playing two different games.
>
> **The calibration, and three findings that are not flattering.** On `natural` × 24 × 12000: expert **30.4 ± 8.1** score/min, `still` (always HOLD) −4.4, `random` −357.6. `knockPenalty` was set from that table at 40 — ~40% of the expert's gross income, on top of the 14% of the episode he spends grounded earning nothing — which leaves the incumbent clearly positive, because a game where the shipped watcher scores negative is a game where the do-nothing floor looks like a strategy. Then: **(1)** standing still is not safe — `still` is knocked 1.07/min against the expert's 1.27, so the penalty is close to a constant tax on *existing* rather than a price on recklessness; **(2)** under crowding, cowering wins — on `dense` the expert scores −47.4 against `still`'s −36.5, and the two tie exactly at `knockPenalty=29` on `dense` against `knockPenalty=215` on `natural`, so the game as specified prices activity out of the market at high density and the *curriculum* corpus currently teaches freezing; **(3)** camping earns a startling amount of *gross* income — `still` collects 382 points a run without deciding anything — because diminishing returns and the cap are per incident and a crowd supplies a stream of fresh ones, each paying full early-arrival rate. ⚠️ **(3) was overstated and C2 corrected it:** net of cost every camping bot loses, and loses badly; the finding that survives is the narrower one, that gross view income is a bad diagnostic and the ledger's components must be read together. (1) and (2) are reward-*shape* problems, which is what the appendix predicted about per-tick proximity rewards, and remain open. They are recorded rather than tuned away: a default chosen to hide a finding is a default nobody can reason about. Also measured, and smaller than expected: closing the `R_VIEW`-intersection exploit (`payAll: false`) moves the expert's income by 3–4%, so incidents rarely overlap at radius 180 — the parking exploit is small before anyone works it. 81 trainer tests, 170 engine tests.
>
> **Progress — C2 done (2026-07-27): the yardstick gate runs, and its verdict is split.** Built as **one planner over three information sets** rather than three bots (Ameya's call), because a gap between two separately written bots measures how hard I tried on each. `planner.js` is one score-maximising body — it prices every candidate using the *exact integral* of the game's own diminishing-returns curve, so the oracle is limited by information rather than by optimising something other than what it is paid — and `percept.js` hands it either the truth (**oracle**: true state, the incident feed, every FSM's countdown), the truth minus everything temporal (**reactiveTruth**, the conservative ceiling: it strictly dominates any memoryless policy reading observations, so the gap above it is a bound no 2-frame net can close), or one observation frame with visible slots only (**reactiveObs**, the honest analogue of the plan's memoryless twin). Plus the plan's exploit battery — spawn-centroid camper, `R_VIEW` parker, corner cowerer — and `node evaluate.js --gate`, which computes both exit checks instead of leaving them to be eyeballed.
>
> **`natural` × 24 × 12000: oracle 85.3 score/min, reactiveTruth 81.3, reactiveObs 10.2, expert 30.4; camper −53.4, parker −50.9, cowerer −13.5, still −4.4.** So **exit check 1 splits**: the full gap is 75.1 = **88% of the oracle** against a 30% threshold, and the conservative gap is 4.0 = **5%**. **Exit check 2 passes**: every camping bot sits *below* the reactive ceiling — which is what corrected C1's finding (3). `parker` is floored 2.71 times a minute, because the place where incidents overlap is the place you get run over.
>
> **The finding that matters: all of the value is in knowing *which* panda is worth watching, and none of it is in knowing *how long*.** Stripping every temporal quantity from a policy that keeps the feed costs 5%, and it stays at ~5% under every knob turned at it (`stepCost` ×6, `anticipationTau` ×5 harsher, both at once). The diagnosis is structural rather than a magnitude: **the feed re-tells you what is live every tick, so nothing has to be predicted** — and commitment is reversible, since retargeting costs a few strides. Optimism therefore beats prediction, which is also why the reactive arm scores a hair *above* the oracle: the oracle correctly skips trips its estimate says will not pay and is occasionally wrong, while the optimist just goes. The anticipation economy — the arrival multiplier and the duration posterior, "the money" in the section above — is currently worth nothing, and the *statistically inferable* tier of the knowability spine has no teeth. Making it bite means making commitment **irreversible** rather than expensive (pay only after a minimum dwell; pay on arrival rather than per tick), and neither is a knob that exists yet. That is C4's work. The *fully inferable* tier, by contrast, is nearly the whole game: reactiveObs scores 10.2 looking at the same world, because it cannot tell a sleeper from a panda that was just run over and prices both at the measured base rate for "down", 0.21 — and its passivity is not timidity, since forcing it to chase harder (zero switch margin, quadrupled dwell, no risk aversion) moves it to 5–7 and no further. It is not that it will not go; it is that it does not know where.
>
> **One real exploit, and it is in the action space rather than the reward.** `applyHatAction` executes a STEP as one immediate 50 px stride with no cadence check — pacing lives in the policy by design — so a policy that strides every decision tick travels **25 px/tick, 5.5× the expert and 2.5× a zoomies**, and the movement cost cannot restrain it, being charged per stride and therefore per *pixel* rather than per second. `speeder` (the oracle with that brake off) scores **107.2, +26% over the oracle**, failing check 2 at 129% of the ceiling→oracle line. It is in the battery so that the check can say so out loud. It will fail the character gate long before it is a scoring problem, and every yardstick holds itself to the expert's cadence so that it measures the game a deployed policy will actually play.
>
> **The oracle's time estimator is a second implementation, and is tested like one.** `remainingOf` mirrors arithmetic that lives in `anomalies.js` — which `truth.js` warns against — because truth's exact `ttl` comes from a second pass over the same episode and an oracle acting on it would change the episode the label came from. The first attempt used `aTimer` alone, which is the *sub-phase* countdown rather than the anomaly's life (for a loop it reads 8 ticks when 200 remain); the oracle skipped every loop, moonwalk and spinner and **scored below the strictly-less-informed reactive arm**, which is how the bug surfaced. `test/percept.test.js` now holds the estimator to truth's exact `ttl` per kind, with per-kind error bounds that say what each can honestly know — exact for a sleeper, starer and loop; loose for a spinner (stagger count drawn only at the end) and a zoomies (crashes into a wall the estimator projects, and a hero card it does not). The one engine change: `anomalies.js` **exports** its sub-phase constants as `PHASE`, so the estimator reads them instead of copying the numbers. Behaviour-free; the golden digest is unmoved at `d4a2d47b`. 95 trainer tests, 170 engine tests.
> **Progress — C3 done (2026-07-27): the twin-episode battery runs, and exit check 3 passes.** In [`trainer/twins.js`](../trainer/twins.js), on the constructed-episode harness in [`trainer/scenario.js`](../trainer/scenario.js). Checks 1 and 2 are differences of *scores* over 24 draws of the eval corpus; this one is a difference of *behaviour* on a matched pair, and it answers what no average can — **can this policy tell these two situations apart at all?** Three batteries, one per tier of the knowability spine, 12 pairs each (bearing over the 8 sprite axes × 300/340/380 px), the whole thing in **0.2 s**, so it lives *inside* `evaluate.js --gate` rather than beside it: the gate is now the whole gate.
>
> **A scenario builds a stage rather than sampling one** — a bare 1240×900 room, no hero card, a hand-placed roster, and all three directors' clocks pushed past the end of time (silenced rather than bypassed, so a scenario is a state `step` treats like any other), with a script hook injecting the one event at an exact tick through the engine's own `startAnomaly`/`beginKnock`. Three details are load-bearing. **(1) The hat is frozen until his cue**: the policy is consulted on every decision tick from the first — so a policy with memory has seen the whole lead-in, which is where the answer lives — but its action is discarded and replaced with HOLD until the decision tick. Without that the two arms diverge during the lead-in (in one of them there is already something worth walking to) and the frames stop matching. Frozen is the hand held, not the eyes shut. **(2) The identity is asserted, not assumed**: before any verdict both arms are re-run with the hat still and their frames diffed, and a mismatch throws naming the token and field — the battery's whole claim is "these two are the same picture", and a claim nobody checks is one refactor away from false. **(3) `impact` is 85 on this stage rather than the shipped 80**, because a knock carries its slide as `slide / fallTicks` per tick and 85/17 = 5 exactly, so the knocked twin lands on the pixel the sleeping twin was placed on with no float residue. Stated rather than hidden: **the knock arm has no collider on stage** — a real contact knocks both bodies, and matching two arms through a mutual knock means matching two subjects. What is kept is the tell that carries the information: an ordinary knock *slides* its victim 85 px away from the struck side across the fall cels and a sleeper lies down where it stood, so the 17 ticks of the fall are the whole difference and they are on screen in both arms.
>
> **The statistic is `discrimination = approach(hot) − approach(cold)`**, where approach is the fraction of the *closable* distance (the trip minus `viewRadius`) he closed in a 90-tick window: 1 = arrived, 0 = never set off. A policy that walks to both, or to neither, scores 0, which is the honest verdict on one that cannot tell them apart. **`natural`-independent, 12 pairs: on `identity` (sleeper vs freshly knocked) oracle 1.00, reactiveTruth 1.00, reactiveObs 0.00 — and reactiveObs's zero is the interesting kind, because it walks the whole way to *both* (hot 1.00, cold 1.00) rather than being too timid to walk anywhere. On `duration` (a fresh nap vs one with 30 ticks left) oracle 1.00, reactiveTruth 0.00, reactiveObs 0.00. On `unknowable` (a cascade armed vs not) every policy is 0.00 and not one action differed on any pair.** Stable at 24 pairs and under `viewRadius=130`.
>
> **Four findings.** **(1) The split between the two knowable tiers is exactly the split between the two reactive ceilings.** `reactiveTruth` — the feed kept, every temporal quantity stripped — passes `identity` at ceiling and fails `duration` at zero, which draws C2's headline boundary in behaviour rather than in a score difference. **(2) The duration tier is decidable, and C2 measured it at 5%; those are not in tension, and together they are the diagnosis.** The information is there — the oracle separates the two naps on every single pair — but the live distribution almost never *forces* the choice, because the feed re-announces what is live every tick and retargeting costs a few strides. The tier does not need to be made knowable. It needs to be made **consequential**, which is C4: pay only after a minimum dwell, or pay on arrival rather than per tick. **(3) The shipped watcher fails the flagship certificate, and not for lack of information.** `expert` reads the feed and still scores 0.16 on `identity`, walking 0.79 of the way to the worthless twin (and on one pair *further* toward it, −0.17), because with nothing flagged nearby its ambient behaviour is to study the nearest panda — and a body on the ground is the most interesting thing on a bare stage. That is a Phase-D result arriving early: BC from this expert teaches indiscriminate approach, the exact superstition the plan predicts, now measured rather than argued. **(4) The speed hole erodes the anticipation economy a second time.** `speeder` scores 0.67 on `duration` against the oracle's 1.00 — at 25 px/tick even a 30-tick nap is worth the trip — so the hole C2 priced at +26% of score also *removes* the pressure to predict, a stronger reason to close it than the score was.
>
> **The negative control has stakes and stays at zero.** The armed arm's cascade genuinely fires four ticks after the window closes, and the battery asserts that it did and that the unarmed arm's did not — a control whose two arms share a future is not a control. Because those two arms agree on every observable byte for the entire window, the check asked there is stricter than "blind": `identical`, meaning not one action differed on any pair. It didn't. No engine change was needed — the golden digest is unmoved at `d4a2d47b`. 115 trainer tests, 170 engine tests.
>

> **Progress — C4 done (2026-07-27): the action-space holes are closed in the body, commitment has a price, and three of the four things blocking the gate turned out to be in the instrument.** The two holes first. `applyHatAction` checked neither the stride cadence nor the roll cooldown, because both limits were enforced by the rules expert *on itself* and by nothing else — the seam ran on an honour system only the incumbent honoured. Priced before closing: `speeder` **107.2 (+26% over the oracle)**, and a new bot, `roller`, at **103.9 (+22%)** — the worse of the two, since `engine.js` skips ROLLING in the collision pass, so travelling by dive-roll is faster than walking *and* buys immunity to `knockPenalty` for 2 points a roll. Neither is a reward bug and no knob in `game.js` could have priced them: the ledger pays for where he is, and these buy position with motion the character does not possess. `limitAction` (engine `hat.js`) now applies both to any externally-supplied action, and a blocked action becomes HOLD *and is logged as HOLD*, because the BC contract is that `hat.action` is what actually moved him. The ceiling is `hatAlert`, the expert's own full-alert cadence, deliberately not the calm one — the corpora are recordings of expert actions and a clone has to be able to reproduce the alert strides in its own training data. **Behaviour-free for the expert, asserted rather than assumed:** a test replays 4000 expert decisions through the limiter and none is refused, both frozen corpora re-cut to their committed digests, and the golden digest is unmoved at `d4a2d47b`.
>
> **Exit check 2 was asking the wrong question of half its battery.** "Much nearer the reactive ceiling than the oracle" assumes a bot that wins *without knowing anything* — `camper` and `cowerer` are that. An action exploit is the opposite animal: it is the privileged oracle with one brake off, so its information *is* the oracle's, and a climb near 1 is what it scores when the exploit is **closed**. Measured against the reactive ceiling a working limiter reads as a 100% failure. Each family is now scored against what it would have to beat to be a problem, and **check 2 passes outright**.
>
> **Then the instrument.** Three bugs, one of which invalidates a published C2 finding. **(1) The yardsticks priced the wrong game.** The planners are constructed once at module load against `DEFAULT_RULES`; `--rules` changed only the referee, so every knob sweep ever run measured policies that had never seen the knob. C4's first reading of `dwellMin` showed *identical stride and knock costs at every setting*, which is what exposed it — the trajectories were byte-identical. ⚠️ **C2's claim that the conservative gap "stays at ~5% under every knob turned at it" is therefore void**; those sweeps moved the referee and left the policies alone. `runEpisode` now hands the episode's rules to the policy through `ctx`. **(2) The planner kept its book in decision ticks while the referee counts engine ticks**, so it believed every visit was half as long as it was — harmless to the gap, since all three arms shared the error, but it made the dwell arithmetic meaningless; correcting it alone moved the conservative gap from −5% to 18%. **(3) A guess was being treated as a short countdown.** An arm with no clock substitutes `priorRemaining`, and treating that point estimate as *known* turns the dwell check into a hard refusal of every trip costing more than ~30 ticks of travel. The clockless arm went catatonic, stopped walking to the flagship battery's sleeper at all, and **failed exit check 3**; the huge conservative gap it produced was mostly paralysis. An uncertain arm now bets the survival curve and finds out by winning or losing, which is the direction `percept.js` says a gate must err in.
>
> **The commitment economy, and what it bought.** `dwellMin: 120` + `arrivalPay: 120` (with `incidentCap` 280, game v2) make commitment irreversible rather than merely expensive: a trip pays nothing unless the incident is still running 120 ticks after he arrives, and walking away forfeits the escrow. 120 is a coin flip against the measured duration distribution (p10 20 / median 115 / p90 334 over every live incident-tick), so a clockless arm's flat prior is wrong about half the time it matters. The bounty is not optional — a dwell only ever *removes* income, and without it the shipped expert falls to −26.7/min and below `still`, which is C1's own disqualifying condition. **On the corrected instrument the C1–C3 default game has a conservative gap of −5%: the clockless arm beats the oracle outright, "optimism beats prediction" in its purest form.** The shipped C4 economy takes it to **17%** with the expert at 32.3 (against 30.4 under C1) and `still` at 2.6. Every setting that clears 30% — 240/240 gives 23%, 300/300 gives 63% — does it by putting the incumbent below the do-nothing floor.
>
> **What C4 concludes.** The conservative threshold is being asked of the wrong quantity, and that is now measurable rather than arguable: `reactiveTruth` is handed *identity* for free by the incident feed, so the gap above it prices the **duration tier alone** — while C3 established that both knowable tiers require memory, since a 2-frame reader cannot tell a sleeper from a panda that was run over once the fall is finished. The memory gap decomposes into identity (~78% of the oracle) and duration (17%), and demanding 30% from duration by itself is a stronger ask than the plan's own wording — "oracle − the memoryless twin", which is `reactiveObs` at **95%** — ever made. The gate still prints FAIL rather than being relaxed to fit; the number to carry into Phase E is that the duration tier is now worth 17% of a much larger pie against ~0% when C3 closed. **Still open and explicitly not fixed here:** cowering still wins under crowding (on `dense` the expert scores −55.0 against `still`'s −36.2, slightly *worse* than C1's −47.4 vs −36.5), which matters because Phase E trains on the dense-ish `wild`. It is a cost-side problem, not an information one — a probe at `knockPenalty` 20 puts the expert ahead on `dense` for the first time — and that recalibration is its own piece of work. 122 trainer tests, 174 engine tests.

> **Progress — C5 done (2026-07-27): the phase closes. All three exit checks PASS.** One rule moved and one verdict moved, and both changes are measurements rather than settlements.
>
> **The knock penalty was 85% a tax on existing, so it fell 40 → 20.** C1 suspected this from one number (`still` is floored 1.07/min against the expert's 1.27) and left it as finding (1); C5 priced the bill. On `natural` × 96 at the old penalty, the do-nothing floor pays **425 points** of knockdowns and the oracle pays 501 — so 85% of the oracle's knock bill is what it would have paid for standing still, and on `wild` and `dense` it is 80%. `cowerer` pays 277, which names the shape exactly: **the cheapest way to cut the bill is to leave the crowd, and the incidents are on the crowd.** The term was not pricing recklessness, it was pricing proximity to pandas — a density-scaled constant subtracted from the one behaviour the pay exists to buy, and (worse for Phase E than for the gate) 85% variance in a per-tick PPO reward. 20 halves the ambient constant with the ordering intact: below it the exploit bots start climbing (`camper` −25.6 → +2.3 as the penalty vanishes) and the memory gap sags with them; above it the incumbent drowns.
>
> **C1's finding (2) — "cowering wins under crowding" — was a claim about the incumbent, not the game.** The evidence had always been the expert scoring below `still` on `dense`. Ask the same question of a player that knows what it is doing and it is not close: on `dense` × 96 the oracle scores **153.0 against `still`'s −4.6**. The game pays an informed player 157 points a minute for not freezing. The expert's collapse is a triage failure and the ledger says which one — it fails **81.8%** of its commitments against the oracle's 49.0%, arrives 87 ticks late against 68, walks twice as far and collects a third of the view income. It is a bot hand-tuned for the live density, wandering. **C5 deliberately does not chase this the rest of the way:** 20 cuts the deficit on `dense` by 80% (−63.1 vs −39.3 becomes −9.2 vs −4.6) but leaves the expert a little behind, and the arithmetic says it stays behind until the penalty drops under ~15. Tuning to *that* would be tuning the game to flatter a badly-triaging incumbent. What is adopted is what the ambient measurement justifies on its own. On the two specs that carry weight the margin closes anyway, and `wild` is the one Phase E trains on: at 40 the BC anchor scored **−22.2** on its own training distribution, below every floor in the table; at 20 it scores **21.8** and is first. That is the change's real justification.
>
> **The conservative ceiling lost its threshold, because it is not a bound.** Through C4 the gate printed two verdicts on check 1, the second resting on an argument about information: `reactiveTruth` strictly dominates any memoryless observer, so the gap above it is a lower bound on the memory gap. Sound about *information sets*; silent about achieved **scores** unless the planner underneath is optimal — and it is not. Two measurements retire it. **(1) On `wild` the conservative gap is −19%**: the clockless arm out-scores the oracle 127.8 to 107.8, and a lower bound on a non-negative quantity cannot be negative. **(2) One unprincipled planner knob walks it from 17% to 6%** — the planner declines any trip pricing out negative and stands still instead; sweeping the floor it compares against moves the conservative reading between −19% and +58% across specs **while the full gap never leaves 77–126%.** That is the difference between a measurement and an artefact. So the verdict now sits on `reactiveObs` alone, which is also word for word what this plan asked for ("train a deliberately memoryless twin… the gap between them is the exact quantity of score only a world model can claim"). The conservative number is still printed every run, because C4's separate point about it stands and is worth seeing: handed identity free by the incident feed, what it prices is the **duration tier alone** — 15% under the C5 economy against ~0% when C3 closed. It carries no `ok` field and a test pins that, since an `ok` is how it would quietly become a threshold again. **The floor stays at 0 in the shipped planner**: declining a negative-EV trip is the principled rule, and the sweep is evidence about the instrument, not a proposal.
>
> **Three things C5 leaves open, none blocking.** `speeder` beats the oracle by 15% on `wild` while losing by 4% on `natural`, and it is *not* the limiter leaking — over 24 episodes of `wild` it strides **fewer** times than the oracle (6311 against 6731) and still scores higher, so what the number measures is chaotic sensitivity: an episode set of this sim is worth about ±15% to a policy that changed nothing about how it decides. Read it as the noise floor for any near-identical-policy comparison, and as a second reason not to have gated on a 15% conservative gap. The **planner's myopia** is real and shared by every arm: it prices no option value for being where the next incident will open, and the oracle is inside the pay radius for only 26–27% of ticks while something is live somewhere for 74–79% — so no oracle number here is a ceiling on *play*, only on information. And `still` now scores **23.9/min** on the eval set, 41% of the incumbent where it was 8%: freezing loses everywhere but is no longer worthless, so Phase E should expect a shallower gradient out of it. No engine change — C5 is entirely trainer-side, and the golden digest is unmoved at `d4a2d47b` (re-run). 124 trainer tests, 174 engine tests.

### Phase D — tracer bullet: full-observation BC policy, shipped *(parallel with C once B is done)*
**Build:** BC clone on *full* observation (the bar is only "does he still dodge"); binary weight export (little-endian blob + JSON manifest, lazy fetch after first paint); hand-rolled JS inference (preallocated `Float32Array`s, zero per-tick allocation, KV cache + recompute-from-ring-buffer debug path); `?policy=rules|nn` kill switch + auto-fallback on NaN/out-of-range logits.
**Done when:**
- ✅ Action agreement JS↔PyTorch > 99.9% — **24,000/24,000, largest logit difference 2.2e-5.**
- ✅ Weights ≤ ~400KB on the wire — **237.5 KB** (122k parameters, float16).
- ❌ Forward pass < 1ms measured in-page — **p99 is 2 ms in the browser** (Ameya, 2026-07-28) against 567 µs mean in Node. **This exit FAILS as measured.** Node flatters it by ~3.5× at the tail; the gate in `tools/policy-bench.html` is p99, which is the strict reading of "< 1ms measured in-page". Two fixes exist and are Phase-E work either way: shrink (d48 → d32 is 2.4× fewer flops) or move the forward pass to a **Web Worker**, which makes the budget moot — decisions are at 10 Hz and the renderer interpolates, so an action arriving a tick late is invisible, while a 2 ms tail on the main thread is not. *Update, same day: the worker landed as pre-E groundwork (status note at top). The exit's number stands as recorded; `?policy=nn` now ships the worker path, and the bench measures both arms.*
- ❌ Character pass on live preview — **run by Ameya 2026-07-28: FAILED.** "It looks about the same, except the hat panda is terrible at dodging." That matches the ledger (2.04 knocks/min against the expert's 1.23) and localises the same way the measurements do: not a broken reflex, aimless walking. The clone stays opt-in behind `?policy=nn`; the page keeps the rules watcher. Per D5 of the plan this is a recorded outcome, not a blocker — Phase D's purpose was the pipeline.

> **Progress — D0 done (2026-07-28): the corpora were pairing each action with the frame it produced, and every corpus was re-cut.** C1 recorded this as "a policy sees the state it acts *from* (tick t−1) while the corpora pair the action at tick t with the observation encoded *after* that step… it is Phase D's to settle". Settling it turned out to matter far more than "one tick off" suggests. `hat.action` is applied *during* the step, so the state it produces already shows the outcome on the hat panda's own token — he faces the way he stepped, his cel has flipped to WALK, his roll cooldown is freshly stamped. Measured over 36,000 decisions: **P(drawn facing == the step's direction | stepped) is 93.3% on `natural` and 85.5% on `wild` when the frame is taken after the step, against 48.9% / 39.0% before it.** The second number is an honest prior (he often keeps going the way he was pointed); the first is a readout. Direction is 8 of the 17 classes and the entire content of a locomotion policy, so the old pairing hands a clone most of its label on the self token and teaches it to consult its own body instead of the world — training to a flattering number and then failing in the browser, where the only frame a decider can ever have is the one before its action. `rollout.js` gained a `decide` hook that fires before the step; a row is now a **decision** (`obs` and `truth` at t−1, `action` at t). `SHARD_VERSION` → 2, manifests carry `rollout.alignment`, the freeze test compares it, and the Python loader refuses v1 outright. All four corpora re-cut, 3.5 minutes of machine time; **no Phase-C number moves**, because `evaluate.js` scores episodes from seeds and specs rather than from shard bytes.
>
> **Progress — D1 done: the full-observation corpora, and a slot count set by the browser.** `train-bc` (`wild`, 840 × 12000, 6.73 GB) and `eval-bc` (`natural`, 120 × 12000, 962 MB), both `coneDeg 360`, `occludeFence false`, no ground truth. `observation.params` was deliberately left out of the roster freeze for exactly this; `cut.js --obs` is now wired to reach it. Opening the mask argues for more than 8 slots (on `wild` the p95 of bodies in range is 9, and 5.7% of frames have more bodies than slots), and 8 is kept anyway — **because the token count is what the deployed forward pass is quadratic in.** That is the phase's most transferable finding: the sensor's width is a browser budget, not a perception decision.
>
> **Progress — D2/D3 done: the clone, and the shape the budget forced.** A hand-rolled `Float32Array` matmul does **4.43 GFLOP/s** in this engine (dot-product form with four accumulators and weights in PyTorch's `[out, in]` layout — 40% faster than scatter-accumulate). Against a 1 ms budget that priced every architecture up front: 4 layers × 9 tokens × d64 is 0.82 ms, but 122k → 208k parameters puts the wire over 400 KB; 4 × 18 tokens × d64 (two frames along the sequence) is 1.67 ms; 4 × 36 is 3.50 ms. **So tokens are pandas and time is stacked into them:** slot identity is already sticky (B2), so token *j* is the same panda at t, t−1, t−2, t−3, and velocity becomes a subtraction the first layer can do while attention stays 9×9. 4 layers × d48 × 4 stacked frames = **0.49 ms predicted, 567 µs measured, 122k parameters, 237.5 KB as float16.** ⚠️ Stacked frames are a fixed-length memory *handed* to the network, not a belief it carries — fine for cloning a reactive expert and precisely what Phase E may not do. The table says real temporal attention over a 100–200 token window does not fit on the main thread at this width; that is a known bill, recorded now rather than discovered later. **`mathx.exp` was added and pinned** (two-part argument reduction, Taylor core, an exactly-built table of powers of two, 1 ULP against `Math.exp`) because the attention softmax needs it and a policy is not a bystander to determinism — its action goes into `step`, so an engine computing `exp` differently would diverge the episode exactly as `Math.sin` did. ReLU rather than GELU for the same reason. **The exported float16 file *is* the model**: `export.py` rounds before anything else and the parity reference re-loads that file, so quantisation can never surface as a JS-vs-PyTorch bug.
>
> **Progress — D4 done: parity, and the one bug it caught.** 24,000/24,000 actions agree, largest single logit difference 2.2e-5. The gate earned its keep immediately: the first run was off by 5.33 in logit space, because the fixture wrote the k-th most recent frame to ring slot *k* while `forward` reads a ring where it lives at `(−k) mod F` — the model's history, backwards. That is exactly the class of error a >99.9% agreement number exists to catch and no offline metric would have.
>
> **Progress — D5 built: shipped opt-in, and what the clone actually learned.** `?policy=nn` fetches the weights after first paint and hands the panda over mid-scene; the rules watcher drives until then and drives again on any failure (NaN logits, unusable distribution, a throw, a 404), with the driver retiring after 8 consecutive bad passes rather than asking 10 times a second forever. Sampling is two-stage — family (hold/step/roll) at the trained temperature, direction re-tempered — because **the clone is good at one of those questions and bad at the other, and one temperature cannot serve both.** Asked which of 8 directions the expert stepped it is right **57.8%** against 12.5% chance; asked *whether* to step it is near-blind, because the expert's stride clock `moveTimer` is internal state no observation exposes. A single temperature couples them the wrong way: sharpening to clean up the wandering collapses the family onto HOLD and the dive-roll — 3.5% of actions and the whole of "does he still dodge" — disappears first (at T=1 the clone rolls on 1.0% of ticks; at T=0.6, 0.0%).
>
> **What it learned, measured:** `dir_given` **0.578** (chance 0.125), `roll_recall` **0.620**, `move_f1` 0.301, macro-balanced 0.269 — against an always-HOLD baseline that scores 0.883 raw accuracy, which is why raw agreement is not reported as a result anywhere in this phase.
>
> **And what it costs online, which is the honest part.** On the C5 ledger the clone scores **−60.9/min against the expert's 70.1 and `still`'s 13.5**, and is floored **2.04 times a minute against 1.23 and 1.16**. The mechanism is not what it looks like. It is *not* failing to dodge: conditioned on something being inside `hatDangerR`, it rolls at **0.098 rolls per danger-frame against the expert's 0.103** — the reflex is intact. It is *not* crowding: its median distance to the nearest panda is **231 px against the expert's 170**, and it is in danger **19.6% of the time against 28.6%**. It is knocked more because it **walks 45% more** (13.9% of ticks against 9.6%), in a direction that is right 58% of the time rather than always. And it walks more for a stateable reason: `hat.js` paces *any* externally-supplied action at `cfg.hatAlert` — the expert's full-alert cadence, chosen deliberately in C4 as the ceiling — while the expert itself falls back to its own slower `moveSpeed` when calm (`watcher.js:423`). A policy that cannot infer calm-versus-alert therefore walks at the fastest pace the character has, all the time. **That is a Phase-E problem with a Phase-E answer** — movement costs money in the ledger, so RL has direct pressure on exactly this — and it is the imitation gap doing what the appendix said it would, now localised to a specific half of the behaviour: the clone learned the reflex, which is driven by visible geometry, and not the navigation, which is driven by the invisible incident feed.

> **Two preview reports from Ameya (2026-07-28), one confirmed and one refuted by the artefacts.** *"It never pauses — it always stays moving"* is the measured deficit wearing its live costume: `move_f1` 0.301 because the expert's stride clock (`moveTimer`) is internal state no observation exposes, every sampled step executing at `hatAlert` — the walks-45%-more mechanism above, observed rather than plotted. *"The cone was added in — it doesn't see anomalies behind"* is **not what is happening**: the shipped manifest records `coneDeg: 360` (checked 2026-07-28), and the loader builds the observer from the policy's own recorded params, so this clone sees all directions. What reads as a blind spot is `sightRange` 520 px (distant anomalies are invisible in *every* direction), the 8-slot k-nearest truncation in a crowd, and the absence of the incident feed — it does not preferentially walk to anomalies anywhere, and behind is just where it gets noticed. The 120° cone genuinely arrives in Phase E, so the report is also a preview of how cone-blindness will read when it is real.

### Phase E — the emergence run
**Build:** partial observation per D1; BC warm-start → the locked RL recipe; **setting-2 main arm + setting-1 aspiration arm, matched seeds**; dense checkpoints (every ~1M steps early); instrumentation probes at every checkpoint (kind/phase/time-remaining decodability vs an untrained-twin baseline, macro-F1 — *instrumentation, not results*); and the **shortcut hunt** loop: score climbing while probes stay flat ⇒ RL found an exploit we didn't ⇒ watch rollouts, find it, patch the game, re-run. That loop, repeated until score and decodability rise together, is what "engineering emergence" means in practice.

**Workstreams and estimates (planned 2026-07-28 — Phase E is not "just training"; five build pieces precede the first real run):**

- **E0 — the on-policy rollout bridge** *(the real engineering, ~2–4 days).* BC trained from a static corpus; RL needs the live sim in the loop, and the sim is JS while the gradients are Python. The answer is vectorized environments: N Node workers (B5's pool, finally needed) each hosting a batch of episodes, exchanging observation/action batches with the Python trainer at the decision cadence over a binary pipe. With batched GPU inference on the Python side, throughput stays engine-bound — roughly 25–50k decisions/s aggregate on the dev machine — rather than bottlenecked on per-decision JS forward passes (~1.8k/s/core).
- **E1 — partial-obs BC warm-start** *(hours; do this FIRST, before E0 exists).* Retrain the clone on `train-wild` (already cone-masked) with `--delay 1`. It is the KL anchor E3 needs anyway, it is a drop-in replacement for the current `?policy=nn` weights, and it tests the whole worker/delay/cone deployment pipeline end to end on real corpora — hours of work that retires risk from everything downstream.
- **E2 — the memory architecture, priced before training** *(a day).* Stacked frames are a fixed window handed to the net; the memory-gap claim needs history the net *carries*. Candidates: KV-cached temporal attention, or the GRU fallback. Per the budget exit below, the pick is benched in `tools/policy-bench.html` under CPU throttle before a single training step.
- **E3 — the PPO trainer** *(~3–5 days).* Critic-only warmup, unfreeze at ~10× lower LR, KL-to-frozen-BC in place of the entropy bonus, the leash anneal, the setting-2 auxiliary loss. All specified above; all standard pieces.
- **E4 — checkpoint probes** *(a day).* Linear probes for kind/phase/ttl per checkpoint. Under the site-first reframe they earn their keep twice: instrumentation for the shortcut hunt, and the overlay's future data feed.
- **E5 — the runs, and the loop** *(the real calendar time).* A single run is overnight-on-the-Mac territory: small-scale POMDP work typically lands at 100–500M environment steps, which E0's throughput covers in hours. The unschedulable part is the loop — Phase C closed every exploit a *scripted* bot could find, and RL is a better exploit-finder than its authors. Expect **5–15 runs** before score and decodability rise together. Honest total: one to two weeks of focused build, then a few weeks of part-time iteration.

> **Progress — E2 done (2026-07-28): the memory architecture is priced, and the pick is slot-state.** Three carried-memory candidates implemented as costed kernels (`tools/bench-kernels.js` — random weights, real shapes, deliberately naive matmuls so every number over-estimates) on the same single-frame spatial transformer (4 × 9 × d48, frame-stacking dropped since the carried state replaces it): **`gru-global`** (one GRU d96 on the readout, 160k params), **`gru-slot`** (a d48 GRU *per token*, shared weights, fed back into that token's input next decision — memory with a per-panda address, 133k params / 260 KB), and **`kv-temporal`** (2 KV-cached attention layers over a 128-decision ring, GTrXL-lite, 173k params). `tools/policy-bench.mjs` now automates the page in headless Chrome with CDP CPU throttling; measured at 4×, **all three pass the 100 ms worker deadline with 45–50× headroom** (worker rtt p99 2.0–2.2 ms), and the honest caveat is recorded in the runner itself: CDP throttling largely misses worker threads, so the conservative old-laptop bound is the throttled *main-thread* compute — 5.3–5.6 ms p99, still 15–18× headroom and ~5% duty at 10 Hz, comfortably inside the battery clause. **The budget therefore does not decide; the plan's other criteria do, and they point one way: `gru-slot`.** It is the only candidate whose memory has an *address* — the lit review's own emergence criterion (Sokoban's cell↔square bijection, our per-panda tokens), the overlay's natural feed (belief chip for slot j reads h_j, joined to pandas by `truth.slots`), and the flagship certificate's shape (the sleeper-vs-knocked event lands in the subject's own slot state). It trains like a GRU (POPGym-legitimate; no GTrXL gating risk), and it is the smallest wire. `gru-global` is the fallback arm; `kv-temporal` is parked unless slot memory proves horizon-limited. **Known risk, recorded — and decided (Ameya, 2026-07-28): the slot-turnover field is postponed.** The observation has no explicit "new tenant" signal, so a reassigned slot's carried state goes stale until the net notices the discontinuity (`present` flips, relX/relY jumps). Adding the field would move the frozen encoder digest and re-cut every corpus, and there is a fair chance the update gate simply learns the discontinuity — so it waits for E5 evidence of stale-slot pathology before anyone pays the re-cut. E3 is next.*
>
> **Progress — E0 done (2026-07-28): the on-policy bridge, at 4–8× the throughput bar.** The episode loop inverted into `makeEpisodeStepper` (`runEpisode` reimplemented on top — one loop; `cut.js --verify` still re-cuts committed digests), `vec.js` batching N lockstep episodes with the gate's own ledger as per-decision reward, B5's worker pool landing at last as `vec-host.js` (any sharding is byte-identical to the single process — pinned by test), and a fixed-size binary stdio protocol to `py/vecenv.py` whose reader is `np.frombuffer`. Measured end to end through Python on `wild`: **47–51k decisions/s on one worker, 176–201k at 8×64** against the plan's 25–50k target — a 500M-step run is under an hour of environment time, so E5's calendar is GPU- and iteration-bound, not sim-bound. The delay contract stays policy-side (feed the forward pass the previous frame, as `data.py` pairs windows), so one bridge serves delay 0 and 1 alike; `-1` actions hand decisions to the rules expert, which is both the deploy fallback and the pipeline-fill behaviour. 140 trainer tests. E2 (the memory architecture, priced in the worker bench before training) is next.*
>
> **Progress — E1 done (2026-07-28): the deployed weights are now the partial-obs, delay-trained clone.** Re-trained on `train-wild` (120° cone, decision-aligned, the ground-truth-bearing corpus) with `--delay 1` and `eval-natural` as the scoring pair — 72k × 512 in 8.1 min, exported in place (digest `1f0d343e13a8aea9`), parity **24,000/24,000**, and the page's `?policy=nn` arm picks up the cone from the manifest with no code change. The cone and the delay cost the clone almost nothing offline (`dir_given` 0.578 → 0.555, `roll_recall` 0.620 → 0.629): what a reactive clone can learn was visible-reflex all along, which is the pipeline confirmed rather than a finding. Online under the deployed contract it scores **−61.7/min at 2.18 knocks/min** on `natural` against the old clone's −68.6 / 2.42 run delayed — training under the delay buys back the delay penalty, no more. The imitation gap to the expert (57.7/min, 1.27 knocks/min) is untouched, as predicted; it is E3's to close. One instrument fix rode along: `eval.py` now follows the manifest's `trainedOn` to the matching corpora instead of assuming the 360° pair. This checkpoint is E3's KL anchor. E0 is next.*

**Done when:**
- Score ≥ reactive ceiling + ~70% of the memory gap (it actually claimed the money only a world model can earn).
- Twin-episode battery passes *behaviorally* — flagship: approaches the sleeper, ignores the freshly-knocked.
- Instrumentation decodability clearly above the untrained-baseline gap and rising through training.
- Believability metrics in band; leash schedule executed as designed.
- **The deployment budget holds (site-first, added 2026-07-28).** The candidate architecture is priced against `tools/policy-bench.html` *before* training, and the ship candidate's in-worker round-trip p99 clears the 100 ms pipeline deadline with real margin under Chrome's 4× CPU throttle (the stand-in for a visitor's old laptop). If the memory design wants real temporal attention over 100–200 tokens, it must be KV-cached or recurrent — Phase D's table says full recompute at that context does not fit, and a model that busts the budget fails this exit *no matter what it scores*. Battery is part of the budget: sustained worker CPU at 10 Hz should stay a rounding error, not a fan-spinner.
- **Trained under the deployed timing (added 2026-07-28):** `--delay 1` through BC warm-start, rollouts and RL alike; the exported manifest records it, and any online score quoted for the candidate is measured under `--delay 1`.
- **The overlay has a tenant (added 2026-07-28):** the checkpoint instrumentation probes double as the overlay's data source — the ship candidate comes with a working belief provider on the `render/overlay.js` seam, even before Phase G's full control battery. If nothing decodable can feed a chip, that fact gates here, not at the writeup.
- **The recipe is an artifact (added 2026-07-28):** configs, seeds, and a one-command rerun that reproduces the ship candidate end to end (cut → train → export → gate). The roster is only escalatable — new anomalies later, retrain — if retraining is an evening, not archaeology.
- Seeds and the setting-1 arm are now *nice-to-have* rather than gating (the 2026-07-28 reframe): run ≥3 seeds and the purity arm if budget allows, record what happens either way; one policy that passes the gates above ships the site.
- **Fallback trigger:** if repeated shortcut-hunt iterations can't get decodability moving while score saturates, the supervised-belief fallback activates — same infra, one loss change — and the post becomes "what control pressure doesn't buy." Under the site-first framing this is a *legitimate ship path*, not a consolation: the overlay reads identically either way.

### Phase F — ship the learned line-walker
**Done when:** the Phase-E policy is live behind the seam (sampling deployment, fallback verified) and passes Ameya's character gate on the homepage preview. If the best-scoring policy fails the gate, the ship candidate is the best *character*, or the site stays on rules — the page wins over the number, always.

### Phase G — the probing study + the post
**Build (offline, Python, on saved activations + logged ground truth):** the full control battery — untrained-network gap (expect random weights to probe *well above chance*; report the gap), shuffled-label selectivity, magnitude-matched random-direction intervention controls, bag-of-heuristics slot/region transfer tests, value-entanglement control (decodability on reward-neutral configurations), and — under D1(b) — the memory control (state of currently-unseen pandas). Egocentric probe bases (labels relative to *his* evidence stream; if linear probes fail, suspect the basis first). Bayes-posterior calibration curves vs observation time — the POMDP novelty over Othello/chess. Read-layer and write-layer swept independently; interventions by simple vector arithmetic with a swept scale; scored by edited-belief-consistent action mass. The homepage flinch demo ("make him believe that panda is zooming"); the belief overlay beside ground truth — **no longer optional: the 2026-07-28 reframe promotes it to co-equal deliverable with the policy, and its rendering seam already ships (`render/overlay.js`, `?overlay=truth`)**.
**Done when:** every positive claim has its controls, every negative is written up honestly, and the flagship post ships.

## Open decisions

- **D1 — FOV: ~~open~~ RESOLVED (Ameya, 2026-07-26): heading-cone FOV (variant b).** Observation-encoder mask only — no engine change, no action-space change; gaze flourish stays cosmetic. Cone angle + peripheral radius are Phase C tunables (they directly move the memory gap). Controlled gaze (c) parked as an add-on-era idea.
- **D2 — purity dial:** defaulted above (setting 2 main + setting 1 alongside). Veto point.
- **D3 — memory-gap threshold + reward magnitudes + `R_VIEW`: RESOLVED (Ameya, 2026-07-27, on C5's measurements).** The threshold stays at 30% of the oracle and the **reading** is settled: the verdict is on the full gap, over `reactiveObs` — the memoryless twin this plan named — which measures 87%. The conservative reading over `reactiveTruth` is printed as a diagnostic with no verdict, because C5 measured that it is not a bound (−19% on `wild`; 17% → 6% under one planner knob, while the full gap holds at 77–126%); what it prices is the duration tier alone, 15%. On magnitudes: the structural knobs landed first as planned (caps and diminishing returns in C1, the commitment economy — `dwellMin` 120 / `arrivalPay` 120 — in C4), and the one magnitude that moved is the +view/−hit ratio, `knockPenalty` 40 → 20 in C5, on the finding that 85% of what it charged for was ambient. `R_VIEW` never needed to move from 180.
- **D4 — his own two-way collidability:** keep (on-theme), with an explicit exploit watch in Phase E (a policy weaponizing its own collisions is a hackable lever).
- **D5 — scope honesty:** unchanged — a season, not a sprint. Phases A and D are each independently worth having; the fallback means even a failed Phase E produces the site's brain and a post.
- **D6 — the slot-turnover field: RESOLVED (Ameya, 2026-07-28): postponed.** E2's pick (per-slot GRU) carries state on slots whose tenant can change, and the observation has no explicit "new tenant" bit. Postponed rather than added, because the encoder is frozen against every corpus (adding a field re-cuts them all and retrains E1) and the GRU's update gate has a fair chance of learning the discontinuity unaided. Revisit only on E5 evidence of stale-slot pathology.

## Appendix — what the prior art says (lit review 2026-07-26, compressed)

**The OthelloGPT lineage** ([Li et al.](https://arxiv.org/abs/2210.13382) → [Nanda's linear reframing](https://www.neelnanda.io/mechanistic-interpretability/othello) → [Chess-GPT](https://arxiv.org/abs/2403.15498) → [bag-of-heuristics](https://www.lesswrong.com/posts/gcpNuEZnxAPayaKBY/othellogpt-learned-a-bag-of-heuristics-1) → [Hidden Pieces](https://ial.eecs.ucf.edu/pdf/Sukthankar-Austin-ICMLA2024.pdf) → [depth study](https://arxiv.org/abs/2310.07582)): probe-only claims died twice (nonlinear→linear via basis choice; world-model→heuristic-pile via neuron analysis); only intervention-backed claims survived. Untrained nets probe far above chance (66% vs 89% trained — report the gap). Interventions: simple vector arithmetic beat fancier methods, scale is critical, read- and write-layers peak at different depths, steering works best off-distribution. 1-layer models hold decodable-but-causally-unused state; ~4 layers was the causal threshold. Chess-GPT also grew an *unsupervised* skill latent — probe for our analogs (global chaos level, time-into-episode).

**World models in RL policies** ([Sokoban planning, ICLR 2025 oral](https://arxiv.org/abs/2504.01871); [cheese-vector steering](https://www.lesswrong.com/posts/cAC4AXiNC5ig6jQnc/understanding-and-controlling-a-maze-solving-policy-network); [Leela look-ahead](https://arxiv.org/abs/2406.00877); [meta-RL belief probing](https://arxiv.org/pdf/2510.22039); [AlphaZero concepts](https://arxiv.org/abs/2111.09259)): control pressure alone *can* produce causally-used state — when the representation is instrumentally unavoidable, training is diverse along that axis, and the architecture gives it an address (Sokoban's cell↔square bijection; our per-panda tokens). In POMDP belief-tracking specifically, reward-only agents typically get behavior *without* a clean belief; predictive auxiliaries reliably produce one ([2310.06089](https://arxiv.org/abs/2310.06089): reward-only nets warp representation space around rewarding states; predictive nets capture global structure). Representations arrive redundant (cheese lived in ~11 channels — if a 1-D edit fails, try whole-layer difference vectors); mechanism-present ≠ mechanism-used (measure action *and* score change under belief patching). Emergent tracking of *other agents'* hidden FSMs by a reward-trained policy is nearly unstudied — real novelty, matching risk.

**BC→RL practice** ([VPT](https://arxiv.org/pdf/2206.11795); [AlphaStar](https://storage.googleapis.com/deepmind-media/research/alphastar/AlphaStar_unformatted.pdf); [PIRLNav](https://arxiv.org/abs/2301.07302); [ADVISOR](https://arxiv.org/abs/2007.12173); [GTrXL](https://proceedings.mlr.press/v119/parisotto20a/parisotto20a.pdf); [POPGym](https://arxiv.org/pdf/2303.01859); [specification gaming](https://deepmind.google/blog/specification-gaming-the-flip-side-of-ai-ingenuity/)): KL-to-frozen-BC replaces the entropy bonus and is the anti-forgetting/anti-hacking/believability mechanism in one; a fresh critic's advantages destroy a BC policy without critic warmup; the imitation gap means privileged experts teach superstitious beelining (DAgger doesn't fix it); per-tick proximity rewards always breed parking equilibria — change the reward's shape, don't penalize after the fact; pre-LN or transformers may not train under RL at all; GRU is a legitimate small-scale fallback; ABC-RL-style action-thrash penalties and stochastic deployment for character.

**Shipped in-browser agents** ([REINFORCEjs](https://github.com/karpathy/reinforcejs); [llama2.ts](https://github.com/wizzard0/llama2.ts) — binary-identical pure-TS inference; [WebGPT](https://github.com/0hq/WebGPT); [snake-dqn](https://github.com/tensorflow/tfjs-examples/tree/master/snake-dqn); [poke-env](https://github.com/hsahovic/poke-env); [float determinism](https://gafferongames.com/post/floating_point_determinism/)): hand-rolled typed-array JS is the *fast* option at sub-1M params (ORT-web ≈ 10MB wasm; TF.js = leak discipline for nothing); sub-ms forward passes well-evidenced; keep the sim in JS — nobody ports the sim (ML-Agents/poke-env); Node throughput only bottlenecks at billions of on-policy steps; JS↔JS bit-determinism is easy (IEEE-754 pinned) with seeded PRNG + integer ticks + golden traces; Python↔JS policy parity is statistical (>99.9% action agreement — near-tie argmax flips at 1e-6 are expected and harmless); binary weights + manifest, never JSON floats.
