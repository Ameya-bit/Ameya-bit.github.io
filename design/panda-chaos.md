# Panda hero chaos — the overwhelmed interpreter

> **⚠️ There are now TWO implementations of everything in this doc, and the one that
> ships flipped on 2026-07-27.** The deterministic fixed-tick **port** in
> `assets/pandas/engine/` — Phase A of [panda-policy-net.md](panda-policy-net.md),
> which is what Phase 6 below became — **is the homepage default**: tiers 1–3, the
> watcher, collision, the sprites, the entrance, all of it, holding byte-for-byte
> between Node and the browser. `assets/pandas/pandas.js` is kept unmodified behind
> `?engine=old` as the reference the port is judged against.
>
> So **a behavioural change specced here has two homes**, and as of the Phase-B corpus
> cut (2026-07-27) it also has a **price**: the roster is frozen against 17 GB of cut
> corpora, so changing sim behaviour means re-cutting them and retraining anything
> fitted to them. `trainer/test/freeze.test.js` goes red the moment the engine drifts
> from a committed manifest — that failure is the reminder, not a nuisance. Before
> tuning any constant or rate here, read
> [`assets/pandas/engine/README.md`](../assets/pandas/engine/README.md) and
> [`trainer/README.md`](../../trainer/README.md), and land the change in both engines —
> the port's constants are tick-denominated in `engine/config.js`, not ms. The
> behavioural spec the port was validated against is
> [panda-engine-map.md](panda-engine-map.md).

*Started 2026-07-23. This spec **supersedes [panda-choreography.md](panda-choreography.md)** (the activation-patching troupe). That system shipped all five phases and was then retired by design review: the conga lines turned characters into notation — coordination read as uniform and janky, line membership suppressed the collisions that are the scene's actual charm, and the patch fired too rarely/late for anyone to see. Base engine, palette, hat pipeline, and fencing rules still live in [whimsy.md](whimsy.md), [direction.md](direction.md), and [[panda-hat-pipeline]]; this doc specifies what the troupe does **now**. Status: **Phases 0–1 shipped 2026-07-23. Phase 2 shipped (2026-07-24): 2A–2D done incl. the sidestep tier, nearest-first ties, and the nav cleanup (ghost-through retired; boxed → hold/re-vantage/abandon). Phase 3 shipped (2026-07-24): loop + starer + zoomies — all six tier-1 kinds live. Phase 4 shipped (2026-07-24): the stack — mount, parade (unstoppable base), growing wobble, topple (incl. zoomies-into-tower early topple + collateral knocks); riders **standing** (option 1, zero art). Phase 5 shipped (2026-07-24): the cascade — arm-clock (2–5min) + ignition-by-next-collision (staged away from the watcher) + steered two-front nearest-neighbour BFS + 70–90% coverage cap + the topple-coupling rule + a tier-3 pull to the origin; recovery is plain ma5a get-up (open decision #2 resolved). Phase 6 (the learned line-walker) is **no longer parked — in progress since 2026-07-26**, as [panda-policy-net.md](panda-policy-net.md); its engine groundwork (Phase A) is **complete as of 2026-07-27** (the whole of this doc is ported, presentation included, it passes the Node-vs-browser parity gate, and it is now what the homepage runs), and its **corpus phase (B) is complete too** (2026-07-27: 10.08M ticks of training rollouts + an eval corpus cut and versioned, the roster frozen). Phases C–G — the yardstick gate, the BC tracer bullet, the emergence run, the ship, the probing study — not started.** Principle #4 was revised mid-phase — the hat panda is now a real collider that walks the line (see below).*

---

## The thesis (revised)

The hero is a **self-portrait, not a demonstration**. A field of small agents doing inscrutable things, and one figure — the hat panda, Ameya's self-insert — moving among them, trying to understand. That *is* mechanistic interpretability, rendered as disposition instead of diagram. The audience for the interp meaning is **Ameya alone** (decided 2026-07-23): visitors see charming panda slapstick; the private layer needs no caption and no legibility bar. The old acceptance test ("reads without a caption") is retired. The new one: **does the scene feel alive without feeling noisy — and does the watcher's scrambling make you smile?**

### Principles (locked)

1. **Characters, not notation.** Every chaos behaviour is an *individual* malfunctioning. No formations, no coordinated walks, no diagram vocabulary. (The lesson of the conga lines.)
2. **Variety over frequency — raise the ceiling, not the floor.** The baseline texture stays calm (at a glance: wandering pandas, maybe one odd thing). Chaos comes from many *kinds* of rare events, not a higher event rate. The union keeps the hat panda busy; the scene stays ambient.
3. **The hat panda misses more than it catches — emergently.** Never script lateness. Anomalies have different lifespans (a nap lasts 20s, a crash is over in 1s); with independent clocks and one attention slot, early/on-time/too-late arrivals fall out on their own, each a different little phenomenon.
4. **The hat panda walks the line.** *(revised 2026-07-24 — supersedes the original "always a collision ghost, never suffers any of it".)* To observe is to travel the fine edge between detached study and getting swept in — **that edge is the character.** He is fundamentally an outsider and an observer, but he is now a **real, two-way-collidable panda** (the ghost is retired): he studies the chaos from close range and actively, skilfully avoids being pulled into it. He mostly succeeds; a knockdown is the line *momentarily crossed*, not his home — he then scrambles back to his side. Crucially, **the rarity is emergent from his own maneuvering alone** — dodge quality, alertness, the dive-roll cooldown. We never bend, thin, or steer the chaos to spare him; if he slips, he slips honestly.
5. **No new baked art.** Everything below composes existing cels + the three shipped primitives (`throwArc` parabola, `spin3d` facing-cycle, asymmetric knock). The one candidate exception — a hand-drawn rider cel for the stack — goes through the body editor in the workshop ([[panda-hat-pipeline]]), never AI generation ([[no-ai-generated-art]]).

### The private key (for Ameya; never rendered)

| On screen | In the head |
|---|---|
| The wandering troupe | the model — small units doing inscrutable things |
| The hat panda scrambling incident to incident | me; post-hoc analysis of behaviours that already happened |
| Sleeper (flops down, naps) | a dead / ablated unit |
| Loop (stuck retracing a circle) | repetition — the canonical LLM failure |
| Zoomies (3× sprint into a wall/panda) | a runaway activation |
| Tumbler (trips on nothing) | noise, clumsiness of the substrate |
| The stack (tower parading as one) | stacked layers pretending to be one coherent entity — until it falls apart |
| The cascade (one bump fells the herd) | a perturbation propagating through the network (the old patch metaphor, reborn as slapstick) |
| The oblivious one (never participates) | the unit no probe ever explains |

---

## The chaos economy — three tiers

One **director** (see below) owns all rates. Indicative constants are starting points; every one is tuned on the live Quarto preview ([[verify-animations-static-only]]).

### Tier 1 — individual weirdness (frequent, small, cheap)

One roamer at a time goes strange, then recovers. Roster, each with duration class (long ones are "catchable" by the hat panda; instant ones are always post-hoc):

- **Sleeper** — stops mid-walk, plays fall→fallen deliberately (no slide, no impact), lies 8–20s, stands, resumes. *Long.*
- **Loop** — walks a small closed octagon (turnIndex +1 every stride) 2–4 times before snapping out. Organic-looking, mechanically trivial. *Medium.*
- **Starer** — stops, faces the nearest edge, holds the settled idle 6–12s, moves on. Nothing happens. *Medium.*
- **Spinner** — `spin3d` in place ~1.2s, then 2–3 quick stagger-steps in random directions, resumes. *Short.*
- **Zoomies** — locks a straight heading at ~3× speed until it hits a wall (tumbles) or a panda (ordinary knock — feeds the collision economy). *Short, kinetic.*
- **Tumbler** — trips on nothing: facing-cycle tumble over a short slide, ends splayed in the fall frames, gets up. *Instant.*

Cadence: one tier-1 anomaly begins every ~`ANOM_GAP` 7–14s (jittered), never two of the same kind back-to-back, never targeting the hat panda, the oblivious one, an entering panda, or a current stack member.

### Tier 2 — the stack (occasional, medium spectacle)

- **Mount:** a roamer walks up behind another, hops onto its head — a small `throwArc` (~400ms, ~55px rise). A third may join the same way. Cap **3 high** (the reference image; the hero band is short).
- **Parade:** the bottom panda is the only real actor — it walks (ordinary wander), collides as the **unstoppable force** (the conga asymmetry code, retargeted: knocks roamers aside, never knocked). Riders are ghosts pinned at vertical offsets, holding a still pose with a CSS sway, z-ordered above.
- **Wobble accumulator:** sway amplitude grows the longer it parades — collapse is visibly foreshadowed. A wobbling stack should be irresistible to the hat panda.
- **Topple** (wobble maxes out, or something kinetic hits the stack — a zoomies panda into the tower is a gift): *(revised 2026-07-24 — supersedes the fling-off below.)* the whole tower **collapses in place**: the riders drop straight down onto the base so all three hitboxes coincide, every stack flag is cleared, and the ordinary collision logic knocks the coincident pile apart within a frame — all three fall and scatter, staggered by collision order, each recovering ma5a-style. (The earlier version `throwArc`-flung only the top two off while the base walked away, which read as the riders jumping ship rather than the tower falling.) A rider landing on / knocked into a bystander is still a free collateral knock. A topple is a **self-contained payoff** — it is *not* a cascade (see the coupling rule below).
- Frequency: a stack forms every ~`STACK_GAP` 60–120s; at most one stack alive; no stack during a cascade.

**⚠ Rider pose — workshop before building (the one open art question).** These pandas are upright bipeds, so stacking = standing on heads, which the WBB reference (quadrupeds) sidesteps. Options, in build order:
1. **Standing on the head, as-is (zero art).** Circus-acrobat read; precariousness is the point, and the standing pose already exists in all 8 facings — full parade freedom for free. *Prototype this first.*
2. **Hand-drawn sitting cel** (legs crossed → reads as sitting on the head — Ameya's suggestion). Cheapest honest version: riders always face **down** (toward the viewer) regardless of travel direction, so exactly **one** cel is needed, drawn by trace-and-nudge in the body editor and baked like the hat. Per-facing sitting cels (5 rows) only if the always-face-down cheat looks wrong.
3. Decide on the live preview, like every motion call before.

### Tier 3 — the cascade (rare, the jackpot)

The chain-reaction knockout. **Directed, not hoped for — cartoon physics: outcome-authored, physics-flavoured.** Pure chance can't chain at our density (~200px mean neighbour spacing vs an 80px slide), so:

- **Arming:** the director arms tier 3 on a long jittered timer (`CASCADE_ARM` 2–5min). While unarmed, all collisions are ordinary.
- **Ignition:** once armed, the **next natural collision escalates** (or a stack topple, if one happens first — see coupling). Both parties take a hard slide aimed **at their nearest standing neighbour** (slide vector continuous, fall facing snapped to the 8), magnitude = the actual gap, so they land overlapping.
- **Propagation:** each felled panda, on landing, fells its own nearest standing neighbour within `CHAIN_RANGE` (~350px) the same way — a greedy nearest-neighbour BFS with **two fronts**, animated. A front with no neighbour in range dies out naturally.
- **Coverage: 70–90%, never 100%.** A full clear reads scripted. A couple of inexplicably untouched survivors are part of the joke, and the **oblivious one is structurally guaranteed to survive** (steering never targets it).
- **Recovery:** ordinary knock recovery, naturally staggered by fall order. **No freeze, no spotlight** — the world never pauses; chaos continuing around the watcher is the point now.
- **Staging:** the director prefers to ignite while the hat panda is **mid-inspection elsewhere** — it looks up from a sleeping panda to find the field going down behind it, then stands amid the bodies, gaze darting, not knowing where to start. (Decided 2026-07-23.)
- **Coupling rule (rarity lives in the director, not the trigger):** a stack topple is always just a topple — *unless* the director happens to be armed, in which case the topple serves as the ignition. Cascade frequency stays pinned to the arming clock no matter how many ignition sources exist; topple-into-cascade is the rarest, best co-occurrence the site can produce.

### The standing anchor — the oblivious one

One roamer, chosen at spawn (never the hat panda): it is never selected for anomalies, never mounts or is mounted, never targeted by cascade steering. It wanders a small patch, idles often, and sits placid through everything. Chance collisions may still knock it (keeps it honest). Every big event gets a comic foil; the hat panda may occasionally plant and watch *it*, puzzled — the panda that does nothing is the biggest mystery of all.

---

## The hat panda — attention, scramble, inspect, relax

The Phase-3 observer machinery survives nearly whole, **retargeted from lines to incidents**:

- **Incidents:** every anomaly/stack/cascade emits `{panda(s), position, tier, expiresAt}`. The hat panda holds **one** incident at a time.
- **Priority:** tier 3 > tier 2 > tier 1; within a tier, newer preempts older only after a stickiness window (~2.5s) so it doesn't thrash between simultaneous events.
- **Scramble:** `stepWeaving` threads through the troupe to the incident, routing round the hero card (`crossesFence`/`detourCorner`) — but the ghost-through escape hatch (`WEAVE_STUCK`) is **removed**. He is a real collider now, so when boxed he holds, re-vantages via `bestAxis`, or abandons the incident; if that all fails he takes the hit. Losing the crutch is the point.
- **Inspect:** plant at a closer standoff than the old line-watch (`INSPECT_NEAR` ~140px feels right for a single subject — tune live), face the subject dead-on via the axis logic, run the gaze scan (`pickGaze` retargeted: the subject, the point of impact, a nearby witness, a brief look-around). Hold until the incident expires or is preempted, then a beat, then back to ambient.
- **Ambient / relax:** no live incident nearby → plant somewhere clear (`bestAxis` crowd penalty, unchanged) and watch the field, gaze drifting between whatever's mildly notable. This is the "relax amidst the chaos" beat — it emerges from empty queue + planted idle; do not script it.
- **Arrival phenomenology (free, do not engineer):** long anomalies → it arrives in time, gets the long look. Instant ones → it inspects aftermath. Mid ones → coin flip. The mix of durations *is* the timing design.
- **Alertness → speed (a hierarchy, not a flat cap):** calm, he keeps `HAT_MOVE_MS` 540 with the 2s glide. Near danger he goes to **full alert and moves faster** — he has *intention*, unlike the trudging troupe, so spending full speed when it matters is earned. Hard ceiling: **never faster than zoomies.** Zoomies is *chaos*, not a panda capability; he doesn't get it. Order: `trudge < hat-calm < hat-full-alert < [dive-roll] < zoomies`.
- **Graduated avoidance — sidestep the easy, roll the hard.** His response is tiered so the roll stays rare and reads as an emergency, not his default:
  - **Sidestep (the easy tier):** while planted, a single *slow* panda drifting into his space (within `HAT_SIDESTEP_R` ~108px) gets a calm **step aside** at stride pace, toward the clearest cell (`bestEscape` with a crowd penalty so he never steps into a body). This is the "quick stepping" layer, and it plugs the gap where a planted watcher had *no* gentle avoidance and simply got walked into.
  - **Dive-roll (the hard tier):** the reflex (`hatReflex`, 80ms) spends a roll **only** for an emergency — a **fast** (zoomies/tumbler-skid, `threatSpeed ≥ HAT_FAST_SPEED`) threat anywhere in the alert radius, **two+** closing at once, or one already **too close to step out of** (`HAT_PANIC_R` ~50px). It reuses the knockdown cels (`roll: [3,4,5,6,5]` — a resequence, no new art) + the traveling-skid mechanic, launches toward the clearest cell, and pops straight up (no fallen frame, no get-up). Cooldown-limited. **No i-frames** — its only value is *where* it lands him, so picking clear space is the whole job; the cooldown is the honest limiter.
  - Detection is **omnidirectional** — he reacts to threats from any bearing, not just the way he's facing.
- **Collidable, two-way, rarity emergent:** an ordinary collider — he gets knocked *and* can knock (he's toeing the line, not orchestrating from outside). Knockdowns stay rare **purely from his maneuvering** (dodge quality, `DANGER_R`, roll cooldown) — never from touching the chaos. On a knock: **hat flies off, and he sprints** (full alert, not a trudge) to fetch it, then back to watching.

---

## Demolition & salvage (Phase 0 scope)

**Delete** (from `pandas.js` + `styles.scss`): `class Line`, `manageLines`, `joinLine`, snake stepping, `LINE_*`/`JOIN_*`/`MANAGE_*`/`POST_PATCH_*`/`DISSOLVE_*` constants, `runPatch`, `managePatch`, all `PATCH_*` constants, the freeze system (`frozen`, `freezeFrame` call-sites guarding it), spotlight CSS (`.intervening`, `.spot`), `makeLine`, `pushAside`, `angleBetween`.

**Keep as-is:** entrance (Phase 1), ma5a collision + knock/fall/get-up, tap-to-knock, hat drop/retrieve skit, the hero-card fence, hat pipeline.

**Salvage (retargeted):** `throwArc` (mount hop, topple tosses), `spin3d` (spinner), asymmetric unstoppable-force collision (stack bottom), the whole observer suite (`observe` loop skeleton, `stepWeaving`, `bestAxis`, `crowdAt`, `pickGaze`, `AXES`, watch/relocate hysteresis).

**Engine debts folded into Phase 0** (binding — they predate this spec and were never done):
1. **Transform glide.** Motion moves off `margin-left/top` onto `transform: translate` (z-index still from y). Kills the layout-invalidation churn; also makes the 20Hz `getBoundingClientRect` collision reads cheap (layout stays clean under transform-only animation). Optionally switch collision to logical `x/y` later — note the caveat that logical leads rendered by up to a stride.
2. **Off-screen / hidden-tab pause.** IntersectionObserver on the stage + `visibilitychange`: all loops (animate, wander, director, incidents) pause when the hero is off-screen or the tab hidden. The old spec called this binding; it was never implemented.
3. **Mobile: no pandas.** Below the mid breakpoint (~800px) the stage doesn't populate at all (decided 2026-07-23: the hero content owns that space). `PANDA_COUNT` 20 wide / 12 mid stands.
4. **Reduced motion = a composed tableau**, not random scatter: troupe scattered, one 3-stack mid-parade, one fallen panda with the hat panda planted facing it. The frozen story, per the original locked decision.
5. Small repairs: `Panda.timers` unbounded growth; `bestAxis` off-stage fallback.

---

## Implementation phases

Build one at a time, review each on the live preview before the next (the workflow that worked last time). Bar for every phase: **alive, not noisy.**

- **Phase 0 — demolition + engine debts. ✅ shipped.** Ripped out lines/patching/freeze; landed transforms, pause, mobile gate, tableau, leak fix. The observer suite survived retargeted from `Line` to a generic `subject`, so Phase 2 replaces one call (`pickSubject`) and the rest of the loop is untouched.
  - *Found:* the conga lines had been incidentally making ~10 pandas collision-immune. Without them the ma5a baseline alone kept a mean of **12.8 of 20** pandas on the ground (vs 7.9 before) — enough to bury the Sleeper. `PANDA_COUNT` dropped to **10 wide / 7 mid** so an anomaly reads against a calm baseline; raise it once the tiers are in (Ameya's call, 2026-07-23).
  - *Fixed:* `stepWeaving` stepped along the **normalized** `AXES`, so the hat panda's diagonal strides were 50px against every other panda's 71 — it had been losing ~30% of its ground on every diagonal. `AXES` is now for scoring bearings only; both steppers share `stepCell()`.
- **Phase 1 — director + first anomalies. ✅ shipped.** The scheduler, plus sleeper / spinner / tumbler and the oblivious one. Each anomaly owns its panda through an `owns()` closure; a real knock clears `anomaly`, so the sequence sees it lost the panda and stops rather than fighting for it. Verified: 0 wedged pandas over 2 minutes, no console errors, ground occupancy mean 4.5/10.
  - *Open:* `OBLIVIOUS_R` was cut 170 → 110. At 170 the oblivious one covered as much ground as an ordinary roamer (~270px in this short band), so the whole feature was imperceptible. Tune on the preview.
- **Phase 2 — hat panda attention + the line-walker.** Built and reviewed part by part on the live preview:
  - **2A — Incident attention (spec core). ✅ shipped.** `incidents[]` with tier priority + ~2.5s stickiness; the `pickSubject` seam is now the incident-first `pickWatchTarget` (ambient wander-watch as the empty-queue fallback = the relax beat); `INSPECT_NEAR` standoff. Anomalies emit on `beginAnomaly`; TTL = episode length + a short aftermath linger. Lateness emergent from unequal TTLs + one attention slot. *Refinement (Ameya): same-tier ties resolve **nearest-first** (recency only breaks a distance tie), so he doesn't sprint past a near incident to a just-happened far one.*
  - **2B — Two-way collidability + recovery wiring. ✅ shipped.** Observer skip removed from `collisionCheck`; `knock()`/`pickUpHat()` resume `observe()` (not `moveAbout()`); knocked → hat flies off → **sprints** (`HAT_SPRINT_MS`) to fetch it → back to watching. The observe loop now *dies* on knock so recovery is the sole restart (no double loop). Also: he's tappable now (a poke triggers the whole skit — handy for review).
  - **2C — Alertness + the dive-roll + the sidestep tier. ✅ shipped.** Alert stride (`HAT_ALERT_MS`) near danger; the fast reflex (`hatReflex`, 80ms) spends a cooldown-limited `dodgeRoll` (no i-frames) only for emergencies. *Refinement (Ameya): added the **sidestep tier** — a planted watcher steps calmly aside from a single slow drifter, so the roll is reserved for fast/crowd/too-close. Fixed the "walks right into pandas while planted" gap.* Knob-heavy; still tuning feel.
  - **2D — Nav cleanup. ✅ shipped.** Deleted the `WEAVE_STUCK` ghost-through in `stepWeaving` (and the now-dead `stepToward`); a boxed watcher now **holds** (stepWeaving stands a tick when every step pushes deeper into a crowd), and if he stays boxed `observe()` escalates on a stuck-tick counter: first **re-vantage** (`bestAxis` gains an `avoid` param → a genuinely different approach angle), then **abandon** (the incident is flagged `abandoned` so `topIncident`/`pickWatchTarget` skip it; he drops the subject and moves on). `startRelocate()` resets the escalation accounting on every fresh vantage walk. If avoidance *and* both escalations fail he simply takes the hit — the crutch is gone. *Acceptance: he never phases through anyone again — met by construction (`stepWeaving` only ever holds or steps to an in-bounds cell; there is no straight-line fallback left). Tuning `WEAVE_STUCK`=5 (≈2s to re-vantage, ≈2s more to abandon) on the preview.*
- **Phase 3 — remaining tier 1. ✅ shipped (2026-07-24).** Loop (retraces its closed octagon `LOOP_LAPS` 2–4× at `LOOP_STEP_MS`; the 8 unit steps sum to zero so each lap closes), starer (faces the nearest edge, holds the settled f1 idle `STARE` 6–12s), zoomies (locks a random heading, dashes at `ZOOM_INCR`/`ZOOM_STEP_MS` ≈ 0.17px/ms = 3× trudge with `.stop` killing the glide, until `applyPos` is fully blocked → crash + `lieDown` tumble, or a panda collision → collisionCheck knocks it and clears the anomaly). `threatSpeed` gained a `zoomies` case so the runaway reads as fast and the watcher spends a dive-roll on it; the full six now feed one `ANOMALIES` roster (never the same kind twice running). *Acceptance: variety reads — six kinds across three duration classes, tuned live. `LOOP_STEP_MS`/`STARE`/`ZOOM_*` are guesses; watch the loop's pace and the zoomies' crash-vs-knock split on the preview.*
- **Phase 4 — the stack. ✅ shipped (2026-07-24).** A `Stack` coordinator (`stackDirector`, `STACK_GAP` 60–120s, at most one alive) picks a base with headroom + the 1–2 nearest roamers as riders. **Mount:** the base is `solid` from frame one (never jostled apart); each mounter ghosts up (`flying`) and `throwArc`s onto the head (`MOUNT_HOP_MS`, stacked at `RIDER_RISE`=62). **Parade:** the base drives its own tick-by-tick gait with `.stop` (no glide) so the pinned riders — ghosts (`riding`), z-ordered above — track it exactly; it wanders as the unstoppable force (the existing `solid` asymmetry knocks roamers aside, is never knocked). **Wobble:** each rider's lateral sway + a foot-pivoted rotate grows with parade age (`WOBBLE_MAX_AMP`, higher riders swing more), foreshadowing collapse. **Topple:** at `age ≥ life` (`PARADE` 18–34s) *or* a zoomies within `TOPPLE_HIT_R` of the base → riders `throwArc` off and `collapseRecover` (fall→get-up); a rider landing on a bystander is one free collateral knock (the Bug-1 fix means only the bystander goes down). One long tier-2 incident on the base (outranks every tier-1 → the wobble is irresistible), shortened to a brief linger on topple. **Rider pose: standing (option 1, zero art)** — held `stop` cel facing down; no sitting cel drawn. `throwArc` now self-adds `.flinging`/`.flying` (it had no caller before); new `.riding` CSS pins transition-off with a foot pivot. *Acceptance: the tower forms, sways, falls — judge on the preview whether standing-on-heads reads as a stack or a smear (the one open art call), and whether the topple's a satisfying payoff. Rates (`STACK_GAP`, `PARADE`, `WOBBLE_MAX_AMP`, `TOPPLE_SPREAD`) are guesses.*
- **Phase 5 — the cascade. ✅ shipped (2026-07-24).** A `cascadeDirector` arms tier 3 on a long jittered clock (`CASCADE_ARM` 2–5min; `CASCADE_KICK` 40s for the first, so a reviewer catches one). While unarmed every collision is ordinary. Armed, the **next natural collision between roamers ignites** (`collisionCheck` collects the just-knocked roamers as seeds) — but only via `cascadeElsewhere`, which **holds the arm** if the collision is right under the watcher's gaze (his subject, or within `INSPECT_NEAR·1.6`), so it erupts where he isn't looking; a `forceIgnite` backstop (`CASCADE_ARM_TIMEOUT` 40s) manufactures one from the roamer farthest from him if no natural collision arrives. **Ignition → two-front BFS:** each felled panda claims its `nearestStandingNeighbour` within `CHAIN_RANGE` (350), and a `CASCADE_HOP` (150–230ms) later `cascadeKnock`s it — a plain knock-recovery whose slide is *steered* (continuous vector, gap-sized, facing snapped to 8) at that neighbour's own nearest, so it lands on the next domino — then recurses. Claims lock the victim (`cascadeLock`), so the two fronts never fight over one and `collisionCheck` skips claimed pandas (unclaimed bystanders still take honest spillover knocks). **Coverage** `CASCADE_COVER` 0.70–0.90 of the roamer universe (the oblivious one is outside the universe → structurally spared; sparse spreads fizzle early → extra stragglers; a headless BFS check over 5000 random fields confirmed never >90%, always ≥1 survivor, oblivious never felled, nothing felled twice, always terminates). **Coupling:** `Stack.topple()` ignites only when armed — the rarest, best co-occurrence, no staging guard (the watcher's already there). **Staging payoff:** a stationary tier-3 incident at the seeds' centroid outranks every tier-1/2 → he scrambles to the origin, arrives late (it propagates faster than he crosses the field), and stands amid the recovering bodies, gaze darting. **No freeze, no spotlight;** recovery is ordinary ma5a get-up, staggered by fall order (open decision #2: resolved to *just get up*, no "check on a victim" flourish). *Acceptance: rare, spectacular, survivors standing, the watcher overwhelmed. Rates (`CASCADE_ARM`, `CHAIN_RANGE`, `CASCADE_HOP`, coverage) are guesses — tune on the live preview; `CASCADE_KICK` gives the first one at 40s.*
- **Phase 6 — the learned line-walker. ⏳ IN PROGRESS (no longer "optional/parked" — see the banner at the top).** Its groundwork phase (A: the fixed-tick engine + the action seam) is **complete as of 2026-07-27** — the port runs the whole scene, sim and presentation, and it is now the homepage default — and so is its **corpus phase (B)**: the rollout harness, the heading-cone observation encoder, per-tick ground truth, the shard format, 10.08M ticks of training rollouts plus an eval corpus, and the roster freeze. C–G (yardstick gate, BC tracer bullet, emergence run, ship, probing study) have not started. Replace the hand-written maneuvering rules (sidestep + dive-roll + weave) with a **small neural-network policy** that learns to walk the line itself. Thematically the self-portrait goes recursive: a mech-interp site whose hero is literally a tiny *trained* policy dodging chaos. **→ Now specced in [panda-policy-net.md](panda-policy-net.md) (2026-07-24, reworked after design discussion): a partially-observed, memoried **small transformer** playing the **viewing game** (+points for watching anomalies, −points for getting hit, no incident feed) — a task designed so the score is unearnable without reconstructing the hidden anomaly FSMs — trained BC-warm-start → RL, then probed as a real interp project (knowability spectrum as built-in controls). Direction agreed 2026-07-24; the engine (A) and the corpora (B) are built, the training itself is not.** Original rationale for the sequencing and shape:
  - **After Phase 5, because the environment must be complete.** The policy trains against the *full* chaos — all tier-1 anomalies, the stack, the cascade. Train against a partial world and every later phase forces a retrain. So build the whole thing on rules first; **the rules-dodge is the stand-in, the baseline, and a ready-made imitation-learning warm-start.**
  - **Shape:** ~~a small MLP → action (step in one of 8, or roll), weights exported to JSON~~ — **superseded; do not build from this bullet.** [panda-policy-net.md](panda-policy-net.md) locked it as a **4-layer, d64–128 transformer** with per-panda tokens over a partially-observed (heading-cone FOV) history, a **17-way action space** (hold / step×8 / roll×8) at 10 Hz, BC-from-the-rules-expert as a style prior only, then PPO with KL-to-frozen-BC in place of an entropy bonus — and **binary** weights + a manifest, never JSON floats. The action seam and the rules expert behind it are already built (`engine/actions.js`, `engine/watcher.js`).
  - **i-frames ON here (unlike the rules version).** With a learned policy, the roll becomes a *committed* escape and all the skill moves into *when* it chooses to roll — cleaner fallibility (fail = bad timing / on cooldown, not bad luck mid-roll).
  - **Eyes-open:** this is a genuine sub-project (a sim matching the exact panda physics, reward shaping, a training loop, weight export) — likely more work than Phases 2–5 combined. Worth it only as a thing built for its own sake, which for this site it may well be.

## Open decisions

1. **Rider pose** — **seated riders now shipped to the live site** (2026-07-24), replacing the weird-looking standing option. Riders are drawn seated and **face the tower's heading** (8 facings from 5 pixel maps + a mirror, via `DIR_SPRITE` like the walk sprite), with a **head-linked teeter**: a foot-pivoted `rotate` whose head-shift accumulates up the tower (`SIT_TILT_DEG`, `SIT_TRAVEL_PX`), so the base leads and the wobble travels up. On topple the seated layer is hidden and the ma5a sprite returns for the fall. **Art status: ✅ all five facings finished and baked (2026-07-24).** Ameya drew the remaining four in the workshop and the five center maps are baked into `pandas.js` as `SIT_CELS` (~37KB); the runtime cel rasterisation (and its `sitifySit` squash) is deleted. The full 15-frame export (leans carry hand-edits — verified not pure `lean(center,±3)` derivations, so they're preserved verbatim) lives in the workshop's `BAKED` constant, which is now its boot state and the `reset to site` target. The pixel studio `design/sketches/panda-sitting-workshop.html` (also an artifact) draws all 8 facings + a compass + a spinning-wobble preview + solo/tower galleries, has undo/fill/brush/onion/clone editing, and exports `SIT_DIRS`. Seated-rider rendering: `.sit` layer holds an `<svg viewBox="0 0 48 48">` of rects; `.riding` hides the walk sprite; `.sit.flip` mirrors the three left facings.
2. **Cascade recovery flourish** — ~~do the fallen just get up ma5a-style, or does the hat panda "check on" one specific victim first?~~ **Resolved (2026-07-24): just get up ma5a-style**, staggered by fall order — the ripple is already a nice recovery, and the tier-3 pull to the origin plus gaze-scan reads as "checking on the wreck" without a bespoke flourish.
3. **Exact rates** — all `*_GAP` constants are guesses; tune on the preview with real dwell.
