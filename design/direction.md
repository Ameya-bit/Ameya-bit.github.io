# Design Direction — ameyapanchal.github.io redesign

*Started 2026-07-10 from reverse-engineered analysis of emilkowal.ski, devouringdetails.com, and ma5a.com (actual shipped CSS/JS, not visual guesses). Layer 1 (the Emil base) is **built and approved** as of 2026-07-10; its as-built conventions are below. Next layers at the bottom — build them one at a time, workshopping between each.*

## The governing formula

**Severity buys whimsy.** ma5a's base is *more* minimal than a typical minimal site — one font, one size, two colors, one column. That severity is exactly what gives its bunnies and cursor trail room to read as signature instead of noise. Emil proves the same equation from the other side: a near-monotone system makes his one flourish (serif-italic word emphasis) feel deliberate.

Our design = **Emil-severity base (done) + Rauno minimap TOC + one or two mech-interp-native whimsy elements, fenced by ma5a's rules.**

The whimsy must speak the site's research language (attention, copying, scattering) the way ma5a's bunnies preview an animal-toy portfolio. Decoration that demonstrates competence reads as signature; off-topic decoration reads as clutter.

---

## Layer 1 — The Emil base (BUILT, approved). As-built conventions

All implemented in `styles.scss` + `_quarto.yml` + `index.qmd`. These are the rules every future layer must respect.

> **Reading note (2026-08-05, doc audit).** This section records the base **as approved on 2026-07-10**. Layer 1b (2026-08-02) and the fluid pass (2026-08-03) have since moved several of its numbers, and until this audit they were still written here as current fact — the section read as the site's spec while the session log below described a different site. Superseded figures are now marked inline. The principles are untouched; three numbers changed enough to matter:
> - **Type is 17px at the 1440px calibration root, not a flat 16px** — and the root itself is a clamp (16px under ~880px → 19px at 2560px). The *flatness* is the rule; the number never was.
> - **The rhythm was rebuilt around the 28.9px line**: paragraphs 43px, h2 72/29, h3 52/22, figures 96px.
> - **The accent exists** — `#d55e00` marks / `#b04d00` text, spent on interaction only.

### Type
- **Font**: Nunito variable (Google Fonts, `wght 400..700`, roman + italic), ligatures off, antialiased *(changed 2026-08-03 from Inter — see the session log; Inter's `cv01`/`ss03` feature settings came out with it, Nunito has no such axes)*. Mono: system stack (ui-monospace/SF Mono/Menlo). No serif (the Tiempos-style italic flourish remains an open decision).
- **Flat 16px prose — with an apparatus tier under it (amended 2026-08-02).** The original rule said 16px for *everything* including captions and metadata. Re-checked against emilkowal.ski's shipped markup and that overshot: his prose really is flat (his post `<h1>` carries `class="mb-8 font-semibold"` with no font-size utility at all — it inherits the 16px root, and weight alone ranks it), but he keeps a whole second tier for things that aren't the argument. His figure attribution is `text-xs text-gray-1000`, his prev/next post links are `text-sm`, his code blocks are `text-xs`. Collapsing that tier into 16px is what made our figure captions weigh as much as the paragraphs they described.
  - **Prose stays flat**: body, h1–h6, post titles, listing items. Hierarchy = weight only: 400 body / 500 h3+labels+listing titles / 550 h2 / 600 h1+post title. *(Written as "flat at 16px"; the size became 17px at the calibration root in Layer 1b and fluid on 2026-08-03. Flat is the rule — the post title is the one sanctioned jump, see Layer 1b.)*
  - **Apparatus drops to `--text-apparatus` (14px), `--gray-1000`**: figure captions, post filing line (date + categories), TOC rail, epistemic-status blocks. Nothing that carries argument may use this tier, and nothing in this tier may grow back to 16px.
- ~~Line-height 1.65 for prose~~ → **1.70** (Layer 1b, with the 17px root); 1.6 for apparatus, unchanged. ~~Paragraphs end with `margin-bottom: 26px`~~ → **43px**, ≈1.5 lines (the 26px gap was 0.90 of a line and read as a loose line, not a break — see the rhythm rebuild in Layer 1b). Body text at `rgba(33,32,28,0.9)`, unchanged.
- **Emphasis is a contrast event, not just a weight event.** Emil's `<em>` is `font-serif-inline text-[0.98em] font-normal italic text-gray-1200` — a dedicated italic-only webfont *and* full ink against 0.9-opacity body. We don't take the serif (that flourish is his), but we take the principle: `<strong>` is 600 **and** `--gray-1200`, so the verdict sentences lift off the paragraph.
- **The scale-breaking exception stays exactly one**: the homepage hero headline — `clamp(2rem, 1.2rem + 3vw, 3.25rem)`, weight 600, letter-spacing −0.02em, line-height 1.12. The apparatus tier is a *tier*, not a second exception; nothing else may go larger than 16px. *(Amended 2026-08-04: still exactly one exception, but it is a display object now — `clamp(2.35rem, 1rem + 3.9vw, 4.7rem)`, tracking −0.025em, line-height 1.05. See the six-findings entry in the session log.)*
- Code: inline 0.857em on a `--gray-200` pill with `--gray-400` 1px border, radius 6px; blocks 13px mono in a bordered radius-8 card, `max-height: 332px`, scroll inside.

### Color — warm 12-step gray (Radix Sand), zero chroma
| Token | Value | Use |
|---|---|---|
| `--gray-100` | `#fdfdfc` | page background |
| `--gray-200` | `#f9f9f8` | raised surfaces / code bg |
| `--gray-400` | `#e9e9e7` | borders (figures, code, pills) |
| `--gray-500` | `#e2e1de` | text selection |
| `--gray-800` | `#bcbbb5` | link underlines at rest |
| `--gray-900` | `#8d8d86` | link underlines on hover |
| `--gray-1000` | ~~`#82827c`~~ **`#6f6f69`** | tertiary (dates, captions, listing metadata) — darkened 2026-08-02: the original was 3.80:1, a WCAG AA failure, and it carried every caption, date, category and rail item on the site |
| `--gray-1100` | `#63635e` | secondary (descriptions, ledes) |
| `--gray-1200` | `#21201c` | ink |
| `--hover-fill` | `#f5f4f4` | listing hover pill |

~~No accent color exists yet — reserved for the minimap TOC layer (open decision).~~ **Resolved 2026-08-02: `--accent: #d55e00` (marks) / `--accent-ink: #b04d00` (text), spent on interaction only.** It did not go to the rail in the end — a rest state cannot be paid for in accent (see the 2026-08-03 amendment in Layer 1b).

### Layout
- **Posts**: Quarto page grid intact and symmetric — `$grid-body-width`, `$grid-sidebar-width`, `$grid-margin-width` at 692/300/300px. Equal sidebar/margin keeps the article column truly centered. `main.content` gets 24px inline padding → ~646px measure. *(Amended 2026-08-03: all three are declared in **rem**, not px — 40.706/17.647/17.647rem, which is those same pixel values at the 17px calibration root. The root is fluid, so the column genuinely widens on a large display while the line stays ~75 characters.)*
- **Homepage only** breaks the narrow column (`page-layout: full`): full-viewport hero (`min-height: calc(100svh - 5.294rem)`, ~~`calc(100svh - 9.5rem)`~~ — retuned when the staircase hero was built) inside a `--shell: 1160px` container, then the wide writing list — **and, since 2026-08-04, a second Notes list under it**. Everything still built from Emil materials.
- **Masthead** = the Quarto navbar stripped to just the name: transparent, borderless, no search, no icons, `padding-block: 2rem 0`. It tracks `--shell` on the homepage via `body:has(.hero-screen)`. **On posts (≥992px) the name sits in the left gutter, flush with the TOC rail at the 24px screen inset (amended 2026-08-02)** — Quarto renders `#quarto-header` as `position: fixed`, so a name over the reading column gets scrolled underneath by the prose; parked in the empty gutter there is nothing for the text to pass beneath. Below 992px the TOC collapses, the gutter closes, and the name returns to the column edge. The override must be written `#quarto-header > .navbar`, not `.navbar`: Quarto ships `#quarto-header > nav { padding-left: 1em }` and an id outranks any amount of class weight, leaving the name 16px off the rail otherwise.
- No footer (`.nav-footer { display: none }`, no `page-footer` in yml). Social links live in the hero. ~~**Exception (2026-07-24): one faint centred colophon line (`.site-colophon`) at the foot of the homepage only**, crediting the ma5a panda sprite/engine.~~ **Withdrawn 2026-08-03 with the pandas** — the credit came out of `index.qmd` in the same edit as the `#panda-stage` div, because a colophon crediting a sprite engine the page no longer runs is an attribution for nothing. The no-footer stance is therefore unqualified again: every page is footerless. *(The `.site-colophon` rules are still in `styles.scss` and currently match no markup — dead until the troupe returns.)*

### Links
Ink-colored text, always underlined; only the underline is tinted (`--gray-800`, hover → `--gray-900`), `text-decoration-thickness: 0.08em`, `text-underline-position: from-font`, offset 2px. **No transition** — hovers are instant.

### Writing list (homepage)
Two-column grid per item (`1fr 8rem`): title (500) + description (`--gray-1100`) left, date + reading time right-aligned in `--gray-1000`. Hover = instant `--hover-fill` pill bleeding 12px past the text edge (`margin: 0 -12px; padding: 12px; radius: 8px`). No borders, no thumbnails. Collapses to one column under 620px (metadata on top).

### Post pages
**Masthead (rebuilt 2026-08-02).** Publication grammar, not Emil's — he opens a post with a bare `<h1>` and *nothing else*: no date, no description, no categories anywhere on the page. We have all three and they're worth showing, so the block reads title → standfirst → 1px `--gray-400` rule → filing line (date · categories) in the apparatus tier. 64px top padding, 56px below. Categories are back but as plain middot-chained text, never pills — pills would be the only rounded filled object on a post page. Implementation: the header is `display: flex; flex-wrap: wrap`, `.quarto-title` is `display: contents` (promoting `h1` and `.quarto-categories` to direct flex items so `order` can reach them), and the rule is the header's own `::after` — a pseudo-element on a flex container is a flex item, so it orders with the rest and needs no extra markup. **Every metadata selector must carry `#title-block-header`**: the prose block is scoped `#quarto-content main.content p`, the title block lives inside `main.content`, and an unqualified `.quarto-title-meta-contents p.date` loses the id-weight fight and snaps back to 16px.

**Figures (unframed 2026-08-02).** No border, no radius — the PNGs sit on white and the page is `#fdfdfc`, so removing the frame turns them from cards into ink printed on the page. The frame returns on hover as `outline: 1px var(--accent)` at `outline-offset: 6px` — outline so nothing reflows, offset so the ring never crops the artwork — and that hover *is* the click affordance. ~~32px~~ **96px** block margins (Layer 1b: 2.2:1 against the paragraph gap, so a figure reads as an event). Captions 14px `--gray-1000` left-aligned (the old rule pinned them to 16px with `!important`; that was the single biggest reason figures read as undifferentiated slabs).

**Figure zoom** (`assets/site/figure-zoom.html`, inlined via `include-after-body`). Click or Enter/Space on a figure opens it full-size over the page; click anywhere or Esc closes. Overlay is `rgba(253,253,252,0.96)` — the page colour, not a black scrim: the reading room doesn't go dark, the article steps back. 150ms opacity fade on `cubic-bezier(0.16, 1, 0.3, 1)`, zero under `prefers-reduced-motion`. Chosen over Quarto's built-in `lightbox:` (GLightbox, ~30kb + dark chrome to restyle) and over widening figures into the margins — the 644px column is the point of the post page, and expand-on-click keeps it while giving dense figures somewhere to go.

**Epistemic status / closing aside.** A paragraph whose whole content is italic (`<p><em>…</em></p>`) is, by convention in this writing, not part of the argument — it's the confidence note up top and the nudge at the end. Both posts bookend exactly this way, so `p:has(> em:only-child)` catches all four with no markdown changes. Rendered as apparatus: 14px, `--gray-1100`, 1px `--gray-400` left rule, 20px indent, italics removed (the block carries the meaning now).

**Math.** KaTeX display: 32px block margins, `overflow-x: auto`. The equations are wider than the measure on a phone and used to scroll the whole document sideways (476px scrollWidth at a 375px viewport). Wide content scrolls inside its own box; the page body never does.

h2 rhythm: ~~56px above / 20px below~~ → **72 / 29**; h3: ~~40/16~~ → **52 / 22** (Layer 1b, rebuilt on the 28.9px line — the old space-below was 0.69 of a line, so a heading sat *closer* to the paragraph above it than a paragraph break does).

### TOC (superseded 2026-08-03 — this is the rail's ancestor, kept for the record)
Left sticky sidebar in the grid's sidebar column, 14px, gray-1000 → ink on hover/active, 1px ink left-border on active. Quarto's own responsive collapse handles narrow viewports.

**What actually runs now** is the docked bar rail: per-section marks whose length encodes section length, words on request (scroll-up, hover, focus), ranked by ink and weight rather than accent, docked under the masthead on the title block's own line. Built across three sessions on 2026-08-03 — see Layer 2 below and the session log.

### Motion spec (Emil's principles — binding for all future layers)
1. Every animation needs a nameable purpose (explain / feedback / delight) or it doesn't ship.
2. Frequency decides: high-frequency interactions (hovers, keyboard nav) get minimal or **no** animation — list hovers are deliberately untransitioned.
3. Never animate keyboard-initiated actions.
4. UI motion < 300ms; ease-out for enters/exits; custom beziers, never CSS defaults.
5. Never animate from scale(0); press feedback = scale(0.97) on `:active`.
6. Transform/opacity only; everything gated behind `prefers-reduced-motion`.

### Quarto implementation gotchas (learned the hard way — don't re-learn)
- **Quarto hoists the first `<h1>`** in a page (even inside raw HTML blocks) into `#title-block-header`. The hero headline is therefore a `<p role="heading" aria-level="1">`.
- **Specificity**: prose rules are scoped `#quarto-content main.content p/a/h3…` (id-weight). Any component override inside content (listing, hero) must nest under the same `#quarto-content main.content` scope or it silently loses.
- **Never flatten the page grid or `position: fixed` the sidebar.** quarto.js recomputes sidebar geometry on scroll; fighting it makes the TOC collapse into a full-width header block in real browsers (worked in static screenshots, broke live). Center the column with symmetric grid widths instead.
- `.navbar-brand-container` ships with `mx-auto` — zero its margin or the masthead drifts off the column.
- `design/` is excluded from render via `project.render: ["**/*.qmd", "!design/"]`.
- `search: false` under `website:` kills the navbar search icon.

---

---

## Layer 1b — The apparatus tier (BUILT 2026-08-02). The un-Emil half

*Built after an outside design review. The finding was not that the base is wrong — it's that **the severity was paid for and the whimsy was never spent**: Layer 2 unbuilt, accent undecided, serif undecided, so a visitor got the austerity with none of the return. Layer 1b is the answer to "how is this not just Emil's site," and it is deliberately drawn from what this site **publishes** rather than from whose CSS it was reverse-engineered out of. Emil runs a personal blog. We run a publication. A publication has an apparatus; a blog does not.*

**The rule: prose keeps Emil's voice, apparatus takes a second role.** Flat 17px, weight-only rank in the body, instant hovers, no chrome — unchanged. What is new is a **monospace numbering system**. *(Written as "17px Inter"; the family became Nunito site-wide on 2026-08-03 and every "Inter" in this section now means "the prose face" — the argument never depended on which one.)*

> **Amended 2026-08-05 — mono is for instrument readouts; the site's own numerals are Nunito.** Ameya's call on the gutter numerals: they earn their keep (they identify sections *in the article*, which the rail's marks cannot do), but they should not be a second typeface. When the rail dropped its numerals in 2026-08-03, the gutter's lost their counterpart — and **one mono object on a page of Nunito is not a numbering *system*, it is a material used once**, which is the failure this whole layer was written against. They are Nunito at `--text-apparatus` now, with `tabular-nums` so 01 and 11 start on the same vertical line. What mono still holds: the IOI lens figure's layer and head indices, which are labels *inside an instrument*, and the hero ledger's counts. **Open, and small: the ledger's counts and the gutter numerals are arguably the same object, so if the ledger should follow, that is a one-line change.**

- **Mono is for the numbering system, nothing else** *(amended same day — see below; and again 2026-08-05, above).* Section numerals in the article gutter and their counterparts in the rail: one material, two places, the same job. Everything else in the apparatus — dates, categories, TOC labels, listing metadata — is the prose face at `--text-apparatus` in `--gray-1000`. *(The colophon was in that list until it came out with the pandas, 2026-08-03.)*
- Tokens: `--font-apparatus` (system mono stack), `--text-apparatus-mono: 0.8125rem` (13.8px, numerals only), `--text-label: 0.6875rem` (11.7px, uppercase, tracked — kickers only, in the prose face).

**Amendment — mono was first applied to the whole apparatus tier, and pulled back the same day.** Ameya's read on the built page: *"the toc is a different font and size than the rest… it's not cohesive like a minimal website would be."* He was right, and the failure is diagnosable: the tier changed **typeface AND size AND tracking AND case simultaneously**. That is not a quieter tier, it is a second voice — and on a page whose content is only paragraphs and charts, a second voice has nothing to do but clash. It also caused a concrete regression: mono is wider, so the date and categories, which are meant to share one filing row, wrapped onto two lines.

The principle that survived: **numbers are what an instrument shows; the moment mono touches words it stops reading as instrumentation and starts reading as a different font.** Differentiate on one axis at a time.

**Related — the materials count.** The post page briefly carried ~9 materials (Inter prose, Inter gray secondary, mono uppercase tracked kicker, mono tracked labels, mono numerals, vermillion, hairline rule, left-rule aside, unframed figure) for a page whose content is *paragraphs and charts*. Emil runs about four — body text, small gray text, bold heading, bordered card — repeated dozens of times each, and that repetition is what reads as a system. **Cohesion is few materials used often, not many materials used once.** Pulling mono back to numerals removed two.

**Sections number themselves.** `counter-reset: section` on `main.content`, `counter-increment` on h2, rendered `decimal-leading-zero` in mono. Below 1200px the numeral sits inline ahead of the title; at ≥1200px it goes `position: absolute; left: -3.4rem` and hangs in the gutter, so the prose edge stays a clean vertical line and the numbers read as their own column down the page. The TOC carries a second, independent counter on `#TOC > ul > li` so the rail and the headings agree. Homepage is unaffected — the listing uses h3 and the hero headline is a `<p role="heading">`, so there are zero h2s on the index.

**Accent — open decision #1, resolved: `#d55e00` / `--accent-ink: #b04d00`.** Not chosen so much as discovered. Every figure on this site already reaches for Okabe-Ito vermillion when it needs to say *this is the finding* — the copying-score bar, the DLA annotation, the S-inhibition boxes. It is warm, so it belongs on sand, and it makes a highlighted bar and a hovered link literally the same colour. Two steps because one can't do both jobs: `#d55e00` is 3.80:1 (fine for marks, 1px ticks and the 3:1 non-text bar, **not** for small text), `#b04d00` is 5.28:1 and safe for text.

**Spending rule — the accent appears only on interaction.** Link hover underline, figure hover outline, `:focus-visible` ring. At rest the page is monochrome, so severity is intact and colour reads as a *response* rather than decoration. This is also the un-Emil move on motion: his hovers are colourless and instant; ours stay instant and gain colour.

> **Amended 2026-08-03 — the active TOC tick is no longer an accent job.** It used to be listed here (tick in `--accent`, numeral in `--accent-ink`) and it was the one entry that did not fit its own rule: every other job fires on hover or focus and ends when you leave, while the active tick is a *rest* state, so vermillion sat in the margin for the entire duration of a read. That is not colour as a response, it is the only colour on the page, permanently, in peripheral vision. The rail now ranks by ink and weight instead — gray-1000/400 → gray-1200/500 — which is Emil's move and costs no material the page does not already use. **Rule of thumb the exception yields: if a state persists while the reader is doing nothing, it cannot be paid for in accent.**

**Figures break the measure.** At ≥1200px, `.quarto-figure` takes `margin-right: -200px` into the empty right band — display width ~646px → ~846px. Two reasons: the figures are exported 1128–2210px and all shown at 646px, so the same 10pt label renders between **8.1px and 15.9px** across the set (a 2× swing); and a document where every element is one width is the definition of bland minimal. Captions do *not* follow the figure out (`max-width: 40rem`) — a 14px caption at 846px is a worse line than the one it replaced. Guarded by `overflow-x: clip` on post pages — `clip`, not `hidden`, so it creates no scroll container and the sticky TOC keeps working. Selector must be **descendant, not child**: Quarto wraps each h2 and its content in `<section class="level2">`, so figures are grandchildren of `main.content` and a `>` matches nothing.

**The scale's one reckless jump: the post title, 17px → 1.6rem / 27.2px** (line-height 1.22, tracking −0.015em). h2/h3 stay flat underneath it, so the system is *one event, then severity* rather than *no events*.

This breaks the original flat-scale rule on purpose, and the reason is that the rule was borrowed without its support structure. **Emil can run a flat scale because his column alternates prose with bordered demo cards every few lines, and those cards carry all of his page's punctuation.** This site took the flat scale but not the cards (figures here are deliberately unframed), and paired both with long paragraphs — three uniformity decisions stacked, and the column went gray. Every reference concentrates contrast in one place rather than spreading it: Ciechanowski jumps 2.8em→1.8em (1.56×) then runs flat, Distill 40px→24px (1.67×) then flat. Before this the only scale exception was the homepage hero — the page a reader spends the *least* time on, while posts had no contrast event at all.

**Vertical rhythm rebuilt around the 28.9px line** (17px × 1.70). The first pass raised the type and left the spacing, which made things worse, not better: the paragraph gap stayed at 26px — **0.90 of a line** — so a paragraph break read as a slightly loose line, and space below an h2 was 20px, **0.69 of a line**, so a heading sat closer to the paragraph above it than a paragraph break does. Now: paragraphs and lists `43px` (≈1.5 lines), h2 `72px / 29px` (2.5 / 1.0 lines, space-above ≈2.5× space-below so a heading binds to what follows), h3 `52px / 22px`, epistemic block `56px`, code and display math `48px`, figures `96px` (2.2:1 against the paragraph gap).

**The honest limit on spacing.** Emil's paragraph gap is *also* about one line and his page still breathes, because his paragraphs are 2–3 lines: his gap is ~45% of a block. At 8 lines a 26px gap is ~11% of a block, and matching his ratio would need a ~104px gap, which would look absurd. 1.5 lines buys back roughly half of it. **The other half is paragraph length — Layer 5, not CSS.**

**Filing row fixed.** Quarto ships `.quarto-title-meta` as `grid-template-columns: repeat(2, 1fr)` for multi-field bylines; with a single field it still reserves both columns, so the block sits at twice the width of the date. That is why date + categories wrapped to two lines as soon as the categories got any wider. One `max-content` column plus `margin-left: 1.75rem` on the categories, and the row fits with ~200px to spare.

**TOC hanging indent.** The numeral was an inline `::before`, so a wrapped item put its second line under the *number* instead of under the label — three of six items were ragged, which is most of why the rail read as unformatted rather than deliberate. Now `position: relative; padding-left: 2.4rem` on the anchor with the numeral absolute at `left: 0.7rem`. Nested (h3) items get the same padding so they can't reintroduce the ragged edge; neither post has an h3 yet.

**Figure rhythm 96px** against a 43px paragraph gap — 2.2:1, so a figure reads as an event. Ciechanowski runs 3:1 and it is most of why his figures feel like the point of the page. The original 32px against 26px was 1.2:1, very nearly uniform.

**Type: root 17px, prose line-height 1.70** (was 16px/1.65). The measure is fixed at ~646px by the grid, and at 16px that is ~80 characters per line — the longest line in the genre paired with the smallest letter. Tufte runs 21px/~64ch, LessWrong ~19px/~70ch; Distill tolerates ~83ch only on 1.7em leading and larger type. 17px gives ~75ch. **The column was deliberately not narrowed** — that would shrink the figures, which are the weaker link. Note `$font-size-root` reaches the page as `--bs-root-font-size`; `:root{font-size:11pt}` in the compiled CSS is `@media print` and is not a conflict.

**Contrast: `--gray-1000` `#82827c` → `#6f6f69`.** The old value was 3.80:1, a WCAG AA failure, and it carried every caption, date, category, TOC item, listing timestamp and the colophon — all at the small size. `#6f6f69` is the same hue and chroma one step darker: 4.97:1. The only approved palette value changed; the rest of the ramp already passes.

**The page grid is capped and centred** — `#quarto-content.page-columns { max-width: var(--page-max: 1440px); margin-inline: auto }`. The grid's outer slack columns are `5fr`, so at a 2700px viewport each grows to ~784px, stranding the 220px rail in ~670px of nothing and leaving masthead, rail and article reading as three unrelated objects adrift in the left half of the screen. One rule, no flatten, no fixed sidebar — Quarto's sticky recompute never notices. The masthead tracks the same capped box with the grid's own `1.5em` screen inset as padding, so the name lands flush with the rail at *every* width instead of only the one this was tuned on. (`#quarto-header` really is `position: fixed` — it ships `class="headroom fixed-top"` — so the gutter placement stays correct.) The rail also moved from `justify-self: start` to `stretch` + `min-width: 0`: at ~1000–1300px the left band narrows below 220px and the rail used to overflow its track and run under the article.

**Layer 4 brought forward — cross-document view transitions are in.** `@view-transition { navigation: auto }` plus `view-transition-name: masthead-name` on `.navbar-brand`, so the name holds still while the page changes under it. Two lines, fully progressive, no JS needed since every page ships this stylesheet. The UA-generated animation is *not* covered by `prefers-reduced-motion` on its own, so `::view-transition-group/old/new(*) { animation: none !important }` cancels it explicitly.

**Metadata (`_quarto.yml`).** `open-graph: true`, `twitter-card: {card-style: summary_large_image}`, `favicon: assets/favicon.svg`. Research circulates as links on X / LessWrong / the Alignment Forum, and every share of these posts previously rendered as a bare URL — no card, no image, no author — while five publication-grade figures sat in each post unable to be the preview. Both posts already declare `image:` in front matter, so post cards populate immediately. The favicon is **cel 0 of the `down` row of the hero sprite sheet**, extracted from `assets/pandas/engine/render/art-data.js` and cropped to its own bounds (32×32 viewBox, 1082 bytes, 9 paths) — the tab mark and the troupe are literally the same drawing, and nothing new was drawn ([[no-ai-generated-art]]).

**`design/figures.mplstyle` — written, and now applied to every post** (IOI 2026-08-03, induction-head 2026-08-05, pulse-profile 2026-08-06; each has its own `compute.py`/`plot.py` pair under `figures/`, so none needs a model to be restyled again). The rule: fix the **width** at 1280px (846 × 1.5, transformer-circuits' rule), vary height per plot, Okabe-Ito cycle, and the colormap rule stated in comments — diverging pinned to zero for signed quantities, sequential from zero for unsigned. **The width rule had a hole until 2026-08-06**: on macOS matplotlib's default `macosx` backend applies a 2x device-pixel ratio that `savefig` honours, so the pinned 8.0in canvas wrote 2560px and the committed script did not determine its own output. `mpl.use("Agg")` before the pyplot import, in every `plot.py`, is what closes it.

---

## Next layers (in build order, one at a time)

> **Where the layers actually stand (2026-08-05, doc audit).** The heading says "next"; three of the four have moved since it was written. **Layer 2 — built**, in the site's own form rather than Rauno's (2026-08-03, three sessions). **Layer 3 — paused, not cancelled** (2026-08-03: pandas off until the base stands alone; Ask Niche survives post-local on the induction post). **Layer 4 — built early**, in Layer 1b (2026-08-02). **Layer 5 — running, per post**; all three posts have had the pass. What is genuinely *next* is not on this list: the homepage grows from a two-list index into a curated catalogue of several work types (repos, papers), per the 2026-08-03 direction reset. **First instalment landed 2026-08-06** — the repos, as `/instruments/`, reached from the ledger's third row. The catalogue grows by adding rows to the ledger, not lists to the homepage: the index stays a reading surface, and each new kind of work gets its own address.

### Layer 2 — Minimap TOC (Rauno / devouringdetails.com) — **BUILT 2026-08-03**

> **Amendment 2026-08-03 (evening) — the vertical centering is rejected; concept A (dock under the masthead) is BUILT.** Ameya's read on the built rail: mid-viewport "doesn't look right." The diagnosis: Rauno's centered rail works because his whole page is a centered composition; on this site everything else — masthead, title, content — anchors top, so the rail was the page's *only* vertically-centered object and read as unaligned rather than deliberate. It also glued the marks to resting eye height, which is the exact peripheral-attention problem the collapse was built to solve.
>
> **A as built:** the 100svh flex-centering box is gone; the sidebar is `align-self: start` (the grid was stretching it to full row height, and an element as tall as its container has no travel left to stick in) with `position: sticky; top: calc(3.5rem + 1.75rem)` — the title block's own line, so the margin reads name → contents → quiet, aligned with the article's header rather than at an invented offset. Verified stable to ±0.2px across every scroll state at 1024/1440/2560; collapse, reveal, and instant hover all unchanged.
>
> ⚠ **Discovery that cost the first attempt: quarto.js writes INLINE `top` and `max-height` onto `#quarto-sidebar`, synced to headroom's pinned/unpinned state** — its compensation for a header that used to slide away. Ours fades in place now, so that compensation was pure motion: the rail was silently jumping the masthead's height (59px) on every scroll-direction flip — *including before this change, under the centered layout, where the 100svh box happened to mask it.* Only `!important` outranks an inline style; the JS keeps writing, the paint stays put, and Quarto's sticky recompute is otherwise untouched. Any future rule on this element's `top`/`max-height` must carry `!important` or it does nothing.
>
> Considered and not built (kept for the record): **B — the spine** (marks as ticks on one continuous hairline, ink above the reading position — the strongest position-teller; revisit if docked marks feel weak); **C — absent until asked** (loses passive position); **D — in-flow margin markers** (loses the at-a-glance map).

> **Status 2026-08-03 — partially built, deliberately not as specced below.** The rail now collapses on scroll-down to a column of fixed-length hairlines — one per section, active in ink and the rest gray — and restores the words on scroll-up, hover or focus (see the session log). That takes Layer 2's *idea* — a rail with a quiet resting state and a text state — while refusing its **form**: no horizontal dashes, no accent tick, no spring, no off-canvas toggle. Those are the parts that are recognisably Rauno's rather than merely good, and the site's cohesion rule (few materials used often) cannot pay for a shape used in exactly one place. Every section now carries its own mark (corrected 2026-08-03 — a single mark had nothing to be measured against), which is Layer 2's per-section rail arrived at from the other direction. What stays open from the spec below is the contextual expansion of sub-sections, the spring on hover, and the off-canvas toggle with its `S` shortcut.

Original measured spec from the dissection, kept for reference:
- Rest state: column of **1px × 32px horizontal ticks**, 8px apart, vertically centered in the left rail. Tick shade encodes hierarchy (ink = section heads, `--gray-800`-ish = subsections/filler). Zero text visible.
- **Active section: the one accent-colored tick, stretched to 50px** — the entire "you are here" UI. ⚠ Requires the accent-color decision first.
- Hover anywhere on rail → all labels fade in at once (100ms ease-out). Hover an item → its tick springs 32→50px (spring: stiffness 240, damping 25) and takes the accent; label rides the stretch.
- Contextual depth: only the active section's subsections expand inline into the rail.
- Invisible enlarged hit areas (~14px) around each 1px tick; full `:focus-visible` support.
- Narrow viewports (<~1400px): rail slides off-canvas (350ms, `cubic-bezier(0.23, 0.88, 0.26, 0.92)`), toggle button + `S` keyboard shortcut; opened-as-menu shows labels instantly.
- Implementation route: custom JS that reads Quarto's generated `#TOC` nav and rebuilds it as the rail (keep Quarto's heading data, replace the presentation). Scroll-spy via IntersectionObserver, not scroll handlers.

### Layer 3 — Whimsy (pick at most two, ma5a's fencing rules binding) — **PAUSED 2026-08-03**
**→ Full candidate designs, variants, live sketches, and the per-surface allocation proposal now live in [whimsy.md](whimsy.md) (2026-07-10).**

> **Status 2026-08-03 — paused, nothing cancelled.** The panda troupe was built (five choreography phases, then the chaos economy, then a deterministic engine port) and **taken off the homepage** on Ameya's diagnosis: *"two incomplete pieces still make an incomplete piece."* Severity buys whimsy, and severity that hasn't finished paying for itself can't buy anything. **Re-entry condition: the minimal site reads as finished with the hero empty.** What survives live: **Ask Niche** on the induction post (post-local, restyled into the reset's language 2026-08-05) and the favicon (a static cel — art, not motion). What is off: the troupe. What moved out of this repo: the engine and its trainer, to [Ameya-bit/panda-engine](https://github.com/Ameya-bit/panda-engine) (2026-08-02) — see [panda-engine.md](panda-engine.md).

**2026-07-22 amendment — two registers, one author.** The "whimsy speaks the research language" rule is amended: **homepage whimsy speaks the author's *identity*** (the panda troupe — Ameya's cross-platform avatar mark, durable across research pivots); **per-post whimsy speaks that post's *topic***. The site-wide Niche mascot is retired (Ask Niche stays post-local on the induction post). Fencing amendment: ma5a's real rule is *geographic* fencing, not user-causation — one ambient autonomous **system** is permitted inside the hero fence (idle-static rules stand everywhere else; article pages remain still air).

Candidates, all drawn from Ameya's own repos so the decoration demonstrates the research:
1. **Induction-head cursor trail** (from QuotesByNiche's B5H0): a trail that *replays your past trajectory* — `[A][B]…[A]→[B]`, copying what followed last time — rather than merely lagging. Desktop only, ink-colored, ephemeral.
2. **Photon random-walk** (from mc-radiative-transfer): scattering particles as a homepage-hero-only ambient effect.
3. **B5H0 Nietzsche quotes** (from the Niche char-level GPT): an occasional bad Nietzsche quote as a footer/404 easter egg; B5H0 as the site's named mascot.

ma5a's restraint rules (enforce in code, not vibes):
- Fence ambient motion geographically (`scrollY < innerHeight`-style predicates); article body = still air; ambient elements behind content (`z-index: -1`, `pointer-events: none`).
- Anything above content must be user-caused, ephemeral, self-deleting (≤500ms lifetime, sampled events).
- One autonomous animation per screen max; idle = fully static within ~1s.
- One voice: same ink palette, one mascot, whimsy demonstrates what the site is about.
- ~0 bytes: vanilla JS/CSS, transform/opacity only, gated behind `prefers-reduced-motion`, semantic interactive elements.

### Layer 4 — Page transitions — **BUILT 2026-08-02, ahead of Layers 2–3**
~~Decide after Layers 2–3 land — the base may feel complete without it.~~ Brought forward instead: two lines, fully progressive, no JS. `@view-transition { navigation: auto }` plus `view-transition-name: masthead-name` on `.navbar-brand`, so the name holds still while the page changes under it, and an explicit `::view-transition-*{ animation: none !important }` under `prefers-reduced-motion` — the UA-generated animation is not covered by the query on its own. See Layer 1b.

The original candidate, for the record: a View Transitions morph on index→post only (the cobosrl/antfu fluidity), everything else instant. What shipped is broader — every navigation — because the shared element is the masthead name, which is on every page.

### Layer 5 — Content restructuring (ongoing, per-post)
The 646px/17px system wants Emil's rhythm: 1–3 sentence paragraphs, h2 every 4–8 paragraphs, figures/math as breakout moments.

> **STANDING RULE (2026-08-03; re-measured under Nunito 2026-08-05) — section headings fit on ONE line in the TOC rail.** Budget is **~25 characters, with a floor of 20**. Measured against the live rail rather than derived: the label box is **184px**, and Nunito at `--text-apparatus` (14.87px) runs **7.4–8.0px per character** on ordinary sentence case and **9.2px** on wide letters. That is 25 characters of sentence case, 26 of title case, and only **20 for a heading full of m/w/M/W** — so the number is a budget, not a guarantee, and the tripwire below is what actually enforces it. *(The pre-Nunito version of this note quoted 188–198px and Inter's ~7px/character; both were inherited from before the typeface changed and both were slightly wrong.)* This is a *content* rule with a layout consequence, which is why it lives here and not in the CSS notes: each mark in the rail sits beside its own label, so a heading that wraps pushes every mark below it down and the rail's spacing silently starts encoding word-wrap instead of position. **Prefer the method or the subject alone; put the gloss in the section's first sentence, where it reads better anyway.** The IOI post's headings were `Method: gloss` pairs (`Activation patching: what's necessary, and when`) and every one of those glosses was already restated, more precisely, in the sentence directly beneath it — so dropping them cost nothing and the rail went from 26/44/63/44/44px spacing to a uniform 25.484px.
>
> Enforced by a tripwire, not by trust: the rail sets `white-space: nowrap; text-overflow: ellipsis`. **An ellipsis appearing in the rail is a bug report about the heading — shorten the heading, do not widen the rail.** Same pattern as the two-category cap on the filing row: where a layout guarantee depends on how much text a future post declares, put the guarantee in CSS.

**IOI post: DONE 2026-08-03. Induction-head post: DONE 2026-08-05. Pulse-profile post: born inside the system** — written 2026-08-04, after the standing rule, and it needed no pass: 4-sentence maximum, headings 13–24 characters, every one inside the rail budget. Its figures joined 2026-08-06. That is the first evidence the closing instruction below actually holds. The induction-head pass was done under Ameya's constraint that none of his prose be rewritten — the 9/7/7/6-sentence paragraphs were split at sentence boundaries, and the two figure captions that carried argument had that argument *moved*, verbatim, into the prose beside the table it belonged to. Worth noting for the next post: splitting alone took the post to a 4-sentence maximum, which says most of what a Layer 5 pass buys is structural rather than editorial. Write new posts in this rhythm from the start.

**What the IOI pass established, for the induction post to follow:**

1. **Captions are not where the argument goes.** The rule was already written into Layer 1b — *nothing that carries argument may use the apparatus tier* — and the IOI post was violating it in every figure. Its captions ran 288–656 characters and carried the self-repair explanation, the single-prompt-instability finding, and the whole BOS caveat, all at 14px in gray behind the paragraphs they were explaining. Now 91–175 characters, purely descriptive; the arguments moved up into 17px prose. **Mean caption length fell 66%.** The induction post's quote-grammar captions need the same treatment.
2. **Short paragraphs are a load-bearing change, not a cosmetic one.** Splitting 4–8 sentence paragraphs into 1–3 exposed a CSS bug that had been live on both posts (see the `.epistemic` note below). Expect the rewrite to surface things.
3. **Figures get regenerated, not just recaptioned.** `figures.mplstyle` existed but had never been applied to anything. Applying it is what makes a post's figures read as one set — and it is much cheaper than it sounds if the numbers are cached (below).
4. **The `compute.py` / `plot.py` split.** `compute.py` runs the model once and caches every number to a 6kb `_data.npz`; `plot.py` reads the cache and needs only numpy + matplotlib. The visual language of a whole post can then be reworked in seconds instead of a 2,000-forward-pass CPU rerun, and the figures stay reproducible by anyone without the model. This is the pattern for every future post.

## Open decisions
1. ~~**Accent color**~~ — **resolved 2026-08-02: `#d55e00` marks / `#b04d00` text**, taken from the vermillion the figures already use for "this is the finding." Interaction-only spending rule. Layer 2 is unblocked. See Layer 1b.
2. ~~**Font pairing**~~ — **resolved 2026-08-03: Nunito alone, committed.** Ameya's call: *"I like how it looks, and it adds a little character where Inter doesn't."* That closes the serif question too — no second family, period. The second *role* stays filled by mono (numerals + filing), which is a role, not a face. The consequence to design by: every display-scale decision from here must make Nunito's rounded character an asset (weight, scale, spacing) rather than reaching for a new material.
3. **Whimsy pick** — resolved 2026-07-22 (panda troupe on the hero + Ask Niche post-local), then **overtaken 2026-08-03: the troupe is off the homepage and Layer 3 is paused.** Ask Niche remains live on the induction post. What is open is no longer *which* whimsy but *whether the base is finished enough to re-admit it* — the re-entry condition in Layer 3.
4. ~~**Layer 4 yes/no**~~ — **resolved 2026-08-02: yes, brought forward.** Two lines, fully progressive, no JS. Shipped in Layer 1b rather than waiting on 2–3.
5. ~~**Homepage share card**~~ — **resolved 2026-08-06: the cover, recomposed.** The candidate on the table was a rendered still of the panda stage; what shipped instead is the hero's own material set for a 1200×630 frame (`tools/og-card/build.py` → `assets/og-card.png`). Two constraints closed it between them: no AI-generated art, and — Ameya's rejection of the hero instrument — no decontextualised figure either, on a site where a plot without its argument is decoration. That leaves the cover, which is the one image the site already owns. Generated rather than drawn: the script reads the ramp and the type stack out of `styles.scss` and renders with the committed Nunito, so the card cannot drift into being a second design. Re-run it when the hero copy or the palette changes.
6. **The identity corner's second register** — the hero states what Ameya *is* (present tense, durable to 2028). Talk, poster, manuscript-in-prep and the MC REU are what he has *done*, and both proposals for putting them top-right failed on the same axis: a credits row piles as projects accumulate, and a titles stack needs semi-permanent titles the corner does not have (the durable one — Application Specialist, Nittany AI — is off-thesis for an interpretability site; the on-message one is a finished summer program). The corner stays as it is. Standing proposal, not yet built: **output attaches to the post that produced it**, as a second filing row under the existing date · categories line, so it can never pile anywhere and lands where a reader assessing that result is already looking. Open.

## Session log
- **2026-08-16 (the CV first screen and the stone paper — branch `stone-cv-hero`)**: Ameya's read of the homepage: the post pages are right, but the front page "doesn't stand out in the flood of AI generated websites" and is missing the "homey" feeling — it should state who he is, "like a CV". References: juancadile.com and katiemccurdy.com. The diagnosis both share: **neither is whimsical and neither has a mascot; what makes them feel like a person is that the first screen is full of facts about one.** Ours spent the viewport on a slogan, five links and a status line — forty words about the author. So the fix was not an object in the void but the void becoming content, and only then an object beside it. Two mockups (artifact "The CV First Screen", https://claude.ai/code/artifact/2b472fb7-2788-4bdd-9c3b-575a680b4938); Ameya chose B, the plate, on stone. **This overturns the 2026-08-06 verdict that "the middle was never a hole".** That verdict fixed the composition; it did not fix the anonymity.
  - **Stone.** The paper moves from `#fdfdfc` to `#eeece6` and the whole Sand ramp is re-derived one notch off white against it (tokens in styles.scss; tertiary 5.14:1, secondary 6.4:1, `--accent-ink` 4.55:1 — text-safe with no headroom). The same values now live in figures.mplstyle (which also gains `axes.facecolor` — matplotlib's default is pure white, invisible on the old page and a white panel on this one), the three plot.py palettes, the IOI lens's PAGE, the OG card and the colophon swatches; all figures/webp regenerated. Ameya's word for it: "a more neutral color instead of white". Rejected on the way: the AI-default cream `#f4f1ea` + serif look; stone is grayer, and Nunito stays.
  - **The middle.** Headline down to ~2.65rem sharing its row with the identity corner (stacking the corner over the headline cost 100px the first screen did not have); then a two-column middle — **Now** (first person, the one block meant to be rewritten by hand; the status line stays the machine-checked freshness claim) and **Selected work** (three rows, title + year + dek, hairlines like the listings) on the left, the **plate** on the right; the foot unchanged. Fits one screen at 1440×900 (frame bottom = fold, measured 900.0px); stacks under 800px.
  - **The plate — "a head reads this page."** The headline set as characters with a head of the induction post's model (Niche) reading it, attention as arcs. **Real weights**: `tools/hero-plate/compute.py` runs Niche once over exactly the headline string and bakes B5H0 (the post's copying head) and B0H1 (the model's cleanest previous-token head, mean 0.95 on the sub-diagonal over this string) into `assets/site/hero-plate-data.html`, sparse (top-4 ≥ 0.05, ~3 KB), inlined on every page. On this string B5H0's clearest move: at the second "-ented," it looks back at the end of the first (`d`/`,` at 26–27, weights 0.4–0.6) — the copying move; the long arcs to `L` at 0 are the attention sink, and the caption says so. Canvas 2D only, DPR-aware, arc anchors read from the `<i>` boxes; stops off-screen; under reduced motion the full field draws once. **The script refuses to draw if the baked string and the markup differ** — the chips hide and the plate is text + caption, the same as with JS off. Re-run the baker when the headline changes.
  - **Voxels stay parked** (Ameya: the induction voxel needs finishing before any voxel earns the hero; "height correlating to attention" is boring). The plate is deliberately *general* mech-interp imagery — arcs are the field's shared picture — pointed at something specific and true, which is the "evidence, not ornament" test in a form that could actually be built today. The panda mascot was proposed by Ameya and argued down: neither reference has one and it says nothing about him; it can come back somewhere low-rent (404, colophon) but not as the hero's answer.
  - **Same day, Ameya's cuts (commit after 84641f7):** the selected-work strip goes — "it just lists all writing, which is further down the page anyway"; the plate goes — "it looks cool, but it doesn't really mean anything outside of the context of the induction head post", **banked** for a latest-work role (`tools/hero-plate/compute.py` and `assets/site/hero-plate.html` stay in the tree unwired; markup, CSS and the _quarto.yml wiring are in 84641f7); the hero content comes back DOWN — headline at its display size again, low in the frame, the Now paragraph directly under it, foot on the fold. **The Now is his own words now**, one fact-check applied: the global-workspace paper is Gurnee et al. 2026 ("Verbalizable Representations Form a Global Workspace in Language Models"), not Elhage — his own jacobian-lens `plan.md` names it — and the "early layer degradation" is that paper's noisy early-layer J-lens readouts (his Q2). What survived of the CV first screen: stone, and the Now.
- **2026-08-06 (the finishing set — two pages, a ceiling, and a card)**: Five decisions taken as one pass, scoped so the site can be left alone for a few months. The organising finding, which came out of Ameya rejecting both proposals for the hero's empty middle: **the hero's void was never a hole, it was a shortage of weight in the corners.** Two of the four corners carried anything; the fix was not an object in the centre but a third thing worth filing. Nothing was invented to achieve it.
  - **The status stamp got a ceiling.** The line was reported as broken and was not: the API says `jacobian-lens-from-scratch` was genuinely last pushed 2026-07-29, and the recent commits had gone to the site repo and `panda-engine`. The wiring was right and the fact was stale — which is the actual problem, because **a freshness reading is an asset while the work is fresh and a liability the moment it is not.** Past 21 days the stamp and the cursor no longer appear and the line renders as the plain question, the same path as JS off. 21 is where the old wording ran out of days and began rounding into weeks: the line's honest vocabulary and its useful lifetime are the same interval, so the rounding branches were deleted rather than left unreachable. This matters most while nobody is watching, which is precisely when it used to be wrong.
  - **/instruments/ — the code got an address.** Five public repos, each row stating what it *proves* rather than what it is, and every row that has a post linking back to it. That last part is the whole reason the page can exist: a figures gallery was on the table and was **dropped** on Ameya's objection that plots without their arguments are hard to read, which is correct and is exactly what a gallery is. Its rows reuse the homepage listing's grid — extracted to a `listing-row` mixin rather than typed twice — with three deliberate differences, all consequences of one fact: a listing row is ONE link and an instrument row is two. No hover pill (it would promise a single destination that does not exist), the name keeps its underline, and the post link sits under the claim in the filing register.
  - ⚠ **The first build of those rows made the repo name the only link to the repo, and the page read as though the code were missing from it** (Ameya, same day). The reasoning had been that a repo name is self-evidently a repo; it is not. **An underline says "link". It does not say "GitHub"** — and on the one page whose entire job is to point at the code, the destination a reader came for was the one thing not named. The name is plain text now (it is the row's *title*), and Repository and the post sit together underneath in the hero's links grammar, where two links side by side say without a word that there are two. Worth generalising: a link whose target is only inferable from its label is a link that has to be *read* to be found, and nothing in a scan gets read.
  - **Row spacing went from the pill's 12px to 40px a side.** At the listing's inset the rows crowded (Ameya). A hairline reads as a separator only when there is enough air on both sides for the gap to belong to the *pair* — otherwise it reads as belonging to whichever row is louder. 80px total is the h2's 72px section rhythm arrived at symmetrically, and symmetric is right here because these are equal items in a set rather than headings introducing what follows.
  - **The ledger's third row, and the composition it closed.** `Instruments 05` is the first row that leaves the homepage and the first whose count the script cannot check — the page is hand-written, not a listing, so the row carries no `data-counts` and is skipped by construction rather than by accident. The rows on that page are the source of truth.
  - **/colophon/ — the craft became visible.** Organised on the five axes the stylesheet's own header comment uses (type, colour, measure, motion, machinery) so the page and the file it describes cannot fall out of step. It is the one page allowed to show the site's materials as **specimens** rather than use them: Nunito at display scale, the twelve-step ramp as chips with the 500→800 gap left visible because renumbering it to look full would be a lie. Every claim on it was already true and none of it was visible — the KaTeX pin, the TOML résumé, the compute/plot split, the icon tripwire. Reached from the revived `.site-colophon` whisper, a rule written for a panda credit, unused since 2026-08-03, and shaped exactly right for this: the ledger files *work*, and a page about how the site is built is the site talking about itself.
  - ⚠ **The spec labels are NOT mono, against every instinct the genre has.** A spec sheet wants a mono label column and the first draft had one. The standing rule outranks the genre — mono is the numbering system and nothing else — and a column of mono *words* here would have been the same "second voice" failure the apparatus tier was pulled back from on 2026-08-02. The labels take the tier's other half instead: uppercase, tracked, gray, Nunito.
  - ⚠ **Pandoc's tight definition lists put text directly in `dd`, with no `p`** — so every inline code span on the colophon fell through the `p code, li code, td code` rule to Quarto's default and rendered in a purple that exists nowhere in this palette. Caught by measuring the computed colour, not by looking. On a page that spends four rows explaining that the site stays monochrome until you interact with it, that was the loudest colour on screen. `dd` is now in the selector list.
  - ⚠ **`website: image:` written bare is resolved per page, not once for the site.** The index got `/assets/og-card.png` and every other page got `<its own directory>/assets/og-card.png` — `/about/assets/…`, `/colophon/assets/…`, `/resume/assets/…` — three live 404s pointing at a file that exists, in a tag no crawler reports back and no human ever sees. A leading slash fixes all of it. Same species as the `data-repo` staleness: **the failures that survive are the ones nothing in the loop is built to notice**, which is the argument for checking the generated `<head>` rather than the rendered page.
  - **The notes got cards for free.** They declare no `image:` of their own and were text-only shares exactly as the index was; the site-wide default now covers them. Both notes and both new pages share one card, which is correct — they are the site, not a result.
  - **Verified rather than assumed**: zero axe violations across the homepage, both new pages, /about/, a post and /resume/; no horizontal overflow and no console errors at any of 375/768/1024/1440/2560. The colophon *asserts* the audit result in one of its own rows, so the audit had to be re-run for the assertion to stay true.

- **2026-08-05 (the documentation audit — the spec catches up with the site)**: No behaviour changed; eight files did. The finding that organises the rest: **this document's top half had become a description of a site that no longer exists, while its bottom half described the real one.** Layer 1's "as-built conventions" still specified flat 16px prose, a 26px paragraph gap, 32px figure margins, `--gray-1000: #82827c`, "no accent color exists yet", a colophon that came out with the pandas, and a TOC "to be replaced" by a rail built two days earlier — every one of them corrected *elsewhere in this file*, in Layer 1b or the log. **A living document acquires stale sections exactly where it is most authoritative**, because the amendment gets written where the work happened, not where the rule was stated. Superseded numbers are now struck inline with the current value beside them, rather than rewritten away: the history is the point of this file.
  - **Where the layers stand is now stated at the top of "Next layers"** — 2 built, 3 paused, 4 built early, 5 running — because the heading said "next" for three things that had already happened.
  - **The panda docs said they were live.** `panda-chaos.md` opened with "**is the homepage default**" and linked to three files that left this repo on 2026-08-02 (`panda-policy-net.md`, `panda-engine-map.md`, `trainer/README.md` — all dead links); `panda-choreography.md` said "built directly into the live site"; `whimsy.md` said "nothing shipped to the site yet" while Ask Niche was live on a post and the troupe had shipped *and* been withdrawn. All four now open with where the work actually lives, and the engine README says plainly that nothing loads it.
  - **`styles.scss`'s header was the most misleading block in the repo** — first thing anyone reads, and its four claims (16px, 644px, no accent, no motion) were three-quarters false. Rewritten as an as-built summary that names the fluid root, the apparatus tier, the accent's interaction-only rule, and the three motions that do ship.
  - **`.gitignore` was guarding a directory that no longer exists.** Twenty lines of rules for `/trainer/` — corpora, parity fixtures, the Python env — plus a comment asserting that the exported weights *are* committed at `assets/pandas/engine/policy/`, which the engine README says the site deliberately does not ship. The rules left with the trainer; the one rule about a directory that is actually here stayed.
  - **Two smaller drifts:** `_quarto.yml` said "both posts" declare an `image:` (there are three) and that the favicon is "the same drawing as the troupe on the homepage" (there is no troupe on the homepage).
  - **⚠ Unresolved, and left alone deliberately: the entry below is dated 2026-08-06, but all nine of its commits landed 2026-08-05** — as did every commit in the three sessions before it. Either the log's clock or git's is off by a day. Fixing it is a one-character edit; guessing which one is wrong is not the auditor's call.
- **2026-08-06 (the last post joins the figure system, and the icons go)**: Three merges, all live. The session opened by shipping the previous one — a full session of finished work was sitting uncommitted on `main`, which is the same diagnosis that started the CI session and worth naming as a recurring failure: *work that renders locally is not shipped, and the gap is invisible from inside the editor.* Nine commits, split by feature, verified byte-identical to the pre-split tree before merging.
  - **The pulse-profile post gets the `compute.py` / `plot.py` split**, and it is now the last post inside the system. Its five figures came straight from the manuscript pipeline at **1980px** — sized for a Springer text block, not this column — so they downscaled at 0.43x against the other posts' 0.66x and their labels rendered visibly smaller on the same page. Now 1280px, **1.5 MB → 0.46 MB PNG / 0.17 MB WebP**.
  - **The split is asymmetric here, on purpose.** Scalars and sweeps are *read* from the research repo's committed npz evidence base, because rerunning them is hours of Monte Carlo and the archive is what the manuscript quotes; the four pulse profiles are *recomputed*, because they are 0.02s each and a curve is exactly the thing that should not be transcribed by hand. **`compute.py` checks the cache against the post's own prose and refuses to write if a number has drifted** — fifteen claims, all matching. That check is the part worth copying to the next post: a cache that silently disagrees with the paragraph beside it is worse than no cache.
  - **The colour system was rebuilt, and this is the transferable finding.** The manuscript figures encode Riley = blue, Miller = vermillion — hue carrying *whose fit* a curve is. Correct for a journal, where the two teams are the subject under comparison. Wrong here, because the post is not about the two teams; it is about one error and where it hides. **Hue spent on authorship is hue unavailable for the argument**, and it costs the accent rule its meaning: if vermillion is "Miller," vermillion cannot also be "this is the finding." Colour now encodes the claim — isotropic gray dashed, the honest model ink, the bias between them accent — and Riley/Miller are carried by marker shape and a direct label, which the paper's own design contract already said it preferred. The payoff: in fig4 the accent lands entirely on the panel whose number does **not** move, so the figure states the post's conclusion in colour.
  - **One element removed rather than restyled.** The manuscript drew a white contour on the flux floor, but ΔPF is zero exactly where the flux floor is — so that contour traces the colormap's own light-to-dark edge, a line drawn on top of the boundary it describes. The claim is that the *analytic* boundary lands on the numerical transition, and one line against the colour change shows that; two lines hide it. A first pass kept it as a soft pale band and it was invisible for the same reason.
  - **⚠ THE ONE-WIDTH RULE HAD A HOLE, AND IT WAS THE INTERPRETER.** On macOS matplotlib's default backend is `macosx`, which carries a 2x device-pixel ratio that `savefig` honours: the 8.0in canvas pinned in `figures.mplstyle` writes **2560px, not 1280**. Nothing warns and the figure looks correct, so a committed `plot.py` did not determine its own output — the width depended on which python ran it. The induction figures are 1280px only because they happened to run under a venv that had already fallen back to Agg. `mpl.use("Agg")` before the pyplot import in both scripts, and the rule now sits in `figures.mplstyle` beside the width it protects. Verified by rerunning induction with the guard: all three PNGs byte-identical, so it changes nothing except what a future run can no longer get wrong.
  - **The Bootstrap icon set is gone, and the log's number was wrong.** Previous entries quoted "512 KB of compiled CSS and a 280 KB icon font" — those are *raw* file sizes, and Pages serves compressed. Measured: the core CSS is **70 KB gzipped**, the icon stylesheet **14 KB gzipped**, and **the 176 KB font was never downloaded by anyone** — a browser fetches a webfont only when a rendered element matches a rule using it, and there are **zero icon classes across all nine pages**. So the font was artifact weight, not reader weight, and the stylesheet was the real cost: render-blocking, on every page, defining ~2,000 glyphs nothing asks for. `tools/site/drop-icons.py` unlinks it and removes both files — 14 KB gzipped off every page, 273 KB off the artifact.
  - **The tripwire is the point, not the deletion.** The step re-proves the set is unused on every render and **exits non-zero** if any page starts using an icon, rather than silently serving a missing glyph. Detection is tokenised, not substring-matched, because Bootstrap matches whole class tokens — a substring test reports usage for `bibliography`. Verified both directions: fires on an injected `<i class="bi bi-github">`, quiet on bibliography / big-note / combi-x / a `bi-` inside an href. Same pattern as the rail's ellipsis and the filing row's two-category cap.
  - **Still open, and still the site's largest asset: 511 KB of core Bootstrap CSS (70 KB gzipped), of which 101 of 2,784 declared class selectors are used — 3.6%.** Deliberately not touched. It is load-bearing in a way the icons were not: `styles.scss` is written *as a Bootstrap theme layer* (`scss:defaults` / `scss:rules`) and overrides the whole `$grid-*` family, which is what defines the reading column, the rail's band, and the margin band the figures outset into. A purge pass also cannot see the classes Quarto's JS adds at runtime — tabsets, tippy, headroom, code-copy — so it breaks things that only fail on interaction, which no build catches. That one wants a measured allowlist and a cross-browser pass, not a build step.
- **2026-08-05 (the induction post joins the system, and KaTeX comes in-house)**: Ameya's list, in his order. His constraint on the post was the one that shaped everything: **do not edit the paragraphs — split the existing language where it helps.** So not a word of his prose changed; what moved, moved whole.
  - **The induction post gets the `compute.py` / `plot.py` split** the IOI pass established as the pattern. `compute.py` runs the Niche model once and caches every number to a 9.6 KB `_data.npz` plus a `_data.json` of verdict rows; `plot.py` reads the cache and needs only numpy + matplotlib. The replay was checked against the post's own prose before anything was drawn: copying score **0.615**, next best **0.347**, the quote logit **+0.595**, `(` **+0.228** at rank 39/177, `)` **−0.339** and last of 177 — every number the text quotes, reproduced. The whole visual language of the post can now be reworked without loading a model.
  - **Three figures regenerated to `figures.mplstyle`**: one width (1280px, exactly), Nunito, the site's warm ramp, and **one accent per figure** — the old set spent blue, green, orange and vermillion on decoration across five figures. The set went **2.42 MB → 0.14 MB PNG / 0.05 MB WebP**. Two callouts were dropped rather than repositioned: the "the quote logit survives normalization" annotation was restating what the vermillion bar and the accent-ink value already said, on top of the data it was pointing at. **A label that repeats the colour is chartjunk with a sentence attached.**
  - **The two verdict tables stopped being pictures.** `fig4` was a 2210px PNG of a *table* — ten rows of prompts, characters, positions and weights — displayed at 646px, which is the case `figures.mplstyle` names as the reason the one-width rule exists. Text baked into an image is unselectable, unsearchable, silent to a screen reader, and illegible without clicking. They are markdown now, generated by `plot.py` so they cannot drift from the cache, and **they inherit the figure rules for free**: same 96px rhythm, same symmetric outset to 793px, same caption held back at reading measure. Nothing new was invented to hold them.
  - **The verdict column now says the word.** The PNG encoded four verdicts as bare colour dots against a legend — colour as the sole carrier of meaning, which is the one thing an accessibility rule is unambiguous about. The table says *induction* / *sink* / *previous-token* / *other*.
  - **Two traps worth keeping.** `@54` in a markdown cell is a **pandoc citation** — it shipped as `<span class="citation" data-cites="54">` before anyone looked; `\@` escapes it. And a bare `.verdict-table code` **loses the specificity fight** to the prose `#quarto-content main.content code` rule, so the first build rendered sixty bordered, filled chips inside one table. Both were caught by measuring the built page rather than the source.
  - **KaTeX is vendored and pinned** (`assets/katex/`, 0.18.1, js + css + the woff2 fonts, 600 KB in the repo of which a visitor fetches three files). The site had been loading `katex@latest` from a CDN — a floating version free to change the typesetting of every equation without a commit, and a third-party origin in the critical path of a page whose math renders client-side. `html-math-method` takes `url:` **with a trailing slash**; without it Quarto concatenates and asks for `assets/katexkatex.min.js`.
  - **The Firefox MathML complaint was ours, not KaTeX's.** Rendering all 77 expressions on the site through KaTeX in Node and checking every arity-constrained MathML element found exactly one offender, four times: `F_{\max}`. `\max` is an *operator*, so KaTeX emits `<mi>max</mi><mo>⁡</mo>` — the invisible function-application character — as a second child inside the `<msub>`, giving it three children where MathML allows two. The fix is the correct markup rather than a workaround: `F_{\mathrm{max}}` is what "F sub max" actually means, and it renders identically. **Zero bad-arity nodes across all three math posts now.**
  - **The article's gutter numerals are Nunito** (Ameya's call — they earn their keep, they just should not be mono). `tabular-nums` keeps the column true. See the amendment under Layer 1b for what this does to the mono rule.
  - **The rail's ~25-character budget survives Nunito — and is now measured rather than derived.** The old note quoted "188–198px of label width, Inter ~7px/character," both inherited from before the typeface change. Measured against the live rail: the label box is **184px**, and Nunito at 14.87px runs **7.4–8.0px per character** on ordinary sentence case, **9.2** on wide letters. So: 25 characters in sentence case, 26 in title case, and **as few as 20 if the heading is full of m/w/M/W**. The standing rule keeps its number and gains a floor.
  - **The résumé says "system."** Flagged 2026-08-04, ported faithfully until Ameya ruled. Both surfaces regenerate from `resume.toml`, so the page and the PDF changed together — which does retire the "pdftotext is byte-identical to the hand-maintained original" check by one word, on purpose.
  - **Still open, and now the largest thing on the site:** 512 KB of compiled Bootstrap CSS and a 280 KB icon font for icons this site does not have (872 KB in `site_libs/bootstrap`). Images are no longer the page weight; the framework is. Explained to Ameya, not yet acted on.
- **2026-08-05 (later — the craft-debt pass: figures, Ask Niche, and an audit that found six things)**: Four pieces of work, in Ameya's order. Everything below is live.
  - **FIGURES (perf).** Every rendered figure was `<img src class>` and nothing else — no dimensions, so the browser could not reserve the box and each figure shoved the paragraphs below it down; no load policy, so five plots competed with the text for first paint. Fixed in two halves. `tools/site/figures.py` writes a **lossless WebP** delivery copy of every figure (2.42 MB → 0.99 MB, −60%, pixels identical — a lossy codec deciding which pixels of an error bar matter is not a trade this site makes quietly). The PNG stays committed as the archival original AND as the Open Graph target, because social scrapers are the one audience with spotty WebP support and a broken share card is what `open-graph: true` exists to prevent. `tools/site/img-attrs.py` (post-render) then reads width/height **out of the image files** and writes them into the HTML — deliberately not by hand beside sixteen figures, which would be sixteen more numbers free to drift, the same failure the lens figure's generated data exists to prevent. First figure of a page stays eager (the title card can put it near the fold; lazy-loading the LCP element is self-inflicted), the rest lazy. Measured after: CLS **0.005–0.006** against a 0.1 budget, all sixteen aspect ratios exact.
  - **ASK NICHE, restyled.** The widget was built 2026-07-10, three weeks before the reset, and was the one surface that never got the redesign. It broke six committed rules: Inter in five places; mono on *words*; 6/12/16px radii; 32px drop shadows; its own gray focus outline; and no accent at all. All corrected against styles.scss. **The affordance changed object** — it was a floating rounded square holding a lone "N̈" glyph, a chat FAB, which is product furniture on a site with no icons anywhere. It is now the hero links row's twin (filing register, hairline baseline, accent on hover) reading "ask Niche". The attention strip keeps its colour (figures are where this site spends colour at rest) but now **reads its ink from `--gray-1200` at draw time** instead of a hardcoded `rgba(33,32,28)`.
  - **THE AUDIT** — Chromium/Firefox/WebKit × 320/375/768/1024/1440 × 7 pages, axe-core. **Every named cross-browser risk was a non-issue**: `align-content` on block containers (the title card), `text-wrap: balance`, `:has()`, `svh` all supported in all three. It found six real defects instead, all now fixed; the site reports **zero axe violations and zero horizontal overflow at any width in any engine**.
  - **The 320px overflow was a TABLE, and my first diagnosis was wrong.** The offending bounding boxes were the hidden MathML, so I blamed it — `display:none` on it changed scrollWidth by exactly zero. Removing elements one at a time found a bare `<table>` with a 303px floor and no responsive wrapper. Worth remembering as method: *a bounding box past the viewport is not evidence of overflow; only removing the element and re-measuring is.*
  - **Fixing the overflow created a serious a11y violation**, which is the pattern worth noting: `.katex-display` (and now `.table-scroll`) scroll by mouse and could not be reached by keyboard. `assets/site/scroll-boxes.html` grants `tabindex` **only to boxes actually scrolling** and withdraws it when they stop — twenty-five equations must not become twenty-five tab stops to serve the two that overflow. Scrollability is a function of the viewport, so it cannot be baked in at build time.
  - **Gotcha that shipped a broken script and the audit caught: Quarto renders math CLIENT-SIDE.** The first version of scroll-boxes cached its node list at end-of-body, when not one `.katex-display` exists yet, and returned early having done nothing. Anything touching rendered math must run at DOMContentLoaded/load/fonts.ready, never once at parse.
  - **Landmarks.** Three `<nav>`s, two unnamed: the masthead gets a name, `#quarto-sidebar` gets `role="presentation"` (a bare wrapper whose only child is the TOC should never have been a landmark). And the duplicate banner was **ours** — Ask Niche appends to `<body>`, so its `<header>` was a top-level banner; it is a `<div>` now.
  - **The résumé was spending the rail's one announcement** (found the session before, fixed here): it has real h2s, therefore a rail, and sits one click from the hero, so it was routinely the first railed page of a session — and the reader's first actual *post* then got silence. It refuses before claiming the token, on the same `.cv-filing` discriminator the title card uses.
  - **`listings.json` silenced honestly**: Quarto writes the manifest to `/listings.json` then fetches it page-relative, 404ing on every article. `tools/site/listings-shim.py` copies the *real* manifest to the address the script asks for — site-absolute paths inside, so the copy is as correct as the original. Not a stub.
  - **A 404 page, which the site never had.** Title-card grammar, filing-register link row. **Every link absolute** — GitHub serves this one file for a missing path at any depth, so a relative href would resolve against whatever was mistyped and break a second time.
  - **Ameya's calls this session:** motion approved (entrance, seam, rail intro); **do not retrofit interactive figures into existing posts** — forward-going only, the IOI lens stays the one exception; induction-head's figures DO get regenerated to `figures.mplstyle`; **the panda post-mortem is a NOTE, not a post** (source: the docs in `/Users/ameya/mech_interp/panda-engine`; narrative = rules expert → why it didn't work → what came next); inventory tier paused.
  - **Left standing, both upstream, both worth a decision:** Firefox logs invalid MathML from KaTeX (`Incorrect number of children for <msub/>`) on the two math-heavy posts — all 25 rendered nodes measure correctly, so it is the *accessibility* layer, not the visual one. And math renders client-side from an **unpinned `katex@latest` CDN script** — a render-cost question and a supply-chain question at once. Also noted while measuring: images are no longer the dominant page weight; **512 KB of compiled Bootstrap CSS and 280 KB of unused Bootstrap icon font** now are. *(Both KaTeX items closed the same day — see the last entry; the MathML turned out not to be upstream at all. Bootstrap is still open.)*
- **2026-08-05 (the site ships — CI, the branch model, and the split commit)**: The finished base and a full session of unshipped work went live. Diagnosis that started it: `gh-pages` was last built 2026-07-26, `design-base` was 12 commits ahead of `main`, and an entire session (About, Notes, the physics post, the live status line, the rail introduction, the IOI lens) was uncommitted — **the award-level base had never been seen by anyone**. Everything below is now at https://ameya-bit.github.io.
  - **The deploy model, decided with Ameya**: *main is the live site; branch to edit; the merge to main IS the publish.* Encoded in `.github/workflows/publish.yml` as one workflow, two jobs — `build` runs on every branch push and every PR (so a broken render is caught before the merge, not after), `deploy` is gated on `github.ref == 'refs/heads/main'`. Added to his proposal: **build on branches too**, because this project has a Python pre-render hook and a pinned renderer, both of which can fail in ways a local render would not surface.
  - **Pages source flipped from the `gh-pages` branch to the Actions artifact** (`build_type: legacy` → `workflow`). GitHub Pages is still the host — what changed is only where it gets its files. Consequence worth keeping: **built HTML no longer enters git history**, and the build is ours (pinned Quarto 1.9.38, Python 3.12 for the résumé hook) rather than GitHub's generic builder, which could never have run `quarto render` at all — that limitation is exactly why the old flow required rendering locally and pushing output by hand. The `gh-pages` branch is left in place, dormant. No dark window: Pages serves the previous deployment until a new one replaces it.
  - **Quarto is pinned deliberately** (1.9.38, the local version). On a site whose output is this design-precise, a floating release changing emitted HTML underneath the CSS is a real failure mode. Bump the pin and re-render locally in the same commit.
  - **The session's work was split into five commits by feature, not dumped as one.** `styles.scss` had 357 new lines belonging to four separate features, so the diff was hunk-split (`scratchpad/hunks.py`) and `_quarto.yml`'s single contiguous block was rebuilt incrementally across three commits. Verified by shasum that the reassembled tree was byte-identical to the pre-split state. Worth repeating whenever a session's work spans features: the history is the only place the reasoning survives at commit granularity.
  - Actions bumped off the Node 20 runtime (checkout v7, setup-python v7, upload-pages-artifact v5, deploy-pages v5) so the deprecation annotation stops sitting on every run and hiding real warnings.
  - **Motion approved by Ameya at last** ([[verify-animations-static-only]] discharged for these three): the once-per-session hero entrance, the scroll-mapped seam settle, and the rail introduction all pass. The one report — "the TOC bar doesn't flash on first entry" — measured as *working as designed*: instrumented on the live site, the intro arms at load+600ms, reaches opacity 1, and holds 1600ms. Two causes for not seeing it, below.
  - **Finding — the résumé spends the rail's one announcement.** The intro's guards return early on a page with no rail (the homepage costs nothing), but `/resume/` has real h2s and therefore a rail, so visiting it first consumes the session token and the first *post* never announces. The announcement is being spent on the one page whose rail is least useful — a scan document. Fix pending Ameya's call.
  - **Also learned, and worth writing down: a hard refresh does NOT clear `sessionStorage`.** Only a new tab does. Every once-per-session beat on this site (hero entrance, rail intro) is untestable by reload, which makes them very easy to mis-report as broken.
- **2026-08-04 (the résumé adapter — one source, two renderers; the portfolio port begins)**: The first non-post content type arrives, chosen deliberately: the hero's RÉSUMÉ link was the one object that exited the design language (straight into Chrome's PDF viewer), and the résumé's entry grammar (dated, typed, linked items) is a dry run for the Projects index. Architecture per Ameya's call — *"fit the pdf to the qmd, not the qmd to the pdf"* — sharpened one notch: **neither document is the source; the content is.**
  - **`tools/resume/resume.toml`** is the single source of truth (TOML because tomllib is stdlib — zero new deps, the house rule). Both surfaces generate from it via **`tools/resume/build.py`**: `resume/_body.md` (the page body, regenerated by a Quarto **pre-render hook** on every render so the page can never drift) and, under `--pdf`, a filled copy of **`template.tex`** — the Harvard-OCS preamble vendored VERBATIM from `/Users/ameya/resume/Resume.tex`, design layer only, content region machine-filled — through latexmk into `assets/Ameya_Panchal_Resume.pdf`. The compute.py/plot.py split applied to a career: content computed once, presentation per surface.
  - **The PDF is the subset renderer, the page the superset.** Contact items carry `web = false` — the phone number ships in the PDF and never reaches the public page. Entries carry `url` (PDF's `\entryurl`) and optionally `web_url` — on the page the IOI and induction entries link to their own posts; the PDF keeps the canonical published URLs. Faces differ on purpose: Carlito is the PDF-standard's voice, Nunito the site's; the single-source guarantee is the *content* agreeing.
  - **Writing conventions translate per surface** (build.py): ` · ` ↔ `\sep`, — – ↔ `---`/`--`, `*em*` ↔ `\emph`/`<em>`, χ²(= n) → one math span, &%# escaped. Abbreviation spacing (`Prof.\ `, `al.\ `) handled.
  - **Verified: `pdftotext` of the generated PDF is BYTE-IDENTICAL to the hand-maintained original.** One page, letter. The adapter reproduces the artifact it replaces before it is allowed to evolve past it.
  - **`/resume/` page**: generated body under a `title: Résumé` header; filing line "Updated <month> · Download the PDF" in `--text-filing`; sections as real markdown h2s so the gutter numerals, TOC gauge rail (4 marks) and section counters arrive for free; entry rows speak the homepage listing's grammar — work left at prose weight, dates/locations right in apparatus, stacking at the listing's own 620px collapse. Hero RÉSUMÉ link now points at `resume/`.
  - **Two gotchas for the record**: (1) `str.replace` filled the template marker *everywhere it appeared* — including the header comment that named it; the first build compiled the comment. Never name the marker in a comment. (2) `pagetitle:` plus Quarto's automatic site-title suffix doubled the name in `<title>`; `title:` alone suffixes correctly.
  - **Flagged to Ameya, not fixed silently**: the Nittany bullet reads "the Docfinity systematic" in the original tex — looks like it wants "system"; ported faithfully pending his word.
  - Measured (Chrome): 6 entries, 4 rail marks, gutter numerals rendering, head/date baselines aligned, phone absent from the page, zero console errors, zero overflow at 375/1440, entrance attribute correctly absent on the page, both build modes idempotent.
- **2026-08-04 (later — the staircase, and the hero finds its words)**: A second artifact round pushed the framed layout through six iterations (A settled / B staircase / C catalogue cover / D nameplate / E bleed / F stage). **Ameya picked B, with C explicitly banked for later**: "the staircase is worth a try, and keep the catalog cover in mind for when we add everything else." Built:
  - **The copy is the site's function now, not biography.** Headline: **"LLM circuitry — implemented, documented, shown off."** — Ameya's own tricolon construction with one word swapped ("shown off" winks where his draft's "boasted" self-deprecated; the boast should not apologize). Biography moved to where designers put it: a **role line top-right** — "CS, physics & AI at Penn State. / Independent interpretability research." The old lede sentence is gone. The CURRENT EXPERIMENT filing line survives unchanged — a dated status line is allowed to name a specific goal; that is what filing is for. The headline is not.
  - **The staircase**: the tricolon's three beats step rightward — indents 0 / 5.882rem / 11.765rem (0/100/200px at calibration) on the **masks**, not the rising lines, so the entrance's vertical rise happens inside an already-placed step. Three beats, three steps: the list structure of the copy is drawn by the layout, and the editorial Z's diagonal is back inside the type block itself. Flattened to 0 below 800px (an indent would eat the narrow measure). `text-wrap` stays off the steps — each is one hand-set line.
  - **The cue/links collision (Ameya's catch on the built frame) is fixed by semantic corners**: the links moved UP into the identity corner under the role line (`.hero-ident`, contact belongs with identity), the status stands alone bottom-left, and **Writing owns bottom-right alone**. The bottom edge is one object per corner; status and cue share only the baseline, 373px apart.
  - **Entrance re-timed for seven beats**: headline steps 0/80/160, role 260, links 320, status 400, cue 460 — still 500ms each, done by 960ms, same three-way gating.
  - **Measured** (1440): steps at x = 174/274/374 exactly; identity corner flush right at the shell edge (ident and links both 1266); status/cue baselines equal at 874; foldGap 0; zero overflow at 375/1440; reduced-motion arms nothing; zero console errors; 7 rises at identity transform after play.
  - **Banked, not built:** **C — the catalogue cover** (bottom-right ledger: mono-numbered counts of Writing / Projects / Papers replacing the lone cue, headline narrowing to clear it). Adoption condition: real inventory — counts of 02/00/00 would undercut the boast. When repos and papers land, the ledger becomes the hero's navigation and the cue retires. Also noted from iteration F: every variant preserves the walkable middle band, and the fence-per-block geometry the engine already supports means **the framed hero is a better troupe floor than the old centered layout** — the panda re-entry condition (base reads finished bare) is unchanged, but the stage is ready.
- **2026-08-04 (the hero earns its screen — the six-findings review, applied)**: A from-scratch critique of the bare hero through the award lens, delivered as a redline artifact with every fix mocked in the site's own materials (sand, real subset Nunito, vermillion as the finding mark). The verdict: *the hero rents its screen* — a centered band of body-adjacent type in an unstructured field reads as the place the pandas used to be, not as intent. Ameya: "worth a try, apply them." All six findings are in, in the review's build order:
  1. **Scale.** The headline is a display object now: `clamp(2.35rem, 1rem + 3.9vw, 4.7rem)` — 73px at 1440, 89px capped at 2560 — line-height 1.05, tracking −0.025em. The old 2.75rem cap was a size chosen so the copy set as two even lines, which is a *paragraph* concern; at display scale Nunito's rounded terminals finally do the identity work the 2026-08-03 commitment demanded. The copy sets as four stacked lines; **`text-wrap: balance` is disabled on the headline phrases specifically** — balance evened the lines by splitting "computer science" mid-compound; greedy wrapping breaks at the comma. Semantics outrank symmetry in display type. If four lines ever reads as a wall, the lever is shorter copy, not smaller type.
  2. **The frame.** The viewport is a composed frame, not a centered band: lede answers top-right (24rem measure, right-aligned; **visual only — flex `order: -1` moves it while the DOM keeps headline → lede → filing, the reading order**), headline mass sits low, filing corner pins bottom-left, the Writing cue pins bottom-right *inside* the frame. The frame bottom IS the fold: measured foldGap 0/−1/+1px at 1440/2560/1024 (−13 at 375, reads as intent). Below 800px it collapses to a top stack with the foot pinned to the fold. This finishes what the editorial Z started and the panda fence had frozen: the composition is now for readers, not for walkable rects.
  3. **The entrance** (`assets/site/hero-head.html` + `hero-motion.html`, both new; wired in `_quarto.yml`). Five beats of masked lines rising into place — headline phrase 1 at 0ms, phrase 2 at 90, lede 240, filing 360, cue 420; 500ms each on the site curve, done by 920ms. Runs **once per session** (sessionStorage), never under reduced motion, never without JS — the head script only *sets* `data-hero-entrance`, the CSS only hides inside `.hero-screen`, and the after-body script removes a stray attribute on hero-less pages, so no fallback path can hide content. **This is deliberately outside the <300ms UI-motion rule and doesn't amend it**: that rule governs high-frequency interaction feedback; the entrance is once-per-session narrative motion with a nameable purpose (the reveal order teaches the reading order). Every later interaction stays instant.
  4. **The heartbeat.** A resting cursor blinks after the experiment question — 1.1s stepped, no fade; an instrument idling, not an animation performing. It is the one autonomous element the whimsy fence permits per screen, and it is **gray, not accent**: the Layer 1b amendment rules that a state persisting while the reader does nothing cannot be paid for in vermillion. Static at half-ink under reduced motion (it is information — "this line is live" — not decoration).
  5. **The seam.** The hero dims and settles back (−24px) as the reader leaves it, mapped to scroll over the first 55% of a viewport — **state, not animation**, the rail-fill grammar, so scrubbing back restores it exactly. Under reduced motion the settle (the part that moves) is dropped and the dim kept, like the scrollbar. `visibility: hidden` at full fade so invisible links can't take keyboard focus. The cue's old above-the-fold peek is gone; the cue inside the frame is the affordance now, and it hands off to the list it labels (a `visually-hidden` h2 keeps the section named for readers who can't see the cue).
  6. **The links row.** Joined the filing register: same size and voice as the status line (0.8125rem, 500, uppercase, tracked), no underline — a 1px `--gray-400` baseline rule instead — so the corner reads as one designed object. Hover spends the accent on rule and text together, instantly, per the interaction-only rule. Selector must carry `#quarto-content main.content` or the prose link rules re-underline them.
  - **Found while fitting the frame to the fold: Quarto emits an EMPTY `#title-block-header` on the homepage** (`pagetitle` only, no title), and the post-header rhythm gave it 64px padding + 116px margin — 180px of invisible furniture above the hero. Hidden via `body:has(.hero-screen)`. **This was the real cause of the long-standing "Writing cue starts ~2px below the fold" note**: the old 9.5rem masthead assumption was off by exactly this block.
  - **Measured** (Chrome via Playwright, served no-store; motion judged by Ameya, not headlessly): entrance attr pending→play→absent-on-repeat; all six rises at identity transform after play; reduced-motion arms nothing anywhere (attr null, transforms none, cursor static); seam opacity 1.00/0.70/0.39/0.00 at y = 0/150/300/495 with exact restore at 0; zero console errors on index and both posts (the posts' `listings.json` 404 is Quarto's own listing probe, pre-existing); zero horizontal overflow at 375/1024/1440/2560; post pages untouched (rail 6 marks, title visible, no attr leakage).
  - Not done, deliberately: no scroll-jacking, no parallax scenes, no ambient particles, no motion on the listing — the review's own restraint list. The pinned title-card variant of the seam (hero sticky while the list slides over) is the escalation if the settle reads weak.
- **2026-08-03 (the instrument grows to a gauge — equal gaps, half-viewport, centred, 2px)**: Four calls from Ameya, made looking at the built bar rail, that finish the object:
  - **Equal gaps between bars.** The bars had stayed glued to their label rows, so the gaps varied with bar length — but the lengths already say how big each section is, and even gaps are what make lengths legible (a bar chart needs a steady baseline). The bars are now the layout: each `li` IS its bar slot (`height: var(--mark-h)`), gaps a fixed `0.706rem`, and the **labels leave the flow entirely** — absolutely positioned, centred on their bars, revealed on hover as before. The one-line rule survives with a new consequence: a wrapped label would collide with its neighbours in the hover state.
  - **The column spans half the viewport and centres vertically.** Bar heights are each section's *share* of the document scaled so bars + gaps = 50svh exactly (floor 0.824rem so short sections stay visible and adjacent hover labels can't collide); the sticky box sits at `top: calc(50svh − var(--rail-h)/2)`. **This reverses the title-line docking, deliberately**: the earlier rejection of vertical centring was of a centred *text rail* — a list floating mid-air among top-anchored objects. A wordless gauge is a different object; instruments read from the middle of their range. `--rail-h` is written on `<html>`, not the sidebar, because quarto.js rewrites the sidebar's style attribute on scroll and would wipe it. Also found and zeroed: Quarto's ~15px padding on `#TOC`, which had the column 15px below true centre.
  - **Bars are 2px.** At half-viewport lengths a 1px hairline read as a rule; 2px is the least width that reads as an object with a fill inside it.
  - **The kicker is deleted outright** (was hover-revealed): a heading over a list that only exists mid-interaction is furniture.
  - Consequences recorded: nested h3 lists are `display: none` in the rail (a fixed-height bar slot can't hold them; if a post ever needs them that's a design round, not an unhiding); below 992px the off-canvas menu keeps plain visible labels, no bars.
  - **Measured (both posts, 1440×900):** rail exactly 450px = 50svh, centre offset −0.0px, every gap 12.0px, bars 2px — IOI 36/62/103/62/51/76, induction 17/84/66/103/99/21 (floors holding); fills correct at every probe; labels opacity 0 at rest, instant on hover; zero console errors.
- **2026-08-03 (the rail becomes an instrument — words on request, bars that fill)**: Three changes on Ameya's direction, and together they end the rail's life as a list:
  - **Labels exist only on hover/focus, ever.** No resting text, not even at the page top; the kicker goes with them. **This retires the entire scroll-direction system** — data-toc, the 24px hysteresis, the 280ms staggered collapse, the asymmetric timing table — because once the resting state never contains text, direction has nothing left to decide. The machinery's best finding survives as the only rule: *a request (hover, Tab) is answered instantly, never on a curve.* Both reveal directions are instant, so there is nothing left for reduced-motion to cover. (The direction system's full design is preserved in this log, 2026-08-03 "Layer 2's idea" entry, if it's ever wanted back.) Below 992px Quarto's off-canvas menu keeps visible labels — there the labels are the interface.
  - **The bars grew: 8–28px against a pitch widened by 0.5rem per item** (anchor box ~25.5px + 8.5px air ≈ 34px). With no resting labels the bars are the whole resting rail, and at 6–20px they were sized to sit beside text that is no longer there.
  - **The fill: ink tracks the reader through the document.** `--mark-fill` per bar — completed sections solid ink, the current one part-filled to the reader's depth, the rest gray. The reader line sits at 30% of viewport height, and the fill pins to 100% at the scroll floor (the last pixels of a document can never pass an interior line, so a finished read shows a fully inked rail). Implemented as a hard gradient stop on the pseudo-element (it can't hold children; the stop doubles as the you-are-here edge), written from the same rAF-coalesced passive listener that used to run the direction machine. **No transition, and that is the reduced-motion argument: the fill is state, not animation** — it tracks scroll exactly as the scrollbar thumb does. The active-label ink override for the mark is gone; the part-filled bar IS the active marker.
  - The rail now says, at rest, in zero words: what shape this document is, how much of it there is, which section you're in, how deep into it you are, and how much remains. Draft 4's fill idea from the round-2 exploration landed after all — composed onto the bars, as predicted.
  - **Measured (IOI, 1440):** bars 10/17/28/17/14/21px; at y=3000 fills read 100/47.6/0/0/0/0; at floor all 100; labels opacity 0 at rest (including page top) and 1 within 60ms of hover; zero console errors.
- **2026-08-03 (the marks become bars — the TOC question closes on draft 3)**: Two artifact rounds of from-scratch TOC concepts (round 1: scale line / corner ledger / paper-native; round 2: five drafts spanning informative↔minimal, after Ameya's read that A's *information* and C's *minimalism* were both right and "engineered/impressive" is shelved as a later global concern). **Ameya picked draft 3: even pitch, each mark's LENGTH is its section's length** — the rail is a six-bar chart of the document, still nothing but hairlines.
  - **The fixed-12px doctrine is overturned, and the history is the justification**: length was banned because the old `border-left` accidentally encoded word-wrap. The ban was on length encoding *nothing true*, not on length. Now it encodes the one thing worth saying, and the one-line-heading tripwire — which already guaranteed even pitch — is precisely what makes bars readable as a chart (bars need a steady baseline grid). The tripwire carries one more job.
  - **Implementation**: `assets/site/toc-rail.html` gains a second script — measures `section.level2` rendered heights, writes `--mark-h` per `#TOC > ul > li`; CSS turns that into mark height, centred on the label's first line (`calc(0.75rem - var(--mark-h, 12px) / 2)`). Fallback 12px = the old uniform rail wherever the script bails (count mismatch, no JS). Bounds in rem via the live root: 6px floor (a two-paragraph section still needs a hoverable mark), 20px ceiling (label pitch is ~25.5px; the tallest bar keeps ≥5.5px clearance). Re-measured on `load` (images settle heights) and `resize` (the fluid root changes every length).
  - **Measured**: IOI 7/12/20/12/10/15px against section heights 1125/1936/3224/1926/1577/2386; induction 6/16/13/20/19/6 (its two short sections sit on the floor — correct, they really are that short); both posts rescale at 2560 (max 22px, pitch 28.5) with clearance intact; zero console errors; collapse/reveal/hover unchanged.
  - Round-2 drafts kept for the record in the session artifact: quiet scale (prop ticks + viewport segment), true positions (prop gaps), **bars (built)**, filling mark (active fills with section depth), positions+fill. The fill idea is the natural revisit if continuous depth ever feels missed — it composes with bars.
- **2026-08-03 (three catches from Ameya's review of the fluid build, all fixed; TOC placement question opened)**:
  - **The hero status line is Nunito now.** It was the last words-in-mono holdout, grandfathered as "the filing role" — but the standing rule is sharper than the role: mono is for the numbering system, and the moment it touches words it reads as a second font. Uppercase + tracking carry the kicker voice alone; weight 500, same as the rail's "ON THIS PAGE".
  - **The masthead appears in place — the headroom slide is dead.** Quarto's headroom.js translates the fixed header up/down on scroll direction. On a header with a visible bar that reads as the bar returning; on a *transparent* header — just a name — the eye has nothing to attribute the downward motion to except the page, so it read as the page being shoved down (Ameya's report, and the mechanism checks out: the header never actually reflows anything, `body` padding is constant — the *perception* was the bug). Now `transform: none !important` with headroom's own classes driving `opacity` instead: the name dissolves on scroll-down, returns in place on scroll-up, 200ms on the site curve — the exact grammar the rail already speaks, so the margin furniture now recedes and returns as one system. `pointer-events: none` while hidden; transition zeroed under reduced motion.
  - **The figure outset is symmetric now** (`margin-inline: -5.882rem`, was `margin-right: -11.765rem`). Right-only outset put every enlarged figure visibly right of the column's own axis — off-centre against its heading and caption (Ameya's screenshot). Same display width, split evenly, centred on the text column like a Distill/Tufte breakout. The caption stays pinned to the column edge (`margin-left: 5.882rem` compensates for the box's new left overhang). Verified equal overhang left/right at 1200/1440/2560, caption at exactly the prose edge, 38px clearance to the rail at the tightest width. Also: `figure img { display: block; margin-inline: auto }` so the induction post's assorted-width figures centre in the box instead of hugging its left edge.
  - **Rail vertical-centering is REJECTED by Ameya** ("doesn't look right") — see the amendment under Layer 2. Not yet changed; concepts under review before anything is built.
- **2026-08-03 (the fluid foundation)**: **The whole system now scales from one declaration; 17px becomes the calibration root, not the root.** Everything in `styles.scss` is restated so that type, rhythm, grid, shell, cap and outsets breathe together with the viewport, and at exactly 1440px the fluid page is **pixel-identical** to the fixed page it replaces — every number in this document remains true, read as "at calibration."
  - **The root**: `--bs-root-font-size: clamp(16px, 14.4px + 0.18vw, 19px)` in the token block (Bootstrap emits `:root { font-size: var(--bs-root-font-size) }`, so redefining the property wins the cascade; `$font-size-root: 17px` stays only to make Bootstrap emit the rule at all). Anchored at two points: 17px @ 1440 viewport, 19px @ 2560; floor 16px below ~880px, where phones sit at the classic 16.
  - **The grid is rem** (`$grid-body-width: 40.706rem`, sidebar/margin `17.647rem`), which is what makes the article column *genuinely widen* — 593px measure @ 1440 → 663px @ 2560 — while the line stays a constant ~58 zeros (~75 mixed characters) because the type grew by the same factor. **Wider column, same measure — that is the whole trick**, and why the column was not simply set wider in px. Quarto derives ~20 further widths from these with px arithmetic (`math.min(500px, …)`, `+ 200px`) that dies on rem inputs — but every derived var is `!default`, and **Sass skips a `!default` RHS entirely when the var is already set** (verified against Quarto 1.9's dart-sass). All ~20 are pre-computed in rem in the defaults block, Quarto's formula in each comment. A future Quarto adding new px arithmetic fails the compile loudly — a tripwire, not a timebomb.
  - **Rhythm in rem** (px-at-17 ÷ 17, three decimals, px noted in comments): paragraph 2.529, h2 4.235/1.706, h3 3.059/1.294, figures 5.647, epistemic 3.294, code+math 2.824, header 3.765/6.824, listing pill 0.706, caption 0.824. Tokens too: `--measure-pad 1.412rem`, `--shell 68.235rem`, `--page-max 84.706rem` (the cap must scale or it re-pins everything the rem grid unpinned — it now reaches ~1609px @ 2560), figure outset `−11.765rem`, rail `12.941rem`, code block 0.765rem/19.529rem.
  - **Deliberately still px**: hairlines (1px doesn't thicken with a viewport), radii, the rail's 12px marks (fixed so length says *nothing* — and their `calc(0.75rem − 6px)` top self-centres at every root), and **media-query breakpoints** (they describe the viewport, which does not scale with the root; they also stay aligned with Quarto's own 992/1200).
  - **One measure bug fixed while in there**: the homepage listing body was unbounded — descriptions ran the full ~1000px shell column at 95+ characters per line on the same site that argued for ~75. Capped at `40rem` (the hero lede's measure); the hover pill still spans the full row.
  - **Measured** (Chrome, served): root 16.00/16.24/16.70/16.99/17.86/19.00 across 375→2560; zero horizontal overflow at any width; masthead and rail marks flush at every width (both scale on the same inset: 25.5 @ 1440 → 28.5 @ 2560); filing row one line ≥1280; figure 793 → 886px with caption pinned at 40rem; gutter numerals hang proportionally; the rail's collapse behaviour untouched.
- **2026-08-03 (direction reset — the base becomes the whole project)**: Three calls from Ameya, made looking at the panda-less build, that redraw the map above:
  - **The site's ambition is restated: a showboat portfolio, award-level, not just a place that posts.** Coming here eventually: posts, notable GitHub repos, published papers, "maybe more." The hero therefore **stays a full screen** — the collapse-to-index option was considered and rejected. The consequence: the hero has to *earn* that screen with type and composition alone now, and the homepage grows from a two-post listing into a curated index of several work types. The publication grammar (filing lines, numerals, marks) is the system that scales to that — a repo and a paper are just differently-typed entries in the same catalogue.
  - **Nunito committed, alone** — see open decision #2, now resolved.
  - **Sidenotes rejected** (the posts genuinely have none to hoist, and retrofitting notes onto finished research is backwards — the empty margin idea dies on content, same lesson as Layer 5's "the other half is paragraph length"). **In its place: the fluid-layout pass** — widen the post measure and make the whole site *reflexive* to the viewport. Ameya's read: "there is probably a lot about this website that isn't reflexive, but needs to be," and measured, he's right — the system is pinned at 692/300/300px grid columns, an 1160px shell, a 1440px cap, a −200px figure outset, and four guessed breakpoints (620/800/992/1300). The honest constraint to carry into that pass: the ~75ch measure argument from Layer 1b was real, so "wider" cannot mean "longer lines" — the column and the type should scale *together* (fluid root size, measure held in ch), with the freed width going to figures and apparatus first.
- **2026-08-03 (pandas off)**: **The hero troupe is disabled — Ameya's call, and a real diagnosis, not a retreat.** His read: *"I'm trying to incorporate whimsy when the underlying minimalism doesn't stand by itself. Two incomplete pieces still make an incomplete piece."* That is the governing formula's own logic turned back on the build order — **severity buys whimsy, and severity that hasn't finished paying for itself can't buy anything.** ma5a's bunnies work because her base would survive without them; ours was leaning on the pandas to carry a hero the type wasn't carrying alone. What changed: the `#panda-stage` div, the engine loader script, and the colophon came out of `index.qmd`. Nothing is deleted — `assets/pandas/` stays in place and in `_quarto.yml` resources, the favicon (a static cel, art not motion) stays, and engine development continues in the panda-engine repo untouched. Layer 3 is **paused, not cancelled**: the re-entry condition is that the minimal site reads as finished with the hero empty. The current focus is one thing — make the base stand out through pure design.
- **2026-08-03 (type)**: **Inter → Nunito, site-wide.** Ameya's call. One substitution in three places — the Google Fonts link in `_quarto.yml`, `$font-family-sans-serif` in `styles.scss`, and `font.sans-serif` in `figures.mplstyle` — plus two consequences.
  - **`font-feature-settings: "cv01","ss03"` removed.** Those are Inter's stylistic sets (single-storey `a`, disambiguated `l`); on any other family they are inert. `font-variant-ligatures: none` stays.
  - **`design/fonts/` re-vendored** as `Nunito-Regular/Medium/SemiBold.ttf` (~130KB each), cut from the variable font at wght 400/500/600 with `fontTools.varLib.instancer` — matplotlib reads named static faces only, never variation axes, so the three weights `plot.py` asks for have to exist as files. The Inter TTFs were deleted.
  - **Re-measured 2026-08-05 — resolved.** The ~25-character rail budget survives the typeface change: Nunito costs 7.4–8.0px per character against Inter's ~7, and the label box is 184px rather than the 188–198 previously quoted. Net, the same number, plus a 20-character floor for wide-letter headings. See the standing rule under Layer 5.
- **2026-08-03 (hero)**: **The hero copy and layout rebuilt together — coordinates prose in an editorial Z, and the fence learns to be several rectangles.**
  - **Copy.** A survey of 13 personal-site heroes (Emil, Rauno, Paco, ma5a, Carlini, Lilian Weng, gwern, Matuschak, Linus Lee; Neel Nanda / colah / Comeau / Steph Ango have *no hero at all*) showed nobody writes a headline their archive has to cash — at two posts that ruled out thesis headlines ("I take language models apart…" reads as a check the listing can't cover). Landed on coordinates: **"Studying computer science, physics, and AI — researching mechanistic interpretability."** (Ameya's construction; Penn State demoted to the lede, honors parenthetical cut, "physics-grade computation" cut). The old headline's real bugs were the dateline ("Right now") and the hedge ("trying to"), not its instinct for specificity.
  - **The perishable line became apparatus.** "Currently: …" is the one sentence that goes stale, so it now sets as a mono filing line — `CURRENT EXPERIMENT · DOES ANTHROPIC'S GLOBAL WORKSPACE RESULT REPLICATE?` — over the links (`.hero-filing`). The hero now speaks the post pages' grammar: title → standfirst → filing. Mono here is the existing apparatus role, not a new material; updating the experiment is a one-line edit at 13px instead of a headline rewrite.
  - **Layout: the Z.** Headline spans the shell (`width: fit-content`, explicit `<br>` at the em dash), lede answers from `margin-left: 44%`, filing corner closes back at the left edge. Headline clamp max dropped **3.25rem → 2.75rem**: the copy is ~25% longer and 2.75rem is where it sets as two even lines at the dash instead of three stacked ones — still the page's one scale event. Below 800px (the engine's own stage floor) the Z collapses to a left stack and the `<br>` hides (with a guard space after it — hiding a `<br>` deletes the break *and* leaves no whitespace; caught at 375px as "AI —researching").
  - **The fence is now one rect or an array.** The Z's point is the walkable pocket mid-left, and a single `.hero-inner` bounding box would swallow it. Engine change made in **panda-engine** (synced here): `inForbid`/`crossesFence`/`detourCorner` accept arrays (corners pool across rects), `freeArea` subtracts each, host `computeFence` returns one rect per `cardSelector` match. Single-rect behavior byte-identical — the trainer's corpora are frozen against it. New `test/geometry.test.js` (the load-bearing case: the pocket *between* two rects is walkable); 217 pass, determinism lint clean. site.js (site-owned) now fences `.hero-headline, .hero-lede, .hero-filing`; `fit-content` on headline and filing is what keeps their rects honest — a full-shell block fences the empty right sliver too.
  - Measured at 1440×900: two-line headline (46.75px), three fence rects, 18 pandas, no console errors. **Known, pre-existing, not fixed:** the Writing cue's text starts ~2px below the 900px fold — `.hero-screen` sits at top 270px where the `min-height` calc assumes 9.5rem (161px) of masthead; the discrepancy predates this change.
  - The concept ladder from the research round (observer vantage → knock-nudges-a-letter → rare letter-pull → the Phase-E walker on the headline's cap-height) is logged in the session artifact; **the knock reaching the type** is Ameya's picked next step, and it needs per-letter spans that today's markup deliberately did not add yet.
- **2026-08-03 (later)**: **The rail collapses to one mark instead of to nothing — Layer 2's idea, none of Layer 2's shapes.** Ameya's proposal was that the words *morph* into Rauno's lines on scroll-down and return on scroll-up or hover. Adopted, with the object changed.
  - **The hole it closes.** Fading the whole rail to `opacity: 0` had two costs the previous entry did not name. (1) **The affordance was invisible** — `pointer-events` is deliberately left on so the empty gutter still takes a hover, but that only serves a reader who already knows to reach there; anyone who scrolls straight down never learns the rail exists. (2) **A TOC does two jobs and only one is navigation.** The other is *position*, which is passive and continuous — you do not summon it, you glance at it. Hiding everything made "where am I" something you had to ask for. Navigation can be summoned; position cannot.
  - **NOT Rauno's ruler of horizontal dashes**, and the reason is the cohesion rule from Layer 1b: *few materials used often*. A horizontal tick would appear in exactly one place on the whole site, and it is the single most recognisable thing about his rail — morphing into it does not launder it, it delays it by 200ms. What survives instead is a **1px × 12px vertical hairline**, which is (a) the oldest mark in the trade, the printer's change bar, and therefore native to the publication grammar Layer 1b is built on, and (b) *already in the stylesheet* as the active link's `border-left`. Zero new materials.
  - **SIX marks, not one — corrected same day after Ameya saw it built.** The first pass left only the active section's mark, on the reasoning that the scrollbar already reports length and progress. Seeing it: *"this doesn't tell the reader anything."* Correct, and the failure is diagnosable — **a mark alone in an empty margin has nothing to be measured against.** It can say *here* but not *here, out of six*, and not *near the end*, which is most of what position actually means. Marks are legible only as a set; a lone one is a dot. The rest now sit in `--gray-800` with the active one in full ink, which is the same gray→ink ladder the labels use, so the rail ranks one way rather than two.
  - **The numerals are gone from the rail.** Once every section has its own mark, a number beside that mark is the same fact stated twice, and the mark is the version that survives the collapse. This ends the "one mono material, two places" pairing from Layer 1b — **the article's h2 gutter numerals are untouched and still mono**, they just no longer have a counterpart in the rail. Worth a decision later on whether the gutter numerals still earn their keep alone; they were left in because they identify sections *in the article*, which is a job the rail's marks cannot do.
  - **Removing them freed the geometry.** The labels' 53.55px hanging indent existed only to clear an absolutely-positioned numeral; at `padding-left: 1.25rem` the anchors sit flush and the marks inherit the far-left line the numerals used to hold — the one shared with the masthead and the kicker. That is the right holder for it now, because the marks are what is on screen while the reader reads. Verified flush at x=25.5 from 1024 to 2560px. Labels also wrap less at the wider measure.
  - **Mark length is FIXED at 12px**, because the `border-left` it replaces took the anchor's height — 25px beside a one-line label, 63px beside a three-line one — a mark whose length looks like it encodes something and in fact encodes word-wrap.
  - **Mark SPACING was uneven and is now fixed at the source — see the one-line heading rule under Layer 5.** The first build left it at 26/44/63/44/44px, because each mark sits beside its own label and labels wrapped to one, two or three lines. Two mechanical fixes were available and both were bad: detaching the marks from the labels means hovering reveals words that do not line up with the marks that summoned them, and clamping the labels to two lines costs the ends of the longer titles. Ameya's read went past both — *"the lines are unevenly spaced because some of the headers are multiline… shorten the headers."* **The rail was not the thing that was wrong; the headings were long.** Now exactly 25.484px between every mark on both posts. This is the second time on this page that a layout problem turned out to be a content problem (the first: paragraph length, which no amount of `margin-bottom` could fix — see the honest-limit-on-spacing note in Layer 1b).
  - **The morph is a horizontal collapse, not a crossfade.** Labels and numerals go `transform: scaleX(0)` about `transform-origin: left`, which — because the anchor already carries `margin-left: -0.75rem` — is *exactly* the column the mark occupies. The words compress into the thing that replaces them. A crossfade at the same coordinates would be a dissolve, and a dissolve in peripheral vision reads as a rendering fault where a directional collapse reads as an object changing zoom level. Needed **no new JS and no markup change**: the numeral is a `::before` on the anchor, so one transform carries label and number together and they cannot drift apart mid-collapse. The mark is parked on the `li` rather than the anchor precisely so it sits *outside* that transform.
  - **Asymmetric timing, and this is the load-bearing rule. Three cases, not two** (split and slowed 2026-08-03 on Ameya's read that the motion could be gentler):

    | trigger | direction | timing |
    |---|---|---|
    | scroll down | collapse | **280ms** + 15ms stagger down the list |
    | scroll up | reveal | **200ms**, no stagger |
    | hover / Tab | reveal | **instant** |

    The rule is not "hide slow, show fast" — it is **who asked, and with what**. A pointer landing in the gutter or a Tab key pressed is a request for the rail *right now*, and a request answered on a curve is answered late (Emil's rule 2 on high-frequency interactions, rule 3 on keyboard-initiated actions). Reversing the scroll is not a request — it is the reader changing from reading to looking, which is a mode change and reads better carried than cut. **So scroll gets motion in both directions and the pointer gets none.** The stagger stays on the collapse only: coming back, the rail should arrive as one object rather than unrolling, because by then the reader is looking for something in it.

    All three fall out of the existing selectors with no extra class: `[data-toc="full"]` matches only after a scroll-up; the sunk rule excludes `:hover`/`:focus-within`; and hovering while sunk therefore matches *neither*, falling through to a base rule that declares no transition at all. Per-element duration stays inside the <300ms budget; the collapse gesture as a whole runs to 355ms on the last item.
  - **Accent removed from the rail entirely** (Ameya: use Emil's bolding-vs-gray instead). See the amendment under Layer 1b — the active tick was the one accent job that broke the accent's own interaction-only rule. Rank is now gray-1000/400 → gray-1200/500. Verified across all 12 TOC items on both posts that the 400→500 weight change **does not rewrap a single label**, so the active item moving as you scroll cannot reflow the rail. That is tight rather than guaranteed; a future title long enough to sit one word from a wrap would break it, and the fix then is ink-only.
  - **Three bugs found, all pre-existing, all fixed:**
    - **The active tick had never rendered — not once, in any state.** Quarto ships `#quarto-sidebar` with `class="… overflow-auto"`, making its padding box a clipping box whose left edge *is* the rail's left edge. The anchor's `margin-left: -0.75rem` puts the tick 12.75px outside it, so it was clipped. The vermillion tick that Layer 1b describes as "the only coloured mark on a resting page" was invisible for its entire life. **The bug hid the only thing that would have revealed it.** Fixed by moving the *clip edge* rather than the mark: `--rail-bleed: 1.5rem` pulls the box left and pads the contents back by the same amount, so every child lands exactly where it did and only the clip changes. `overflow: auto` is left intact, so a TOC taller than the viewport still scrolls instead of spilling. Scoped to ≥992px — below that Quarto turns this element into an off-canvas menu with its own transform.
    - **Deleting our `border-left` handed the rail back to Quarto's**, which puts a full-height rule on the active link — the exact variable-length mark the fixed 12px one replaces. It is kept as `1px solid transparent`, a shim rather than a style, and not `none`: the numerals are positioned against the anchor's *padding* box, so removing the border would walk the whole numeral column 1px off the line it shares with the masthead.
    - **The rail was drawing Bootstrap's default blue focus ring.** The global `:focus-visible` is (0,1,0) and Bootstrap ships `.nav-link:focus-visible` at (0,2,0), so the rail alone on the site had a non-palette colour in it. It matters more here than anywhere else: keyboard is the *only* route by which a reader who never scrolls up can retrieve a collapsed rail, so that ring is the entire keyboard affordance, not a rare state. Restated at (1,2,0).
    - **The rail's hover was fading over 150ms** while every other link on the site changes instantly, which Layer 1's link rule forbids in as many words. Bootstrap ships `.nav-link { transition: color .15s, background-color .15s, border-color .15s }`. It never touched opacity or transform, so the collapse was unaffected and **the leak stayed invisible until the reveal timings were split apart and measured** — the instant case was the only one that could expose it. `transition: none` on the base, which is also what the hover and focus reveals now fall through to in order to be instant.
  - **Verified** (Chrome, real wheel events, served over HTTP — note `quarto.js` is a module and dies on CORS under `file://`, which silently disables the scroll-spy and makes the marks look frozen at section 01; this cost a detour and is worth remembering before diagnosing the rail again): **six** marks and **exactly one** in ink at every scroll position on both posts, index advancing 0→5 monotonically, zero numerals remaining; masthead / kicker / marks all flush at x=25.5 with the clip edge at x=0 at 1024 / 1280 / 1440 / 1920 / 2560, so the focus ring clears too; no horizontal overflow at any width; bleed correctly absent at 800px; homepage untouched; hover and Tab both reveal instantly while `data-toc` stays `sunk`; focus ring is the site's own vermillion, not Bootstrap's blue; `prefers-reduced-motion` zeroes duration and delay.
- **2026-08-03**: **Layer 5 run on the IOI post — the first post to actually be built to this system rather than retrofitted with CSS.** Backup at `design/backups/ioi-circuit-2026-08-02/`.
  - **Prose.** Rewritten to the Emil rhythm: 70 paragraphs, mean 1.7 sentences, only 2 over three sentences (was 4–8 throughout). 2,361 words across the same six h2s. Every measurement, claim and hedge preserved; what changed is where they sit.
  - **Figures rebuilt from the data, against `figures.mplstyle`** — the style file had been written but never applied to anything. Six figures, all exported at exactly **1280px** wide. The old set ran 1128–1730px and was displayed at one width, which is a 1.5× swing in apparent type size across figures in the same post.
    - **`savefig.bbox: tight` was the enemy of the one-width rule and is now banned in the style file.** It crops the canvas to the content, so the first restyled set came out at 1132, 1165, 1240, 1247, 1248 and 1249px from six figures all declared at 8.0in. `figure.constrained_layout.use: True` instead — fit the content inside a fixed canvas, never the reverse.
    - **In-figure titles dropped.** The figcaption below and the h2 above already name the figure; a bold headline baked into the PNG was a third heading in the same 200px, set in a typeface and size the page cannot control.
    - **One accent.** The old set used four different "this is the finding" colours across six figures (tab10 blue, vermillion, ink, plus two colormap directions). Now `#d55e00` everywhere and at most once per figure — the same colour a hovered link takes.
    - **Canvas is `#fdfdfc`, not white** (style file amended). Figures are unframed, so a white PNG drew a faint rectangle exactly where the border was removed from.
    - **Two figures merged, one added.** The L8H6 and L9H9 attention maps were separate figures at separate widths that the prose read as a pair — now one two-panel figure on a shared scale. The new one is a DLA × patching scatter of every head in layers 8–11; it plots the post's thesis instead of asserting it, from data both sweeps had already produced.
    - **The BOS column is masked** in the attention panels. The sink takes 0.74 / 0.86 of the mass averaged across rows, and the old figures spent two prose sentences and two caption clauses telling the reader to look underneath it. Masking is a real editorial choice and the post now says so.
  - **Inter is vendored** at `design/fonts/` (3 static weights, 1.2MB) and registered by `plot.py`. The style file named Inter first but it is loaded from Google Fonts, so matplotlib had always silently fallen back to DejaVu Sans.
  - **Two numbers were wrong and are corrected in the post** — see its section 4 and the revision note. (a) The published `0.24` vs `0.10` path-patching comparison was between two different interventions: `0.24` went into block 9's whole residual input, `0.10` into one head's query. Held at one receiver type, all of block 9's queries recover `0.09` and L9H9's alone `0.10` — so the single edge carries essentially the whole query-side effect, which is a stronger claim than the one it replaces. (b) DLA is not blind to the upstream and negative heads generally; it ranks four of the five named heads 1st, 6th, 143rd and 144th of 144. Exactly one — L8H6, 138th — is invisible to it. A top-10 chart sorted descending is what made that look like a pattern.
  - **DLA is now measured on the same 100 templated prompts as the two patching sweeps** (it used to come from `transformer_lens`'s own `IOIDataset`). Three methods being compared to each other have to share a dataset. Headline numbers moved: L9H9 `+2.35 → +3.54`, L9H6 `+0.96 → +1.24`, mean clean logit diff `+3.86 → +3.52`, accuracy `100% → 99%`.
  - **Three CSS bugs found by the rewrite, all pre-existing, all fixed:**
    - **`p:has(> em:only-child)` was demoting ordinary prose to the apparatus tier.** `:only-child` counts *element* children and text nodes are not elements, so `<p>prose with <em>one</em> italic word.</p>` matched exactly as well as a fully-italic paragraph did. Long paragraphs hid it; 1–3 sentence paragraphs made a single emphasised word the only element over and over, and four paragraphs of argument silently dropped to 14px behind a rule. No selector can express "this element is the only node here" — **asides are now `::: {.epistemic}`, explicitly**, in both posts. (`.aside` is reserved: Quarto uses it to move content to the margin column.)
    - **The filing-row fix from 2026-08-02 had never applied.** It was written `#title-block-header .quarto-title-meta` (1,1,0); Quarto ships `#title-block-header.quarto-title-block.default .quarto-title-meta` (1,3,0) and won. The row had been sitting in two `1fr` columns the whole time, invisible only because the empty second column had nothing in it. Adding `date-modified:` exposed it. Now a flex row at the matching specificity — and the induction post's date + categories finally sit on one line, which is what that entry claimed in the first place.
    - **The date→categories gap was a `margin-left` on the categories**, so a filing line long enough to wrap put the categories on a second line still carrying a 28px indent. It is a `column-gap` on the header now, which only applies between items sharing a line.
  - **`date-modified:` is supported**, rendering as `· revised <date>`. A research site that quietly edits a wrong number in place is worse than one that shows the date it stopped being wrong.
  - **The filing row rebuilt again, same day, after Ameya's read: one line, date left, categories hard right, one size step smaller.** `--text-filing: 0.75rem` (12.75px) is a new token and the apparatus tier's **floor** — nothing else may use it. The reasoning is a distinction the tier didn't previously make: captions and the TOC are apparatus you *read*, the filing line is apparatus you *refer to*. Nobody reads a date, they check it. At 14px it had the same presence as a figure caption while carrying a fraction of the meaning, and under a title that had just been given the page's one scale jump, the header read bottom-heavy.
    - Right-alignment is `margin-left: auto` on `.quarto-categories` — a flex item eats the free space on its line — so the column's own measure does the separating rather than a fixed gap, and the two groups can never look like one run-on chain. The leading middot came off the first category (it used to hang off the date; now it dangles off nothing) and survives only *between* categories.
    - **Capped at two categories** via `:nth-of-type(n + 3) { display: none }`, plus `white-space: nowrap` on each. The one-line guarantee is a function of how much text is in the row, so it is enforced in CSS rather than left to whatever a future post's front matter declares. All categories still ship in the HTML for search and Quarto's category index.
    - Under 620px `margin-left` resets to 0 and the categories sit under the date, left-flush.
    - **Third time a filing-row rule lost a specificity fight** — the `max-width: 620px` override was written `#title-block-header .quarto-categories` (1,1,0) against its own (1,2,0) parent rule and did nothing. Any rule in this block needs the `.quarto-title-block` class on it, and Quarto's own `.default` variants need `.quarto-title-block.default`.
  - **The header slimmed, third pass — the standfirst and the hairline rule both came out.** The post header is now TITLE → filing line, and nothing else. Two objects were removed for one reason each: the standfirst because the epistemic block a few lines below was already summarising the post in the same register (two summaries stacked is the post starting twice), and the rule because with the standfirst gone it had nothing left to separate — and a rule between a title and its own date argues they are different things when they are the same thing. `description:` stays in the front matter and still feeds the homepage listing, the OG card and the meta description; it is hidden only in the header. Header-to-epistemic gap went 80px → **116px** (four lines of the 28.9px rhythm) and is now the header's only separator. Reference: makemepulse disperses date, keywords and summary down the page instead of compressing them into one block; the cheapest version of that idea is to stop stacking and start spacing.
  - **The TOC rail sinks to its numerals — Ameya's option A, with Rauno's placement.** Full labels while the title block is still on screen; once it scrolls away the kicker and the labels fade and the mono numerals remain. Hover or keyboard-focus brings them back. `assets/site/toc-rail.html` sets `data-toc="full" | "sunk"` on `<html>` from an IntersectionObserver on a sentinel at the foot of the header — a threshold that means "you have stopped orienting and started reading", not a pixel count.
    - **Nothing moves.** The rail keeps its full width and every numeral keeps its position in both states; only `opacity`/`color` changes. That satisfies the transform-and-opacity-only motion rule for free and means the collapse can never reflow the article. The "shrink" is entirely how much ink is in the corner of your eye, which was the complaint.
    - **Chosen over building Layer 2's tick minimap.** The numbering system is already on the page in two places, so collapsing to it costs zero new materials where ticks would add a shape the site does not otherwise use, to do a job the numbers already do. It is also a strict subset of the minimap, so Layer 2 is not foreclosed.
    - **The active section keeps its label.** Six numerals plus one short label is still a fraction of six wrapped labels, and without it the sunk rail is decoration that has to be hovered before it says anything. Remove the `:not(.active)` to collapse it all the way.
    - **Rauno's placement**: `position: sticky; height: 100vh` with the rail centred inside it, and hard left. Sticky, never `position: fixed` — see the gotchas above. **Quarto already ships `#quarto-sidebar { flex-direction: column }`, so `align-items: center` centres HORIZONTALLY**; both axes have to be named.
    - **Alignment chain fixed.** Measured, the sidebar box already sat on the same 26px screen inset as the masthead, but Quarto inset `#TOC` a further 20px and the `ul` another 6px. Both zeroed, so masthead, kicker and numerals share one vertical edge. The active tick now hangs 12px into the margin (`margin-left: -0.75rem` on the anchor, padded back) so the **numerals** hold the line rather than the anchor box — they are what survives the collapse, so they are what has to align.
    - **Regression caught: the 100vh sidebar was painting over the article's section numerals.** Quarto gives the sidebar `background-color: #fdfdfc`; harmless while the box was six links tall, but at 100vh and `position: sticky` it became a full-height opaque positioned panel covering the whole left band — and the h2 numerals hang in that band at `left: -3.4rem`. `background: transparent` fixes it. This is why "04" was rendering as "4" in the gutter.
    - Quarto emits the rail's kicker as `<h2 id="toc-title">` with **no class**, so a `.toc-title` selector alone matches nothing.
  - **The rail rebuilt again on direction, not depth.** Ameya's read: *"when scrolling down, I think all text should disappear, and only reappear when scrolling up."* That is the better signal and the reason is worth keeping: a depth threshold answers *has the reader started?*, but the question a TOC exists to answer is *where am I / where can I go*, and a reader only asks that when they stop moving forward. **Scrolling down is reading and reading wants an empty margin; scrolling up is looking for something, which is the one moment a rail earns peripheral vision.** So the whole rail goes — numerals, kicker, active label, all of it — and returns the instant the scroll reverses. Hover and keyboard focus still reveal it.
    - This also collapsed the implementation to **one `opacity` on one element**, replacing the per-element `color: transparent` juggling (which had a cascade oddity where the active link kept its ink despite a (1,5,1) selector). Fewer moving parts, same motion-spec compliance.
    - **Hysteresis is the whole difficulty.** A naive `y > lastY` flips on every trackpad wobble and strobes. Movement is accumulated and the state only flips past 24px of travel in one direction, with the accumulator reset on reversal so a long run down cannot hide a short bounce up inside a net figure. Thirteen cases unit-tested, including jitter and the bounce-inside-a-run case.
    - This one genuinely needs a scroll listener — direction cannot be observed with an IntersectionObserver — so it is passive, stores a number, and defers all work to rAF. The rest of the site's scroll-spy stays on IntersectionObserver.
  - **The masthead and the rail now hold the WINDOW's left inset, not the capped grid's.** `--page-max` centres `#quarto-content`, which kept the name and the rail flush with each other but pushed them inward together on anything wider than 1440px — at 1920px both sat 266px from the edge, floating in the middle-left rather than holding a margin. The article column still lives in the capped, centred grid; the margin furniture escapes it. The masthead simply stops tracking `--page-max`; the rail pulls back by exactly the cap's overflow with `margin-left: min(0px, calc((var(--page-max) - 100vw) / 2))`, which is a no-op below the cap. Verified flush at 1440 / 1920 / 2560.
    - Note this reverses part of the 2026-08-02 reasoning for capping the grid ("the rail is stranded in ~670px of nothing"). That concern was real when the rail was a permanent block of labels; a rail that is absent while you read and vertically centred when present does not read as stranded, it reads as a margin.
  - **The filing-row breakpoint took four tries and the lesson is worth keeping**: 620px, 800px, 992px, then **1300px**. The first three were guesses at "when does the text stop fitting". The real answer is a statement about the LAYOUT: the justified one-line row needs the full 646px measure, and the full measure needs the whole grid (sidebar 300 + body 692 + margin 300 ≈ 1300px). 992px was the most tempting and still wrong — it is where Quarto collapses to one column, but the measure is already being squeezed well above it.
  - Deferred: the induction post's own Layer 5 pass, and its quote-grammar table (still a PNG).
- **2026-08-02**: Outside design review (award criteria + research-publishing conventions, measured against shipped CSS from Distill, transformer-circuits, ciechanow.ski, thesephist, rauno.me, Tufte CSS). Diagnosis: the base is well executed but **under-composed** — one width for everything (the 300px margin column was allocated and empty on every page, zero footnotes, zero outset figures), 80ch at 16px, a WCAG-failing tertiary gray, an unbounded grid that sprawled at wide viewports, and four different accent colours across eleven figures while the page itself had none. **Layer 1b built** in response (see above): apparatus mono role, section numbering, accent resolved and spent on interaction only, figures outset, 17px/1.70, contrast fix, grid capped, view transitions, OG/Twitter/favicon, `figures.mplstyle` spec. Deferred by decision: no content edits (so the quote-grammar table stays a PNG and the induction post stays without a repo link), and the panda engine's more intentional use is a later conversation — the NN is not ready and development stays in the panda-engine repo.
- **2026-07-10**: Inspiration research (galleries + site dissections of emilkowal.ski / devouringdetails.com / ma5a.com, exact measurements via shipped CSS). Layer 1 built, workshopped (homepage got the wide hero back after the pure-Emil version felt too stripped; TOC un-flattened after breaking in a real browser), and approved as the base.
- **2026-07-22**: Whimsy direction amended — Niche mascot retired; identity-native **panda troupe** adopted for the hero (whimsy.md candidate 6); ambient autonomous motion permitted inside the hero fence as one system. Workshop ran four rounds (flat SVG → procedural 2-bit sprites ×2 → pivot); landed on a faithful replica of ma5a's "panda collision" pen (MIT via CodePen, attributed) as the build base — her sprite art + constants, engine reimplemented. Replica: `design/sketches/panda-collision-replica.html` + live artifact. Next pass (pending Ameya's verdicts in whimsy.md): recolor to sand, dǒulì hat, calmer hero-fenced behavior, then `assets/pandas/` + hero include. Note: her engine animates margins with CSS transitions — conflicts with the transform-only motion rule; resolve when porting.
