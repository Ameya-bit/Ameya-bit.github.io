{
  "name": "panda-trainer",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Rollout harness, corpus tooling and the viewing game for the learned line-walker. Phases B and C of design/panda-policy-net.md. Imports the shipped engine directly (../assets/pandas/engine) — never a copy — so a corpus is always a recording of the machine the site runs.",
  "scripts": {
    "test": "node --test",
    "bench": "node bench.js",
    "cut": "node cut.js",
    "eval": "node evaluate.js"
  }
}
