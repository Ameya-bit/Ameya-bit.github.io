%-------------------------
% Resume in LaTeX — Ameya Panchal
% Harvard OCS template styling: Carlito (Calibri-metric), centered name
% over a rule, centered title-case section heads, two-line entries with
% right-aligned meta. Single column, no tables, ATS-parsable.
%
% SITE-OWNED COPY (2026-08-04). This file is the DESIGN layer only: the
% preamble is verbatim from /Users/ameya/resume/Resume.tex and is the part
% that IS the standard — edit it to change how the PDF looks. The marked
% content region inside the document body is filled by tools/resume/build.py
% from resume.toml; never write content here. (Never name the marker in a
% comment either — build.py fills every occurrence, and its first casualty
% was this very header.)
%-------------------------

\documentclass[letterpaper,10pt]{article}

\usepackage{fix-cm}
\usepackage[T1]{fontenc}
\usepackage[sfdefault,scale=1.05]{carlito}   % metric-compatible with Calibri
\usepackage{microtype}
\usepackage{sansmath}   % keeps the one math bit ($\chi^2$) in the body sans
\usepackage[margin=0.5in,top=0.45in,bottom=0.4in]{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}
\urlstyle{same}   % \nolinkurl prints URLs as plain body text, not typewriter
\input{glyphtounicode}

% Machine-readable PDF for ATS
\pdfgentounicode=1

\pagestyle{empty}
\raggedright
\linespread{1.0}\selectfont


%----------DESIGN TOKENS----------
% Spacing scale: every vertical gap in the document is one of these.
\newlength{\gapXS}\setlength{\gapXS}{1pt}
\newlength{\gapS} \setlength{\gapS}{3pt}
\newlength{\gapM} \setlength{\gapM}{7pt}
\newlength{\gapL} \setlength{\gapL}{11pt}

% Type scale
\newcommand{\sizeName}{\fontsize{14.5}{17}\selectfont}
\newcommand{\sizeContact}{\fontsize{10}{12.5}\selectfont}
% body = \normalsize (10pt class x 1.05 carlito scale = 10.5pt)

% Width of the right-hand meta column (dates, locations)
\newlength{\metawidth}\setlength{\metawidth}{1.6in}

% Inline separator. \allowbreak makes it the only legal break point in the
% contact line, so URLs wrap between items instead of mid-token.
\newcommand{\sep}{~\textbullet~\allowbreak}


%----------SECTION HEADS----------
% Centered, bold, title case -- the Harvard OCS convention.
\titleformat{\section}[block]
  {\centering\bfseries\normalsize}
  {}{0em}{}
\titlespacing*{\section}{0pt}{\gapL}{\gapS}


%----------ENTRY BLOCKS----------
% Two-line entry: bold heading + meta on the first line, detail line +
% meta on the second. Built from parboxes, not tabulars, so the text
% layer extracts linearly.
% \leavevmode is load-bearing: a \parbox[t] whose first content is a
% whatsit aligns on that instead of the first text baseline.
\newcommand{\entryline}[3]{%
  \noindent\makebox[\linewidth]{%
    \parbox[t]{\dimexpr\linewidth-\metawidth-\gapS\relax}{\raggedright\leavevmode #1}%
    \hfill
    \parbox[t]{\metawidth}{\raggedleft\leavevmode #2}%
  }\par\vspace{#3}%
}

% #1 heading  #2 meta (location)  #3 detail line  #4 meta (dates)
\newcommand{\entry}[4]{%
  \addvspace{\gapM}%
  \entryline{\bfseries #1}{#2}{\gapXS}%
  \entryline{#3}{#4}{0pt}%
}

% Single-line entry, for blocks with no detail line.
\newcommand{\entryone}[2]{%
  \addvspace{\gapM}%
  \entryline{\bfseries #1}{#2}{0pt}%
}

% Link line: sits directly under an entry's meta line, above the bullets.
% Small type so a long URL reads as a reference, not as body content.
\newcommand{\entryurl}[1]{%
  \vspace{\gapXS}%
  \noindent{\footnotesize\nolinkurl{#1}}\par
}


%----------BULLETS----------
\newlist{points}{itemize}{1}
\setlist[points]{
  leftmargin=1.5em,
  labelsep=0.6em,
  label={\raisebox{0.15ex}{\footnotesize\textbullet}},
  topsep=3.5pt,
  itemsep=2.5pt,
  parsep=0pt,
  partopsep=0pt,
}


%----------SKILLS ROWS----------
% Bold inline label, running text after it -- no aligned label column.
\newcommand{\skillrow}[2]{%
  \noindent\textbf{#1:} #2\par\vspace{2pt}%
}


%-------------------------------------------
\begin{document}
\sansmath

@@CONTENT@@

%-------------------------------------------
\end{document}
