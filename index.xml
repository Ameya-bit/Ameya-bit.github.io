<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Ameya Panchal</title>
<link>https://ameya-bit.github.io/</link>
<atom:link href="https://ameya-bit.github.io/index.xml" rel="self" type="application/rss+xml"/>
<description>Mechanistic interpretability and machine learning notes.</description>
<generator>quarto-1.9.38</generator>
<lastBuildDate>Wed, 22 Jul 2026 04:00:00 GMT</lastBuildDate>
<item>
  <title>Every Method Has a Blind Spot: Reading GPT-2’s IOI Circuit Three Ways</title>
  <link>https://ameya-bit.github.io/posts/ioi-circuit/</link>
  <description><![CDATA[ 




<p><em>Confident in the measurements — logit differences, recovery grids, attention patterns, all reproducible from the four notebooks in the <a href="https://github.com/Ameya-bit/IOI">repo</a>. More exploratory where I read a circuit off a disagreement between methods. My second mechanistic interpretability post; corrections welcome.</em></p>
<p>Stack three interpretability methods on the same circuit and the temptation is to read their agreement as confirmation. The agreement is real. But the sharper signal is in the disagreement — each method reads one part of the circuit cleanly and is blind to the part the next one is built to see. This post walks the same GPT-2 circuit through direct logit attribution, activation patching, and path patching, and the through-line isn’t “I replicated IOI.” It’s what each method saw, and what it couldn’t.</p>
<p>The last post took a copying head apart from the <em>weights</em> — eigenvalues of an OV circuit, input-independent structure. This one works from the other side: activations, what a specific input actually produces. That seam between the two runs underneath everything here, and it shows up in the first result.</p>
<section id="the-task-and-the-metric" class="level2">
<h2 class="anchored" data-anchor-id="the-task-and-the-metric">The task and the metric</h2>
<p>The vehicle is Indirect Object Identification. Given</p>
<blockquote class="blockquote">
<p>When Mary and John went to the store, John gave a drink to ___</p>
</blockquote>
<p>the model should predict the indirect object <code>Mary</code> over the repeated subject <code>John</code>. Every run is scored by one number, the <strong>logit difference</strong> at the answer position,</p>
<p><img src="https://latex.codecogs.com/png.latex?%5CDelta%20=%20%5Coperatorname%7Blogit%7D(%5Ctext%7BIO%7D)%20-%20%5Coperatorname%7Blogit%7D(%5Ctext%7BS%7D)%20,"></p>
<p>which is <img src="https://latex.codecogs.com/png.latex?+3.36"> on that prompt and averages <img src="https://latex.codecogs.com/png.latex?+3.86"> across a hundred templated prompts, at 100% accuracy. GPT-2 small solves this cleanly; the question is <em>how</em>.</p>
<p>To get causal traction we need a corruption — a minimal edit that flips the answer. The one used throughout is the <strong>S2 swap</strong>: change the second <code>John</code> to <code>Mary</code> (<em>“…the store, Mary gave a drink to”</em>), which makes <code>John</code> the intended answer and drives <img src="https://latex.codecogs.com/png.latex?%5CDelta"> to <img src="https://latex.codecogs.com/png.latex?-2.48">. Everything downstream is measured as <strong>normalized recovery</strong> against those two poles,</p>
<p><img src="https://latex.codecogs.com/png.latex?%5Ctext%7Brecovery%7D%20=%20%5Cfrac%7B%5CDelta_%7B%5Ctext%7Bpatched%7D%7D%20-%20%5CDelta_%7B%5Ctext%7Bcorrupted%7D%7D%7D%7B%5CDelta_%7B%5Ctext%7Bclean%7D%7D%20-%20%5CDelta_%7B%5Ctext%7Bcorrupted%7D%7D%7D%20,"></p>
<p>so <img src="https://latex.codecogs.com/png.latex?0"> means the intervention did nothing and <img src="https://latex.codecogs.com/png.latex?1"> means it fully restored clean behavior. One metric, three ways of attributing it.</p>
</section>
<section id="direct-logit-attribution-who-writes-the-answer" class="level2">
<h2 class="anchored" data-anchor-id="direct-logit-attribution-who-writes-the-answer">Direct logit attribution: who writes the answer</h2>
<p>The cheapest question first. Which heads write <em>directly</em> into the logit difference? DLA decomposes the residual stream at the answer position into per-component contributions, maps each head’s output back through its <img src="https://latex.codecogs.com/png.latex?W_O">, applies the final LayerNorm scaling, and projects onto the answer direction <img src="https://latex.codecogs.com/png.latex?W_U%5B:,%5Ctext%7BIO%7D%5D%20-%20W_U%5B:,%5Ctext%7BS%7D%5D">. No interventions, one forward pass, and the per-head projections sum back to the total <img src="https://latex.codecogs.com/png.latex?%5CDelta"> — a clean ranking of direct responsibility.</p>
<p><strong>The name-mover head L9H9 dominates, at <img src="https://latex.codecogs.com/png.latex?+2.35">, with L9H6 a distant second at <img src="https://latex.codecogs.com/png.latex?+0.96">.</strong> Everything else is noise by comparison.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig1_dla_ranking.png" class="img-fluid figure-img"></p>
<figcaption>Top-10 heads by direct logit attribution to the IO−S direction, averaged over 100 prompts. L9H9 carries more than twice the next head; the tail past L9H6 is a scatter of minor contributors. On a single prompt L9H6 and L9H9 are nearly tied (+2.11 vs +2.10) — averaging is what separates them, which is the first hint that one hand-written prompt is a shaky place to read a circuit.</figcaption>
</figure>
</div>
<p>Here is the seam with the last post. In the weights-only view, a head’s copying strength is the copying score — and by that metric L9H9 scores <strong>0.26</strong>, near the bottom of all 144 heads, while genuine high-copiers like L11H3 sit at <img src="https://latex.codecogs.com/png.latex?1.00">.</p>
<p>The weight-only reading would have walked right past the single most important head in the circuit, because the copying score measures <em>copies what it attends to</em>, not <em>attends to the right thing</em>. DLA finds L9H9 precisely because it folds in the actual attention on this input. Structure in the weights is not the same as structure that gets used.</p>
<p>What DLA cannot do is see anything that doesn’t write straight to the logits. It found the name movers and stopped, because the heads that <em>feed</em> the name movers contribute nothing directly to the answer direction — their effect is routed through L9. To see them, the measurement has to become causal.</p>
</section>
<section id="activation-patching-whats-necessary-and-when" class="level2">
<h2 class="anchored" data-anchor-id="activation-patching-whats-necessary-and-when">Activation patching: what’s necessary, and when</h2>
<p>Activation patching runs the corrupted prompt but splices a single clean activation back in, one location at a time, and scores the recovery. Where DLA sees only direct writes, this catches components whose effect is routed through later layers — the price is a forward pass per location instead of one.</p>
<p>The first sweep patches the residual stream across every <code>(layer, position)</code>. It answers a question no static weight reading can even phrase: not just <em>where</em> the answer-relevant information lives, but <em>when</em>.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig2_resid_patching.png" class="img-fluid figure-img"></p>
<figcaption>Residual-stream patching over (layer, position), mean normalized recovery over 100 prompts. Two bright bands and nothing else. The signal lives at position 10 — the second, repeated subject (S2) — through the early layers, then goes dark above layer 8. At the same depth it reappears at position 14, the END token, and stays lit to the top. Averaging over 100 prompts barely smears the map, which felt like a broken experiment until you realize that <em>is</em> the result: the circuit is stable enough that one prompt and a hundred draw the same picture.</figcaption>
</figure>
</div>
<p>The handoff is the whole story. Early layers carry the answer information at the repeated-name position; around <strong>layer 8 it migrates to the END position</strong> and rides the residual stream out to the logits. That is a <em>temporal</em> claim — information living in one place, then another — and it is the clearest thing patching buys over reading weights, which can only ever describe a static map of what a head <em>could</em> do.</p>
<p>The second sweep localizes heads: patch each head’s output at END and measure recovery.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig3_head_patching.png" class="img-fluid figure-img"></p>
<figcaption>Attention-head patching at the END position, layers 8+, mean recovery over 100 prompts. The brightest cells are not the name movers. S-inhibition heads L8H6 and L8H10 light up hardest; the name mover L9H9 is positive but dimmer; L10H7 goes strongly negative and L11H10 mildly so — the negative name movers, heads that push <em>against</em> the answer. The name mover reading dimmer than the S-inhibition heads is not it mattering less. Denoising one of several redundant name movers understates it, because the others cover for it (self-repair); the S-inhibition signal, by contrast, is exactly what the S2 corruption flips, so patching reads it at full strength.</figcaption>
</figure>
</div>
<p>This is where the methods start to disagree in a way that’s informative rather than annoying. DLA read the name movers cleanly and barely registered L8. Patching reads L8 cleanly and <em>understates</em> the name movers. Neither is wrong — each is bright exactly where its own machinery is sensitive. Put them side by side and you get the roster the paper predicts: <strong>S-inhibition at L8H6 / L8H10, name movers around L9H9, negative name movers at L10H7 / L11H10.</strong></p>
<p>But patching localizes <em>nodes</em>. It tells you L8 matters and L9 matters. It does not tell you that L8 matters <em>by talking to L9</em> — that the edge between them is real and not two independent contributions that happen to co-occur. For that you need to hold everything else still.</p>
</section>
<section id="path-patching-the-wire-itself" class="level2">
<h2 class="anchored" data-anchor-id="path-patching-the-wire-itself">Path patching: the wire itself</h2>
<p>Path patching isolates a single <strong>sender → receiver</strong> edge. The hypothesis, handed up by the previous two sweeps, is that the S-inhibition heads feed the name mover. Testing it took the most care of anything here — my first attempt quietly rebuilt plain activation patching, because patching the sender and reading the output lets its effect flow down <em>every</em> path, not the one edge I wanted.</p>
<p>The real construction freezes everything. Run clean and corrupted, caching both. Then re-run with <strong>every head and MLP frozen to its clean value except the sender</strong> (L8H6, set to corrupted), so the sender’s effect propagates but nothing else moves.</p>
<p>Grab the resulting input to the receiver — the query into the L9 name movers — and stash it. In a final run, patch <em>only that stashed query</em> into <code>blocks.9.attn.hook_q</code> and score. Freezing everything but the sender is what turns a node measurement into an edge measurement.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig6_path_patching.png" class="img-fluid figure-img"></p>
<figcaption>Path patching the S-inhibition → name-mover edge, as a fraction of the full clean→corrupted swing. Routing the sender’s effect into the entire block-9 query input recovers 0.24; restricting it to L9H9’s query alone recovers 0.10. The gap between them is the rest of the name-mover family carrying the other half of the composition.</figcaption>
</figure>
</div>
<p><strong>Patching the whole block-9 query input recovers ~0.24 of the effect; restricting the path to L9H9’s query alone recovers ~0.10.</strong> So roughly a tenth of the entire clean→corrupted swing flows specifically along the L8H6 → L9H9 query edge, and about a quarter along the full S-inhibition → name-mover family. This is the thing DLA had to ignore and node-patching could only assume: a measured edge, not a co-occurrence.</p>
</section>
<section id="verification-do-the-labels-hold" class="level2">
<h2 class="anchored" data-anchor-id="verification-do-the-labels-hold">Verification: do the labels hold?</h2>
<p>Head numbers matching a paper’s head numbers proves nothing on its own. If the labels are right, the attention patterns should show each head reading what its name claims.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig4_l8h6_sinhibition.png" class="img-fluid figure-img"></p>
<figcaption>L8H6 attention. Read past the bright BOS column first — the attention sink soaks up most of the mass on every row and says nothing about the task. Behind it, the END row and the “gave” row put their real weight on <code>John</code>, the repeated subject. An S-inhibition head reads the duplicated name; that is what it is for.</figcaption>
</figure>
</div>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig5_l9h9_namemover.png" class="img-fluid figure-img"></p>
<figcaption>L9H9 attention, same BOS caveat. Behind the sink, the END row attends to <code>Mary</code> — the indirect object, the answer. The name mover copies the correct name to the output position. Together the two panels trace the edge path patching measured: L8H6 reads the repeat, L9H9 writes the answer.</figcaption>
</figure>
</div>
<p>The BOS column is the honest caveat here. It dominates both maps, and reading these patterns means deliberately looking underneath it — an easy way to see a signal that isn’t there, or miss one that is. With that discount applied, the labels hold: 8.6 reads the subject, 9.9 writes the object, and the circuit the three methods reconstructed is the circuit the attention shows.</p>
</section>
<section id="four-views-one-circuit" class="level2">
<h2 class="anchored" data-anchor-id="four-views-one-circuit">Four views, one circuit</h2>
<p>Nothing here contradicts anything else. The methods agree on the answer. What they don’t share is their blind spots, and lining those up is the actual result:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>Method</th>
<th>Gives you</th>
<th>Blind to</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>Weights</strong> (last post)</td>
<td>input-independent structure</td>
<td>what a given input actually uses</td>
</tr>
<tr class="even">
<td><strong>Direct logit attribution</strong></td>
<td>direct contribution to the logits</td>
<td>anything routed through later layers</td>
</tr>
<tr class="odd">
<td><strong>Activation patching</strong></td>
<td>causal importance, and a <em>temporal</em> story</td>
<td>the wiring between components</td>
</tr>
<tr class="even">
<td><strong>Path patching</strong></td>
<td>the sender → receiver edge</td>
<td>— but needs the most careful construction</td>
</tr>
</tbody>
</table>
<p>Read top to bottom, each row’s blind spot is the next row’s headline. Weights miss what’s used; DLA finds what’s used but only at the output; patching finds the upstream heads but not their wiring; path patching supplies the wiring. The copying score buried L9H9; DLA surfaced it but couldn’t see L8; patching surfaced L8 but couldn’t prove it feeds L9; path patching drew the edge. You don’t get the circuit from any single method. You get it from knowing what each one can’t tell you, and reaching for the one that can.</p>
<p><em>The four notebooks — DLA, activation patching, path patching, and the attention-pattern checks — are in the <a href="https://github.com/Ameya-bit/IOI">repo</a>; everything above is reproducible from them on CPU.</em></p>


</section>

 ]]></description>
  <category>mechanistic interpretability</category>
  <category>transformers</category>
  <guid>https://ameya-bit.github.io/posts/ioi-circuit/</guid>
  <pubDate>Wed, 22 Jul 2026 04:00:00 GMT</pubDate>
  <media:content url="https://ameya-bit.github.io/posts/ioi-circuit/figures/fig2_resid_patching.png" medium="image" type="image/png" height="88" width="144"/>
</item>
<item>
  <title>An Induction Head in Disguise: Chasing Grammar in a Character-Level Transformer</title>
  <link>https://ameya-bit.github.io/posts/induction-head/</link>
  <description><![CDATA[ 




<p><em>Confident in the measurements — copying score, OV logits, attention patterns. More exploratory on the interpretations of what the head “knows,” two of which I ended up disconfirming myself. My first mechanistic interpretability post; corrections welcome.</em></p>
<p>I trained a small character level transformer on Nietzsche texts, named it Niche, and now I interpret on it. Feel free to ask them questions on highlighted excerpts or in the bottom right.</p>
<p>Numbers can be flattering, but it’s the control that breaks, diverts, or makes a conclusion. In trying to understand a particular head in a custom transformer, the conclusion changed frequently. We chased a copying head, quotation grammar, and parenthesis grammar, with the latter two disconfirmed. The takeaway is having identified a copying head with the OV circuit half of a bracket-completion mechanism, but a QK circuit insufficient to route the attention needed to complete it.</p>
<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The setup</h2>
<p>The transformer is basically a residual stream with 6 blocks feeding into it, and an embed matrix and unembed matrix on either side. Each block contains 4 attention heads, and a general MLP of Linear, Activation, Linear. This post focuses on just the attention heads, which can be separated into 2 key mechanisms, the QK and OV circuits. The QK circuit is how the head decides what to attend to within the current and previous tokens. The OV circuit decides that given we are attending to a specific token, what token should be boosted when picking the next one.</p>
</section>
<section id="finding-a-copying-head" class="level2">
<h2 class="anchored" data-anchor-id="finding-a-copying-head">Finding a copying head</h2>
<p>The elusive copying head is the first goal, utilizing the eigenvalue method proposed in the paper “A Mathematical Framework for Transformer Circuits” (Elhage et al.). Iterating through all blocks and heads, we compute the eigenvalues for the OV circuit matrix. A positive eigenvalue means that passing an eigenvector through the OV matrix results in a scaled vector, in the same direction. In other words, it is the same token representation. However the eigenvalues can range to complex numbers as well. So for one head, there can be a mix of complex, positive, and negative eigenvalues. From here, we have to compute a kind of copying score to better characterize whether a head is copying. It is just taking the sum of the head’s eigenvalue real parts (up to the matrix’s rank, 64 here) and dividing by the sum of their magnitudes. The imaginary part of complex eigenvalues will drive this to 0. <strong>B5H0 (Block 5 Head 0) is identified as the only contender.</strong></p>
<p>Concretely, for head <img src="https://latex.codecogs.com/png.latex?h"> the OV circuit is the token-to-token map</p>
<p><img src="https://latex.codecogs.com/png.latex?W_%7B%5Cmathrm%7BOV%7D%7D%20=%20W_U%20W_O%5E%7Bh%7D%20W_V%5E%7Bh%7D%20W_E%20,"></p>
<p>whose eigenvalues <img src="https://latex.codecogs.com/png.latex?%5Clambda_1,%20%5Cdots,%20%5Clambda_n">, sorted by magnitude and truncated to the top <img src="https://latex.codecogs.com/png.latex?r"> (the matrix rank), give</p>
<p><img src="https://latex.codecogs.com/png.latex?%5Ctext%7Bcopying%20score%7D%20=%20%5Cfrac%7B%5Csum_%7Bi=1%7D%5E%7Br%7D%20%5Coperatorname%7BRe%7D(%5Clambda_i)%7D%7B%5Csum_%7Bi=1%7D%5E%7Br%7D%20%5Clvert%20%5Clambda_i%20%5Crvert%7D%20,%20%5Cqquad%20r%20=%20%5Cfrac%7Bd_%7B%5Cmathrm%7Bmodel%7D%7D%7D%7Bn_%7B%5Cmathrm%7Bhead%7D%7D%7D%20=%2064%20."></p>
<p>With a copying score of 0.615, a spike relative to the others, it proposes the idea that a copying head does exist. Naturally, you would ask what it copies.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/induction-head/figures/fig2_spike_copying_score.png" class="img-fluid figure-img"></p>
<figcaption>Copying score for every block-by-head combination across all 24 attention heads; B5H0 spikes to 0.615 while the next-highest head sits at 0.35.</figcaption>
</figure>
</div>
</section>
<section id="what-does-it-copy" class="level2">
<h2 class="anchored" data-anchor-id="what-does-it-copy">What does it copy?</h2>
<p>Eigenvectors are a dead end. They’re already in token basis, so it’s readable. However, to do that we need to cancel out the norms of the embed and unembed matrices which dominate the reading. But we can’t do this with eigenvectors because it mixes all tokens together, there isn’t a scalar value to cancel out the norms. So we divert to reading logits directly off the diagonal of the OV circuit — entry <img src="https://latex.codecogs.com/png.latex?(i,%20j)"> of <img src="https://latex.codecogs.com/png.latex?W_%7B%5Cmathrm%7BOV%7D%7D"> is how much attending to token <img src="https://latex.codecogs.com/png.latex?j"> boosts token <img src="https://latex.codecogs.com/png.latex?i">, so the diagonal is each token’s self-copy strength while an off-diagonal entry is one token boosting a different one. Here we can divide out the norms of the embed and unembed matrices, because the diagonal is per-token, we can compute the norms per token as well. This reveals high logits for many tokens, solidifying its status as a copying head.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/induction-head/figures/fig1_make_ov_diagonal.png" class="img-fluid figure-img"></p>
<figcaption>Raw versus norm-normalized OV self-copy logits for B5H0’s top tokens; dividing out the embed and unembed norms sharpens the copy signal instead of deflating it, and the quote logit stays at the top.</figcaption>
</figure>
</div>
</section>
<section id="quote-grammar" class="level2">
<h2 class="anchored" data-anchor-id="quote-grammar">Quote grammar</h2>
<p>But beyond that, it had the highest logit for quotes at 0.594. This means that when attending to <code>"</code>, the logit for <code>"</code> goes up. Maybe this head knows quotation grammar, the rules are simple enough, every quote just needs to be paired. It just needs to be shown that the QK circuit attends to unpaired <code>"</code> over other characters. So we throw in a couple test sentences — a handful of hand-written prompts, not a systematic sweep, so read these controls as suggestive rather than conclusive — and read off the attending tokens from B5H0’s attention pattern. But this control reveals the head attends to tokens that follow previous iterations of the query token. <strong>So quote grammar was a dead end, but also proves this head to be an induction head.</strong></p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/induction-head/figures/fig3_divert_induction_stripe.png" class="img-fluid figure-img"></p>
<figcaption>B5H0 attention on a content-swap pair; the bright induction cell tracks the swapped token (cat to ‘a’, dog to ‘o’), the query attending to the character that followed the query’s own prior occurrence.</figcaption>
</figure>
</div>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/induction-head/figures/fig4_break_quote_grammar.png" class="img-fluid figure-img"></p>
<figcaption>Per-prompt verdicts for the quote tests; the top attended token is the induction successor rather than a matching quote, with the honest misses and position-0 sinks kept in. Two stay in: P3-A (repeat) tops out on ‘r’ at <img src="https://latex.codecogs.com/png.latex?w%20=%200.14"> — a genuine miss, not the induction successor — while P3-B (norepeat) and P5-B (prior-closes) collapse onto the position-0 attention sink (up to <img src="https://latex.codecogs.com/png.latex?w%20=%201.00">) when no induction cue is present.</figcaption>
</figure>
</div>
</section>
<section id="parenthesis-grammar" class="level2">
<h2 class="anchored" data-anchor-id="parenthesis-grammar">Parenthesis grammar</h2>
<p>Before writing it off completely, the OV self-copy logits for the brackets are also interesting:</p>
<ul>
<li><code>(</code> at <img src="https://latex.codecogs.com/png.latex?+0.227"> — the OV circuit copies open brackets,</li>
<li><code>)</code> at <img src="https://latex.codecogs.com/png.latex?-0.339"> — and suppresses close brackets.</li>
</ul>
<p>So it may be able to create opening parenthesis structures while preventing unnecessary closing ones. This reads as a semblance of parenthesis grammar, but not quite. Checking the logit of ‘)’ when attending to ‘(’ and vice versa could fill in those blanks:</p>
<ul>
<li>attending to <code>(</code> <em>increases</em> the logit for <code>)</code> at <img src="https://latex.codecogs.com/png.latex?+0.556">,</li>
<li>attending to <code>)</code> <em>decreases</em> the logit for <code>(</code> at <img src="https://latex.codecogs.com/png.latex?-0.105">.</li>
</ul>
<p>This could be learned parenthesis grammar. The most notable is the 0.556, when it sees an open parenthesis, it wants to close it. It is tempting, it looks very much like parenthesis grammar. If QK reliably attends to parentheses, and better yet if it can understand parenthesis structure, it is basically what we’ve been chasing after. So similar to quote analysis, we throw in a couple sentences and check the attending tokens. And the QK circuit generally does attend to parentheses, but not meaningfully enough to be considered grammar. <strong>The head doesn’t understand bracket positioning, and just attends to the latest parenthesis.</strong> This also holds back the nested grammar that the diagonal suggested, nested parentheses require bracket position understanding, specifically depth tracking to know when to close parentheses.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://ameya-bit.github.io/posts/induction-head/figures/fig5_break_paren_grammar.png" class="img-fluid figure-img"></p>
<figcaption>Per-prompt verdicts for the parenthesis tests; B5H0 attends to brackets but never distinguishes one that still needs closing from one already closed, so it just tracks the latest bracket. The near-ties the verdict column hides: in C8 (<code>A (B (C) D</code>) the still-open ‘(’ at position 2 (<img src="https://latex.codecogs.com/png.latex?w%20=%200.11">) loses to the already-closed ‘(’ at position 5 (<img src="https://latex.codecogs.com/png.latex?w%20=%200.12">), so there is no depth tracking; and P7 unmatched (‘(’ at 3, <img src="https://latex.codecogs.com/png.latex?w%20=%200.40">) versus matched (‘(’ at 3, <img src="https://latex.codecogs.com/png.latex?w%20=%200.18">; ‘)’ at 11, <img src="https://latex.codecogs.com/png.latex?w%20=%200.39">) shows attention following the latest bracket, not whether it needs closing.</figcaption>
</figure>
</div>
</section>
<section id="what-the-head-actually-is" class="level2">
<h2 class="anchored" data-anchor-id="what-the-head-actually-is">What the head actually is</h2>
<p><strong>As we said initially, this is a copying head with an OV circuit that knows how to handle parentheses, but the QK circuit is blind to bracket structure, and thus can’t utilize it properly.</strong> It’s a small, concrete example of a mechanism sitting half-built in the weights — the OV circuit here already “knows” bracket completion — yet functionally inert because a second circuit never learned to feed it: the kind of latent, partial structure worth watching for when we reason about what a model can and can’t do.</p>
<p><em>The model this post dissects is awake on this page. Select any passage and ask Niche to explain it — the stripes in the panel are B5H0’s attention, live.</em></p>
<link rel="stylesheet" href="../../assets/niche/ask-niche.css">
<script type="module" src="../../assets/niche/ask-niche.js"></script>


</section>

 ]]></description>
  <category>mechanistic interpretability</category>
  <category>transformers</category>
  <guid>https://ameya-bit.github.io/posts/induction-head/</guid>
  <pubDate>Fri, 10 Jul 2026 04:00:00 GMT</pubDate>
  <media:content url="https://ameya-bit.github.io/posts/induction-head/figures/fig2_spike_copying_score.png" medium="image" type="image/png" height="63" width="144"/>
</item>
</channel>
</rss>
